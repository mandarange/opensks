# Native Multisession Proof Install Evidence

The relocatable macOS archive proof passed with `scripts/install-macos-local-smoke.sh --clean --archive`. The run created `.opensks/macos/OpenSKS-local-macos.zip`, and the receipt recorded these fields as true:

- `archive_verified`
- `archive_relocatable`
- `archive_workspace_override_smoke_verified`
- `archive_codesign_verified`
