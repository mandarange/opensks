# OpenSKS 0e9dad0 Provider Hyperparallel Implementation Ledger

Authoritative work order: `$HOME/Desktop/opensks-0e9dad0-provider-hyperparallel-production-work-order-ko.md`

Goal objective source: `$CODEX_HOME/attachments/9f601177-8745-42e1-a34e-e65110080c0f/goal-objective.md`

Product language contract: user-facing UI, logs, reason codes, schemas, and identifiers are English.

## Baseline Seal

| Command | Result | Evidence |
|---|---:|---|
| `git status --short` | Passed | Clean worktree at start. |
| `git rev-parse HEAD` | Passed | `0e9dad03fd1070619f1bbeb089d40da0f9bc59cf` |
| `git log -1 --oneline` | Passed | `0e9dad0 feat: harden runtime release gates` |
| `cargo metadata --format-version 1 --no-deps` | Passed | Cargo workspace metadata resolved. |
| `bash scripts/check-architecture-ownership.sh` | Passed | Architecture guard reported all checks passed. |
| `cargo run -p xtask -- architecture-graph --check` | Passed | `architecture-graph: ok` |
| `cargo run -p xtask -- direct-io-audit --check` | Passed | `direct-io-audit: ok` |

## Route And Context Notes

| Item | Status | Evidence |
|---|---|---|
| SKS user prompt hook | Blocked | `sks hook user-prompt-submit` failed with `SKS update migration blocked: update_migration_lock_held`. No SKS mission artifacts were materialized from the hook in this run. |
| TriWiki context pack | Verified | `.sneakoscope/wiki/context-pack.json` was read before implementation. It emphasizes current-docs policy, evidence hydration, and avoiding unsupported completion claims. |
| Baseline commit delta | Verified | HEAD exactly matches the authoritative baseline, so there is no post-baseline user delta to audit at mission start. |

## Latest Verification

| Command | Result | Evidence |
|---|---:|---|
| `swift test --disable-sandbox --package-path swift --scratch-path /tmp/opensks-swift-build-provider-readiness --filter OpenSKSStudioTests.ProviderTests` / `swift-format lint swift/Sources/Providers/ProviderStore.swift swift/Tests/OpenSKSStudioTests/ProviderTests.swift` / `cargo run -- acceptance audit` / `sks validate-artifacts latest` / `sks wiki validate .sneakoscope/wiki/context-pack.json` | Partial | Swift Provider Center readiness now converts known provider adapter-check blocker codes into safe action text before exposing them in `ProviderConnectionViewModel.adapterBlockers`. The diagnostic list no longer shows raw codes such as `configure_OPENROUTER_API_KEY_credential`; it shows user-facing actions like adding an OpenRouter/OpenAI API key credential in Provider Center or Keychain, while unknown/secret-like blocker text remains redacted. ProviderTests passed 19 tests, including render coverage and secret-redaction assertions. `swift-format lint` exited 0 with the existing Provider warning backlog. Acceptance remains 23 total, 22 passed, 1 partial, 0 failed because live remote provider probe credentials/opt-in are still absent. |
| `cargo test provider_commands_write_zero_leak_registry_probe_and_usage --locked` / `cargo clippy --all-targets -- -D warnings` / `cargo fmt --all --check` / `cargo run -- provider adapter-check` / `cargo run -- acceptance audit` | Partial | Provider adapter-check now prints each top-level non-secret blocker to stdout as `blocker: ...`, so the remaining `mvp-004` actions are visible without opening `.opensks/providers/provider-adapter-check.json`. Current local stdout lists `set_OPENSKS_ALLOW_REMOTE_PROVIDER_PROBE_1`, `configure_OPENROUTER_API_KEY_credential`, and `configure_OPENAI_API_KEY_credential`; the report still keeps `secret_value_exposed:false` and the focused provider command regression proves no `sk-`/bearer leak in the artifact. Acceptance remains 23 total, 22 passed, 1 partial, 0 failed because the live remote `/models` probes still require real credentials plus opt-in. |
| `cargo test -p opensks-cli release_proof --locked` / `cargo test -p opensks-retention release_proof --locked` / `cargo test -p opensks-cli supervisor_tick_claims_one_queued_accepted_turn --locked` / `cargo clippy -p opensks-cli -p opensks-retention --all-targets -- -D warnings` / `cargo fmt --all --check` / `cargo run -- release proof` | Passed | Release proof blockers are now more actionable while preserving the existing `opensks.release-proof.v1` blocker shape: when tracked workspace changes block same-SHA artifact binding, the CLI-provided `workspace_dirty` blocker includes the tracked path count, up to five sample paths, and remaining count, and `release proof` stdout prints each `blocker: code - message` before the artifact path. The retention core now adds its generic `workspace_dirty` blocker only when a caller has not already supplied a specific one, preventing duplicate blockers. Current local release proof still correctly remains `NotVerified`; stdout and the artifact both report `42 tracked paths` with samples instead of a generic dirty-workspace message. A stale CLI test fixture was also aligned with the optional legacy `settings` echo contract by wrapping the fixture settings in `Some(...)`. |
| `swift test --disable-sandbox --package-path swift --scratch-path /tmp/opensks-swift-build-turn-full --filter OpenSKSStudioTests.ConversationTurnTests` / `swift test --disable-sandbox --package-path swift --scratch-path /tmp/opensks-swift-build-ui-settings --filter OpenSKSStudioTests.ConversationUITests` / `git diff --check` / `cargo run -- acceptance audit` / `sks validate-artifacts latest` / `sks wiki validate .sneakoscope/wiki/context-pack.json` | Partial | Swift Chat settings save is now optimistic per conversation: `ConversationStore.updateThreadSettings` updates the target thread immediately, confirms with the repository-stamped saved row, and rolls back to the previous cached/default state on save failure. New gated regressions prove in-flight settings saves expose the optimistic `executionMode`/`maxParallelism` state before the service returns and restore the prior state plus error message on failure. `ConversationTurnTests` passed 31 tests, `ConversationUITests` passed 22 tests, whitespace passed, SKS artifact/wiki validation passed, and acceptance remains 23 total, 22 passed, 1 partial, 0 failed because `mvp-004` still requires live remote provider credentials plus explicit probe opt-in. |
| `/Applications/Xcode.app/Contents/Developer/Toolchains/XcodeDefault.xctoolchain/usr/bin/swift-format lint swift/Sources/Conversations/ConversationStore.swift swift/Tests/OpenSKSStudioTests/ConversationTurnTests.swift` | Passed with warnings | `swift-format lint` exited 0. The touched files still report the repository's existing broad style warnings, especially indentation/line-length/private-extension warnings; no hard formatting failure was reported. |
| Computer Use read-only state inspection of running `OpenSKS.app` | Passed | Native app accessibility inspection found the OpenSKS Studio window running from `.opensks/macos/OpenSKS.app`, header `Idle · agent ready`, provider indicator `1 provider`, branch context `main, 40 changed files`, footer `22 passed 1 partial 0 failed`, and `In progress 14 missions`. The selected conversation still showed `Failed` / `Needs setup — connect at least one code-capable model`, so this is native UI state evidence rather than a passing live-provider smoke. No GUI mutation was performed. |
| `sks naruto run "Continue working toward the active thread goal: implement the OpenSKS 0e9dad0 provider control center and hyperparallel production runtime work order while preserving user changes." --json` | Blocked | Naruto mission `.sneakoscope/missions/M-20260625-112058-3cfc` materialized a 32-clone / target-concurrency-25 work graph and worktree allocation, but returned `ok: false`; blockers again include Codex SDK event-stream/structured-output failures, missing patch envelopes for write-capable workers, worker closeout failures, and Supabase MCP auth with no access token. Parent-owned scoped implementation continued with this route limitation recorded. |
| `cargo test -p opensks-contracts turn_request_round_trips --locked` / `cargo test -p opensks-contracts conversation_turn_start_request_roundtrips_as_engine_request --locked` | Passed | `ConversationTurnStartRequest` now carries optional `thread_settings_updated_at_ms`, preserving old clients while adding a client-observed durable thread-settings revision for canonical snapshot validation. Contract round-trip and engine-request nesting both pass. |
| `cargo test -p opensks-conversation accept_conversation_turn_ --locked` | Passed | Four focused conversation repository tests passed, including new coverage that matching persisted thread-settings revision accepts, stale revision rejects before idempotency persistence, default no-settings revision `0` accepts, and the canonical persisted settings snapshot still ignores conflicting request `settings` echo values. |
| `cargo test -p opensks-daemon conversation_turn_start_rejects_stale_thread_settings_revision --locked -- --test-threads=1` / `cargo test -p opensks-daemon conversation_turn_start_snapshots_persisted_thread_settings --locked -- --test-threads=1` / `cargo test -p opensks-daemon conversation_turn_start --locked -- --test-threads=1` | Passed | Daemon stdio turn-start now rejects stale thread-settings revisions with an error event and no accepted handle, while matching revisions continue to snapshot canonical persisted settings. The broader 11-test turn-start filter passed after rerun; an initial combined run had one transient coalesced-drain failure that passed on immediate isolated rerun and on the full filter rerun. |
| `swift test --package-path swift --filter OpenSKSStudioTests.ContractsTests/testConversationTurnStartRequestEncodesTypedPayload` / `swift test --package-path swift --filter OpenSKSStudioTests.ConversationTurnTests/testSendPassesThreadSettingsAndContextRefs` | Passed | Swift encodes `thread_settings_updated_at_ms` in the turn-start payload, and the product `ConversationStore.send` forwards the durable `ConversationThreadSettings.updatedAtMs` alongside the compatibility `ConversationTurnSettings` echo plus selected context refs. |
| `cargo test -p opensks-contracts turn --locked` / `cargo test -p opensks-conversation accept_conversation_turn_ --locked` / `cargo test -p opensks-daemon conversation_turn_start --locked -- --test-threads=1` / `swift test --disable-sandbox --package-path swift --filter 'OpenSKSStudioTests.ContractsTests/testConversationTurnAccepted'` | Passed | `ConversationTurnAccepted` now returns the canonical `settings_digest` for the immutable settings snapshot accepted by the repository. Repository tests prove fresh accept and idempotent replay return the stored digest; daemon stdio tests prove the accepted line carries the same digest stored in `turns.settings_digest`; Swift contract tests decode and route the digest through the pending response router. Swift required `--disable-sandbox` plus `/tmp` cache env because this shell cannot use SwiftPM's default sandbox/cache paths. |
| `cargo run -p xtask -- schemas` / `cargo clippy -p opensks-contracts -p opensks-conversation -p opensks-daemon --all-targets -- -D warnings` / `cargo fmt --all --check` | Passed | Generated `schemas/engine-request.schema.json` includes `thread_settings_updated_at_ms`; focused clippy and Rust formatting passed for the contract/repository/daemon slice. |
| Context7 `/websites/openrouter_ai` reasoning-token docs / Context7 `/websites/developers_openai_api_reference` reasoning-effort docs | Passed | Current docs confirm OpenRouter chat completions use `reasoning: { effort: "low|medium|high|xhigh" }`, while OpenAI Chat Completions use `reasoning_effort`. This follow-up wires both provider-family request shapes and intentionally leaves OpenAI-compatible/custom/local endpoints without a reasoning field unless a future capability contract proves support. |
| `cargo test -p opensks-adapter provider_reasoning_effort_maps_turn_settings_to_reasoning_body --locked` / `cargo test -p opensks-daemon objective_planner_model_call_body_includes_provider_reasoning_effort --locked -- --test-threads=1` / `cargo test -p opensks-cli conversation --locked` | Passed | `ConversationTurnSettings.reasoning_effort` now flows into `AgenticConfig`; OpenRouter request bodies map `quick -> low`, `standard -> medium`, `deep -> high`, and `maximum -> xhigh` under `reasoning.effort`; OpenAI direct request bodies map to scalar `reasoning_effort`; daemon objective-planner/root/role/code-worker dispatch paths use provider-family wire selection, while OpenAI-compatible/custom/local endpoints receive no extra reasoning field. |
| `cargo clippy -p opensks-adapter -p opensks-daemon -p opensks-cli --all-targets -- -D warnings` / `cargo fmt --all --check` / `git diff --check` / `bash scripts/check-architecture-ownership.sh` / `cargo run -p xtask -- architecture-graph --check` / `cargo run -p xtask -- direct-io-audit --check` / `cargo run -- acceptance audit` | Partial | Focused clippy, formatting, whitespace, architecture ownership, architecture graph, direct-I/O audit, and acceptance audit all passed after the provider-family reasoning-effort slice. Acceptance remains 23 total, 22 passed, 1 partial, 0 failed; `mvp-004` still requires live remote provider credentials plus explicit probe opt-in. |
| `cargo test -p opensks-contracts integration_candidate --locked` / `cargo test -p opensks-daemon integration_candidate_apply --locked -- --test-threads=1` / `cargo test -p opensks-git-service commit_preview_and_receipt_link_matching_integration_final_diff --locked` | Passed | Integration candidate, candidate-selection, apply, and final-seal receipts now carry a sanitized `turn_settings` snapshot plus top-level `approval_policy_id`. The approved apply regression proves the snapshot carries pipeline, max parallelism, verifier count, tool policy, image model, and approval policy from candidate readiness through final seal; the awaiting-approval regression proves the snapshot remains visible without mutating the main workspace. This binds the effective turn settings to integration runtime receipts while preserving the existing approval-required gate. |
| `cargo run -p xtask -- schemas` / `cargo clippy -p opensks-contracts -p opensks-daemon -p opensks-git-service --all-targets -- -D warnings` / `cargo fmt --all --check` / `git diff --check` / `bash scripts/check-architecture-ownership.sh` / `cargo run -p xtask -- architecture-graph --check` / `cargo run -p xtask -- direct-io-audit --check` / `cargo run -- acceptance audit` | Partial | Generated integration candidate/selection/apply/final-seal schemas now include additive optional `approval_policy_id` and `turn_settings` snapshot definitions; schema generation also refreshed the current provider-connection enum to include existing `codex_lb`. Focused clippy, formatting, whitespace, architecture ownership, architecture graph, direct-I/O audit, and acceptance audit passed. Acceptance remains 23 total, 22 passed, 1 partial, 0 failed. |
| `cargo test -p opensks-cli thread_settings_default_then_persist_and_survive_relaunch --locked` / `cargo test -p opensks-cli conversation --locked` / `cargo fmt --all --check` / `git diff --check` | Passed | CLI `turn-start` now preserves durable thread `token_budget`, `cost_budget_usd`, and `timeout_ms` when constructing the immutable turn settings snapshot. The focused regression stores budget, timeout, and image-model settings, starts a turn, reads the persisted effective turn settings, and proves those fields survive into the turn snapshot instead of being reset to `None`. |
| `swift test --disable-sandbox --package-path swift --scratch-path /tmp/opensks-swift-build-pipeline --filter OpenSKSStudioTests.PipelineProjectionTests` / `swift test --disable-sandbox --package-path swift --scratch-path /tmp/opensks-swift-build-pipeline --filter OpenSKSStudioTests.LivePipelineTests` / `swift test --disable-sandbox --package-path swift --scratch-path /tmp/opensks-swift-build-pipeline --filter OpenSKSStudioTests.GraphViewTests` | Passed | Swift pipeline projections now preserve unrecognized run/node state strings as `.unknown` instead of presenting them as queued. The UI surfaces `Unknown state · migration required`, maps graph glyph/tint to a warning/question-mark state, omits unknown nodes from normal queued/active/completed counters, and does not offer pause/resume controls for unknown runs. |
| `swift test --disable-sandbox --package-path swift --scratch-path /tmp/opensks-swift-build-conversation-opstate --filter OpenSKSStudioTests.ConversationTurnTests` / `swift test --disable-sandbox --package-path swift --scratch-path /tmp/opensks-swift-build-conversation-opstate --filter OpenSKSStudioTests.ConversationUITests` | Passed | Swift Chat operation state is now keyed per conversation for send and thread-settings save. A gated-turn regression proves one conversation can have an in-flight `turnStart` while another conversation starts and records its own run; send refresh now reloads the target conversation read models instead of only the selected conversation. |
| `sks proof-field scan --intent "Add canonical thread-settings revision validation to conversation turn-start so stale Swift requests cannot race durable thread settings" ...` | Passed | Proof Field report `.sneakoscope/reports/proof-field-1782357266997.json` reported `Mode: balanced`, `Eligible: no`, `Contract clarity: 0.7`, and `Workflow complexity: focused (0.22)`, so this remained on the full Honest/Reflection closeout path rather than a fast-lane close. |
| `bash scripts/check-architecture-ownership.sh` / `cargo run -p xtask -- architecture-graph --check` / `cargo run -p xtask -- direct-io-audit --check` / `git diff --check` / `cargo run -- acceptance audit` | Partial | Architecture ownership, architecture graph, direct-I/O audit, whitespace, and acceptance audit all passed. Acceptance remains 23 total, 22 passed, 1 partial, 0 failed; `mvp-004` still requires live remote provider credentials plus explicit probe opt-in. |
| Computer Use read-only state inspection of running `OpenSKS.app` | Passed | Native app accessibility inspection found the OpenSKS Studio window running from `.opensks/macos/OpenSKS.app`, header `Idle - agent ready`, provider indicator `1 provider`, branch context `main, 122 changed files`, footer `22 passed 1 partial 0 failed`, and `In progress 14 missions`. The selected conversation still showed status `Failed`, so this is native UI state evidence rather than a passing product smoke. No GUI mutation was performed. |
| `sks naruto run "[@computer](plugin://computer-use@openai-bundled) ..."` | Blocked | Naruto mission `.sneakoscope/missions/M-20260625-110835-d6b4` prepared a 32-clone / target-concurrency-25 work graph and reported `parallel_runtime_proof: true`, but returned `ok: false`; blockers include Codex SDK event-stream/structured-output failures, missing patch envelopes for write-capable workers, agent lifecycle not fully closed, and Supabase MCP auth with no access token. Parent-owned local implementation and verification continued with this route limitation recorded. |
| `cargo test -p opensks-contracts turn --locked` / `cargo test -p opensks-conversation accept_conversation_turn_snapshots_persisted_thread_settings --locked` / `cargo test -p opensks-daemon conversation_turn_start_snapshots_persisted_thread_settings --locked -- --test-threads=1` | Passed | Focused contract/conversation/daemon tests prove `ConversationThreadSettings` now carries `token_budget`, `cost_budget_usd`, and `timeout_ms`, persists them on durable thread settings, snapshots them into accepted turn settings, and exposes them in the daemon turn-start path. |
| `cargo test -p opensks-scheduler conversation_turn_bootstrap_records_scheduler_root_item_idempotently --locked` | Passed | Scheduler bootstrap now carries accepted-turn budget limits into the root work item budget and records non-secret receipt fields `work_budget_kind=model_context_units`, `work_budget_amount`, `cost_budget_usd`, and `timeout_ms` in the `WorkItemQueued` event without tripping secret redaction. |
| `swift test --package-path swift --filter OpenSKSStudioTests.ContractsTests/testConversationThreadSettingsRoundTripsSnakeCaseWireContract` / `swift test --package-path swift --filter OpenSKSStudioTests.ConversationTurnTests/testSendPassesThreadSettingsAndContextRefs` | Passed | Swift contract and Chat turn tests prove the new settings round-trip as snake-case `token_budget`, `cost_budget_usd`, `timeout_ms` and are forwarded from durable thread settings into the daemon request. |
| `cargo test -p opensks-adapter artifact_tools --locked` / `cargo run -p xtask -- direct-io-audit --check` | Passed | `artifact.write` still passes focused adapter coverage after removing the remaining product-side direct parent-directory creation from `ToolGateway::artifact_write`; the direct-I/O audit reports `direct-io-audit: ok`. |
| `cargo test -p opensks-daemon integration_candidate_apply_applies_approved_candidate_to_main_workspace --locked -- --test-threads=1` | Passed | Integration apply now emits durable evidence for main-workspace mutation scope: `integration:path-write-lease`, `scheduler:path-scope-bound`, `scheduler:path-scope-external-write`, plus event payload fields `path_write_lease` and `path_scope` for target paths. This is an audit/proof payload for the existing approval-gated apply path, not a claim that every apply path is scheduler-lease enforced. |
| `cargo test -p opensks-daemon conversation_supervisor_tick_blocks_redacted_prompt_without_raw_vault --locked -- --test-threads=1` / `cargo test -p opensks-daemon conversation_supervisor_tick --locked -- --test-threads=1` / `cargo clippy -p opensks-daemon --all-targets -- -D warnings` | Passed | Supervisor execution now fails closed when the only durable user prompt contains redaction markers and no raw prompt vault is available. The regression proves a secret-like `local_test` prompt is redacted in the message store, does not execute a workspace write, records durable `raw_prompt_unavailable_after_redaction` evidence, fails the assistant message, and does not leak the secret-like token into event JSON. This is a safety guard for P1-002, not encrypted raw prompt storage. |
| `cargo test -p opensks-conversation encrypted_raw_content_is_separate_from_redacted_read_models --locked` / `cargo test -p opensks-conversation --locked` / `cargo clippy -p opensks-conversation --all-targets -- -D warnings` | Passed | The conversation repository now exposes explicit opaque encrypted raw-content storage APIs over the existing `messages.content_ciphertext` and `content_nonce` columns. The regression proves ciphertext can be attached to a redacted user message and fetched only through explicit ciphertext APIs, while message pagination, FTS search, conversation digest, and summary paths remain redacted and do not expose ciphertext or secret-like plaintext. This creates the storage contract needed for P1-002 raw prompt vault work, but does not yet implement Keychain-backed encryption/decryption. |
| `cargo test -p opensks-daemon conversation_supervisor_tick_restores_encrypted_raw_prompt_for_execution --locked -- --test-threads=1` / `cargo test -p opensks-daemon conversation_supervisor_tick_blocks_redacted_prompt_without_raw_vault --locked -- --test-threads=1` / `cargo test -p opensks-vault --locked` / `cargo clippy -p opensks-vault -p opensks-daemon --all-targets -- -D warnings` / `cargo fmt --all --check` | Passed | The daemon now bridges the repository raw-content storage to execution: when the durable searchable prompt contains `[REDACTED]`, it retrieves the turn's encrypted raw prompt ciphertext and decrypts it with a workspace raw-prompt-vault identity resolved from macOS Keychain service `dev.opensks.raw-prompt-vault` (test injection only under `cfg(test)`). The new regression proves a redacted local-test prompt executes from restored plaintext in the isolated worktree while message pages and event journal JSON do not leak the secret-like token. The existing fail-closed regression still passes when ciphertext or identity is unavailable. |
| `cargo test -p opensks-daemon conversation_turn_start_encrypts_raw_prompt_for_later_supervisor_restore --locked -- --test-threads=1` / `cargo test -p opensks-vault identity_text_encrypt_decrypt_roundtrips --locked` / `cargo test -p opensks-daemon --locked -- --test-threads=1` / `cargo clippy -p opensks-conversation -p opensks-vault -p opensks-daemon --all-targets -- -D warnings` | Passed | Daemon `conversation_turn_start` now producer-encrypts prompts whose searchable durable copy would contain `[REDACTED]`. It ensures a workspace raw-prompt-vault age identity exists through macOS Keychain generic-password APIs via the `security-framework` crate, avoiding secret material in command-line arguments; tests use a secure-store map. `ConversationRepository::accept_conversation_turn_with_raw_ciphertext` stores the ciphertext on the user message while the existing `accept_conversation_turn` API remains a wrapper. The regression proves turn-start provisions an identity, stores ciphertext, the message page remains redacted, supervisor later restores and executes the prompt, and neither stdio output nor event journal JSON leaks the secret-like token. Live real-Keychain manual proof remains pending. |
| `cargo clippy -p opensks-contracts -p opensks-conversation -p opensks-scheduler -p opensks-adapter -p opensks-daemon --all-targets -- -D warnings` | Passed | Focused clippy passed for the budget settings, scheduler receipt, artifact write, and integration apply scope-proof slice. |
| `cargo run -p xtask -- schemas` / `cargo fmt --all --check` / `git diff --check` / `bash scripts/check-architecture-ownership.sh` / `cargo run -p xtask -- architecture-graph --check` | Passed | Generated schemas include the new conversation-thread budget fields; Rust formatting, whitespace, architecture ownership, architecture graph, and direct-I/O guards pass after this slice. |
| `cargo run -- acceptance audit` | Partial | Latest acceptance remains 23 total, 22 passed, 1 partial, 0 failed. The only partial remains `mvp-004`, which needs explicit live OpenRouter/OpenAI credentials and `OPENSKS_ALLOW_REMOTE_PROVIDER_PROBE=1`. |
| `sks naruto run "남은 작업들 최대한 병렬로 동시에 작업좀해줘 지금 너무 느려" --json` | Blocked | Naruto mission `.sneakoscope/missions/M-20260625-013037-6dc0` prepared a 32-clone / target-concurrency-25 work graph, but returned `ok: false`; blockers again include Codex SDK event-stream/structured-output failures, missing patch envelopes for write-capable workers, Codex Exec failures, and Supabase MCP auth with no access token. Parent-owned scoped integration continued with this route limitation recorded. |
| `cargo test -p opensks-daemon integration_candidate_apply --locked -- --test-threads=1` | Passed | 9 integration apply tests passed after integration verification began reading semantic verifier judgment artifacts as hard gates. New regressions prove a `pass` semantic judgment adds a model semantic verifier lane and allows approved apply, while a `fail` judgment blocks approval-gated apply before `final.diff`, `seal.json`, cleanup, or main-workspace mutation. |
| `cargo test -p opensks-daemon conversation_supervisor_tick_dispatches_provider_registry_route_through_chat_adapter --locked -- --test-threads=1` | Passed | Provider-backed role routing still passes after verification-role prompts require `VERDICT: pass|fail`; the mock provider now returns `VERDICT: pass`, the semantic verifier receipt records only redacted verdict/gates plus provider/model metadata, and raw verifier text remains absent from the persisted receipt. |
| `cargo test -p opensks-daemon --locked -- --test-threads=1` | Passed | Full daemon package passed: 52 tests covering conversation/run control, provider routing, image tools, integration apply, resident supervisor wakeups, subscription replay, and worktree isolation after the semantic verifier gate slice. |
| `cargo test -p opensks-contracts --locked` | Passed | Full contracts package passed: 70 tests after `SemanticVerifierJudgmentReceipt` gained additive/defaulted `verdict`, `passed_gates`, and `failed_gates` fields. |
| `cargo test -p opensks-cli capability --locked` | Passed | 2 CLI capability tests passed after `agent.parallel_build` truth gained `integration:semantic-verifier-gate` evidence and the remaining reason narrowed to live objective runtime orchestration. |
| `cargo run -p xtask -- schemas` | Passed | Regenerated schemas; `schemas/semantic-verifier-judgment.schema.json` now includes `verdict`, `passed_gates`, and `failed_gates`. |
| `cargo run -p xtask -- capability-matrix --runtime-fixture release` | Passed | Regenerated `docs/runtime-truth-matrix.generated.md`; `agent.parallel_build` now reports `objective_plan_apply_seal_runtime_present_live_model_planner_pending` and includes `scheduler:objective-plan-turn-bootstrap`, `daemon:objective-plan-artifact`, `daemon:objective-plan-child-runtime`, `daemon:objective-plan-apply-runtime`, and `daemon:objective-plan-seal-runtime` evidence. |
| `cargo clippy -p opensks-contracts -p opensks-daemon -p opensks-cli --all-targets -- -D warnings` | Passed | Focused clippy passed for the semantic verifier judgment contract, integration coordinator semantic gate, and capability consumer surface. |
| `cargo fmt --all --check && git diff --check` | Passed | Formatting and whitespace checks passed after the semantic verifier gate slice. |
| `sks naruto run "남은 작업들 최대한 병렬로 동시에 작업좀해줘 지금 너무 느려" --json` | Blocked | Naruto mission `.sneakoscope/missions/M-20260624-232201-50f7` spawned a 32-clone work graph but returned `ok: false`; many Codex SDK workers failed structured-output/event-stream requirements, some write-capable workers lacked patch envelopes, and Supabase MCP auth was missing. Parent-owned narrow parallel lanes continued with this route limitation recorded. |
| Narrow parallel lanes | Passed | Three disjoint lanes completed: daemon worktree inventory/recovery invocation in `crates/opensks-daemon/src/lib.rs`, git target-base drift helper in `crates/opensks-git/src/lib.rs`, and Swift queue-drain product-path lockdown in `swift/Sources/Conversations/ConversationStore.swift` plus conversation tests. |
| `cargo test -p opensks-daemon worktree_isolation --locked -- --test-threads=1` | Passed | Three daemon request tests prove product `worktree_inventory`/`worktree_recover` requests write redacted `inventory.json`/`recovery.json`, append redacted event-store evidence, remove runtime isolations on recovery, and reject traversal-style run/workspace input without leaking paths. |
| `cargo test -p opensks-git target_paths_changed_since_base --locked` | Passed | Two git tests prove the read-only target-base helper detects committed drift on candidate target paths, ignores unrelated commits, and returns false for empty target lists. |
| `cargo test -p opensks-daemon integration_candidate_apply --locked -- --test-threads=1` | Passed | Five integration apply tests passed; committed target drift is now rejected before verifier/apply with `target_changed_since_candidate_base`, `target_base_fence`, repair evidence, and no final diff/seal. Existing approved single/aggregate apply, dirty-target refusal, and approval persistence checks still pass. |
| `cargo test -p opensks-contracts worktree_isolation_requests_roundtrip_with_run_id_scope --locked` | Passed | Contract test proves typed `EngineRequestKind::WorktreeInventory` / `WorktreeRecover` round-trip with `run_id`, scope, and reason-code fields. |
| `cargo run -p xtask -- schemas` | Passed | Regenerated schemas; `schemas/engine-request.schema.json` now includes `worktree_inventory` and `worktree_recover` request kinds. |
| `cargo clippy -p opensks-contracts -p opensks-git -p opensks-daemon --all-targets -- -D warnings` | Passed | Focused clippy passed for the touched Rust contracts/git/daemon crates. |
| `swift test --package-path swift --filter ConversationTurnTests` | Passed | 26 Swift conversation tests passed; default load/send remain observer paths and explicit supervisor drain is diagnostic/manual recovery only. |
| `cargo fmt --all --check` | Passed | Rust formatting is clean after the daemon/git/contracts slice. |
| `git diff --check` | Passed | No whitespace errors after the parallel slice and schema regeneration. |
| `sks hook user-prompt-submit` | Passed | Hook selected the SKS/Naruto implementation route for the active `/goal`, materializing request intake `.sneakoscope/missions/M-20260624-225234-c0cf/request-intake.json` and requiring prompt-pipeline, Naruto, reflection, and Honest Mode. |
| `sks naruto run "Continue working toward the active /goal: implement the OpenSKS 0e9dad0 provider control center and hyperparallel production runtime work order while preserving user changes. Inspect current state, choose the next production-aligned incomplete requirement, implement it with tests and evidence." --json` | Blocked | Naruto mission `.sneakoscope/missions/M-20260624-225258-a751` materialized worktree/work graph artifacts, but command returned `ok: false`; final proof blockers were `appshots_operator_action_missing_for_visual_proof` and `source_intelligence_gate_failed`. Parent-owned implementation continued with this native-lane failure recorded. |
| `sks proof-field scan --intent "Add schema-backed worktree isolation inventory and recovery receipts for runtime source worktrees" --changed ...` | Passed | Proof Field report `.sneakoscope/reports/proof-field-1782309270233.json` reported `Mode: balanced`, `Eligible: no`, `Contract clarity: 0.93`, and `Workflow complexity: focused (0.27)` for the inventory/recovery receipt slice. |
| `cargo test -p opensks-contracts worktree_isolation --locked` | Passed | New contract tests prove `opensks.worktree-isolation-inventory-receipt.v1` and `opensks.worktree-isolation-recovery-receipt.v1` round-trip with redacted artifact refs and expected counters. |
| `cargo test -p opensks-git isolation --locked` | Passed | Seven git isolation tests passed, including inventory of git worktrees and snapshots without absolute path leakage, recovery removal of git worktrees plus metadata prune, snapshot recovery removal, and existing cleanup behavior. |
| `cargo run -p xtask -- schemas` | Passed | Regenerated schemas, including `schemas/worktree-isolation-inventory-receipt.schema.json` and `schemas/worktree-isolation-recovery-receipt.schema.json`. |
| `cargo clippy -p opensks-contracts -p opensks-git --all-targets -- -D warnings` | Passed | Focused clippy passed for the touched contract/git crates. |
| `cargo fmt --all --check` | Passed | Rust formatting is clean after the worktree inventory/recovery receipt slice. |
| `git diff --check` | Passed | No whitespace errors after the worktree inventory/recovery receipt slice. |
| `sks hook user-prompt-submit` | Passed | Hook selected `$Naruto` for the active `/goal` continuation and materialized mission `.sneakoscope/missions/M-20260624-230657-093e` for the CLI worktree inventory/recovery slice. |
| `sks naruto run "... expose worktree isolation inventory/recovery through product CLI invocation ..."` | Blocked | Naruto mission `.sneakoscope/missions/M-20260624-230813-37ed` materialized worktree/work graph artifacts, but returned `ok: false` with proof blockers `appshots_operator_action_missing_for_visual_proof` and `source_intelligence_gate_failed`; parent-owned integration continued with this route limitation recorded. |
| `sks proof-field scan --intent "Expose worktree isolation inventory and recovery receipts through CLI commands" --changed ...` | Passed | Proof Field report `.sneakoscope/reports/proof-field-1782310409667.json` reported `Mode: balanced`, `Eligible: no`, `Contract clarity: 0.7`, and `Workflow complexity: focused (0.22)`. |
| `cargo test -p opensks-cli worktree_inventory_writes_redacted_runtime_receipt --locked` | Passed | New CLI product invocation test writes `.opensks/runtime/worktrees/<run-id>/inventory.json`, proves schema/id/state/count fields, artifact refs, and redaction flags, and asserts the serialized receipt does not contain the workspace path. |
| `cargo test -p opensks-cli worktree_recover_writes_receipts_and_removes_snapshot_isolation --locked` | Passed | New CLI product invocation test writes both `inventory.json` and `recovery.json`, removes the snapshot isolation, proves recovered counters/refs, and asserts no workspace path leaks into either receipt. |
| `cargo test -p opensks-cli worktree_usage_preserves_errors --locked` | Passed | Usage regression now covers `inventory <run-id>` and rejects path-traversal-style run ids before constructing receipt artifact paths. |
| `cargo test -p opensks-cli worktree --locked` | Passed | Six worktree-related CLI tests passed, covering legacy create/isolate plus new inventory/recovery invocation and run-id validation. |
| `cargo clippy -p opensks-cli --all-targets -- -D warnings` | Passed | Focused clippy passed for the touched CLI command/test surface. |
| `cargo fmt --all --check` | Passed | Rust formatting is clean after the CLI worktree inventory/recovery invocation slice. |
| `git diff --check` | Passed | No whitespace errors after the CLI worktree inventory/recovery invocation slice and ledger/reflection updates. |
| `jq empty .sneakoscope/missions/M-20260624-230813-37ed/reflection-gate.json` | Passed | Reflection gate JSON is valid after recording the remaining PR-089 gaps. |
| `sks validate-artifacts M-20260624-230813-37ed` | Passed | Mission-specific artifact validation passed for the blocked Naruto mission used by this slice. |
| `sks wiki refresh` | Passed | Post-reflection TriWiki refresh wrote `.sneakoscope/wiki/context-pack.json` with 9 hydrated claims, 20 coordinate anchors, 20 metadata rows, and validation `ok` with 20/20 trust anchors. |
| `sks wiki validate .sneakoscope/wiki/context-pack.json` | Passed | Explicit wiki validation passed after reflection: 20 anchors checked and 20/20 trust anchors. |
| `sed -n '1,240p' $CODEX_HOME/attachments/9f601177-8745-42e1-a34e-e65110080c0f/goal-objective.md` | Passed | Re-read the persisted `/goal` objective before continuing; objective still requires the full OpenSKS 0e9dad0 provider control center and hyperparallel runtime work order while preserving user changes. |
| `sks hook user-prompt-submit` | Passed | Hook selected the SKS/Naruto implementation route for the active goal and required prompt-pipeline plus Honest Mode closeout. |
| `sks proof-field scan --intent "Propagate durable run cancellation into provider-driven agentic tool loops between model/tool/apply steps" --changed ...` | Passed | Proof Field report `.sneakoscope/reports/proof-field-1782307055763.json` reported `Mode: balanced`, `Eligible: no`, `Workflow complexity: focused (0.22)`; parent-owned implementation continued with focused verification. |
| `sks naruto run "Continue working toward the active /goal: implement the OpenSKS 0e9dad0 provider control center and hyperparallel production runtime work order while preserving user changes." --json` | Blocked | Naruto mission `.sneakoscope/missions/M-20260624-221329-5965` produced artifacts, but `naruto-gate.json` reports `status: blocked`, `terminal_state: blocked`, `passed: false`, and 904 blockers including `agent_lifecycle_not_all_closed`, `codex_sdk_event_stream_missing`, `codex_sdk_structured_output_invalid`, `schema:structured_output_missing`, `write_capable_no_patch_envelope`, Codex Exec failure, and Supabase MCP auth with no access token. `naruto-finalizer.json` reports `ok: false`, `run_ok: true`, `local_participated: false`, `apply_finalized: false`, and `final_patch_source: deterministic_no_local`. |
| `cargo test -p opensks-adapter loop_stops_before_next_tool_step_when_cancelled --locked` | Passed | New adapter regression proves a cancellation token set immediately after a model/driver step prevents the next requested tool/write step, leaves `NOTES.md` unchanged, returns `RunProjectionState::Cancelled`, and emits `run_cancelled` warning evidence. |
| `cargo test -p opensks-adapter --locked` | Passed | Full adapter package passed: 68 tests passed and 1 live OpenRouter smoke remained ignored, covering tool loop, canonical tool parsing, image tools, PatchEngine writes, OpenRouter scripted loop, and the new cancellation regression. |
| `cargo test -p opensks-daemon conversation_supervisor_tick_honors_cancel_before_dispatch --locked -- --test-threads=1` | Passed | Pre-dispatch raw-journal cancellation fallback still passes after the daemon request began carrying the shared cancellation token. |
| `cargo test -p opensks-daemon conversation_supervisor_tick_executes_claimed_local_test_turn --locked -- --test-threads=1` | Passed | Successful claimed local-test execution still passes after the heartbeat guard started sharing its cancellation flag with the adapter request. |
| `cargo test -p opensks-daemon conversation_run_control_cancel_projects_before_supervisor_tick --locked -- --test-threads=1` | Passed | Run-control projection cancellation regression still passes after the adapter-loop cancellation token was added. |
| `cargo test -p opensks-cli conversation --locked` | Passed | 6 CLI conversation tests and the filtered PR-099 receipt E2E binary passed after adding `AgentRunRequest.cancellation_token` to the compatibility request path. |
| `cargo test -p opensks-daemon --locked -- --test-threads=1` | Passed | Full daemon package passed: 36 tests covering run-control projection, supervisor cancellation/heartbeat, provider/image dispatch, integration apply, subscriptions, and daemon request handling. |
| `cargo fmt --all --check` | Passed | Rust formatting is clean after the adapter/daemon/CLI cancellation-token slice and the line-budget comment trim. |
| `cargo clippy -p opensks-adapter -p opensks-daemon -p opensks-cli --all-targets -- -D warnings` | Passed | Focused clippy passed for the touched adapter, daemon, and CLI crates. |
| `bash scripts/check-architecture-ownership.sh` | Passed | Architecture ownership guard passed after trimming `crates/opensks-adapter/src/lib.rs` documentation from 903 to 899 lines to stay under the 900-line budget. |
| `cargo run -p xtask -- architecture-graph --check` | Passed | `architecture-graph: ok` after the cancellation-token slice. |
| `cargo run -p xtask -- direct-io-audit --check` | Passed | `direct-io-audit: ok` after the cancellation-token slice. |
| `git diff --check` | Passed | No whitespace errors after the cancellation-token slice. |
| `jq empty .sneakoscope/missions/M-20260624-221329-5965/reflection-gate.json` | Passed | Reflection gate JSON is valid for the adapter-loop cancellation propagation slice. |
| `sks wiki refresh` | Passed | Post-reflection TriWiki refresh wrote `.sneakoscope/wiki/context-pack.json` with 9 hydrated claims, 20 coordinate anchors, 20 metadata rows, average trust 0.8572, and validation `ok` with 20/20 trust anchors. |
| `sks wiki validate .sneakoscope/wiki/context-pack.json` | Passed | Explicit wiki validation passed: 20 anchors checked and 20/20 trust anchors. |
| `sks validate-artifacts M-20260624-221329-5965` | Passed | Mission-specific artifact validation passed for `.sneakoscope/missions/M-20260624-221329-5965`. |
| `sks validate-artifacts latest` | Passed | Latest artifact validation passed; target was `.sneakoscope/missions/M-20260624-222741-c003`. |
| `sks hook user-prompt-submit` | Passed | Hook selected SKS/general route context with prompt-pipeline and Honest Mode requirements, Context7 optional unless external API/library docs change, and default implementation promotion through the native route where available. |
| `sks proof-field scan --intent "Strengthen durable conversation run cancellation projection before supervisor dispatch" --changed crates/opensks-daemon/src/lib.rs --changed docs/implementation/0e9dad0-provider-hyperparallel-ledger.md` | Passed | Proof Field reported `Mode: balanced`, `Eligible: no`, `Workflow complexity: focused (0.22)`, so this remained parent-owned with full focused verification rather than a fast-lane closeout. |
| `sks naruto run "Continue the active /goal work order by implementing the next small production slice for OpenSKS 0e9dad0: strengthen durable run cancellation/timeline projection without reverting user changes." --json` | Blocked | Naruto mission `.sneakoscope/missions/M-20260624-220015-6de3` materialized work graph/worktree artifacts, but `naruto-gate.json` reports `status: blocked`, `terminal_state: blocked`, `passed: false`, and `naruto-finalizer.json` reports `ok: false`, `run_ok: true`, `local_participated: false`, `apply_finalized: false`, `final_patch_source: deterministic_no_local`. Gate blockers include `agent_lifecycle_not_all_closed`, repeated `codex_sdk_event_stream_missing`, `codex_sdk_structured_output_invalid`, `schema:structured_output_missing`, `write_capable_no_patch_envelope`, Codex Exec failures, and Supabase MCP auth failures. Parent-owned scoped integration continued with this native-lane failure recorded. |
| `cargo test -p opensks-daemon conversation_run_control_cancel_projects_before_supervisor_tick --locked -- --test-threads=1` | Passed | New daemon regression proves a normal `run_cancel` request appends `RunCancelled`, immediately projects it into the conversation timeline/run projection with `conversation:run-control-projected` evidence, prevents a later supervisor tick from claiming the turn, emits no `RunStarted`/`RunCompleted`, creates no execution worktree, and writes no patch. |
| `cargo test -p opensks-daemon conversation_supervisor_tick_honors_cancel_before_dispatch --locked -- --test-threads=1` | Passed | Updated fallback regression injects a raw journal `RunCancelled` event without prior projection and proves the claimed supervisor path still observes the journal before dispatch, skips execution, creates no worktree, writes no patch, and finalizes the projection as `cancelled`. |
| `cargo test -p opensks-daemon conversation_supervisor_tick_executes_claimed_local_test_turn --locked -- --test-threads=1` | Passed | Existing successful claimed local-test execution path still passes after immediate run-control projection was added. |
| `cargo test -p opensks-daemon --locked -- --test-threads=1` | Passed | Full daemon package passed: 36 tests covering run-control projection, supervisor cancellation/heartbeat, provider/image dispatch, integration candidate apply, subscription/timeline replay, and daemon request handling. |
| `cargo test -p opensks-conversation --locked` | Passed | 32 conversation tests passed after daemon run-control started projecting cancellation into the repository-owned timeline reducer. |
| `cargo test -p opensks-cli conversation --locked` | Passed | 6 CLI conversation tests and the filtered PR-099 integration-test binary passed after run-control projection changed daemon-side state timing. |
| `cargo fmt --all --check` | Passed | Rust formatting is clean after the run-control projection slice. |
| `cargo clippy -p opensks-conversation -p opensks-daemon -p opensks-cli --all-targets -- -D warnings` | Passed | Focused clippy passed for the conversation/daemon/CLI runtime surface. |
| `bash scripts/check-architecture-ownership.sh` | Passed | Architecture ownership/path/Swift/provider/patch-engine guards all passed after the run-control projection slice. |
| `cargo run -p xtask -- architecture-graph --check` | Passed | `architecture-graph: ok` after the run-control projection slice. |
| `cargo run -p xtask -- direct-io-audit --check` | Passed | `direct-io-audit: ok` after the run-control projection slice. |
| `git diff --check` | Passed | No whitespace errors after the run-control projection slice. |
| `jq empty .sneakoscope/missions/M-20260624-220015-6de3/reflection-gate.json` | Passed | Reflection gate JSON is valid for the run-control projection slice. |
| `sks wiki refresh` | Passed | Post-reflection TriWiki refresh wrote `.sneakoscope/wiki/context-pack.json` with 9 hydrated claims, 20 coordinate anchors, 20 metadata rows, average trust 0.8572, and validation `ok` with 20/20 trust anchors. |
| `sks wiki validate .sneakoscope/wiki/context-pack.json` | Passed | Explicit wiki validation passed: 20 anchors checked and 20/20 trust anchors. |
| `sks validate-artifacts M-20260624-220015-6de3` | Passed | Mission-specific artifact validation passed for `.sneakoscope/missions/M-20260624-220015-6de3`. |
| `sks validate-artifacts latest` | Passed | Latest artifact validation also passed; target was `.sneakoscope/missions/M-20260624-221029-12c8`. |
| `sks naruto run "Continue working toward the active thread goal: implement the OpenSKS 0e9dad0 provider control center and hyperparallel production runtime work order while preserving user changes." --json` | Blocked | Naruto mission `.sneakoscope/missions/M-20260624-214421-03e3` materialized dashboard/work graph artifacts, but `naruto-gate.json` reports `status: blocked`, `terminal_state: blocked`, `passed: false`, and `naruto-finalizer.json` reports `ok: false`, `local_participated: false`, `apply_finalized: false`, `final_patch_source: deterministic_no_local`. Dashboard evidence shows 32 agents, target concurrency 25, `Proof: blocked`, scheduler drained, patch queue depth 0, and repeated `codex_sdk_event_stream_missing`, `codex_sdk_structured_output_invalid`, `schema:structured_output_missing`, `write_capable_no_patch_envelope`, Codex Exec failures, and Supabase MCP auth errors. Parent-owned scoped integration continued with this native-lane failure recorded. |
| `cargo test -p opensks-daemon conversation_supervisor_tick_honors_cancel_before_dispatch --locked -- --test-threads=1` | Passed | New daemon supervisor cancellation regression proves a durable `RunCancelled` control event before tick causes the claimed turn to finish `cancelled`, sets `execution_skipped = true`, records cancellation reason evidence, emits no `RunStarted`/`RunCompleted`, creates no execution worktree, writes no patch, and updates the run projection to `cancelled`. |
| `cargo test -p opensks-daemon conversation_supervisor_tick_executes_claimed_local_test_turn --locked -- --test-threads=1` | Passed | Existing claimed local-test execution path still passes after adding cancellation checks and cancellation observation to the heartbeat guard. |
| `cargo test -p opensks-conversation --locked` | Passed | 32 conversation tests passed after the daemon cancellation-before-dispatch slice. |
| `cargo test -p opensks-cli conversation --locked` | Passed | 6 CLI conversation tests and the filtered PR-099 integration-test binary passed after daemon cancellation handling changed supervisor tick execution JSON. |
| `cargo fmt --all --check` | Passed | Rust formatting is clean after the PR-088 daemon cancellation slice. |
| `cargo clippy -p opensks-conversation -p opensks-daemon -p opensks-cli --all-targets -- -D warnings` | Passed | Focused clippy passed after the daemon cancellation observation helper and heartbeat cancellation flag were added. |
| `git diff --check` | Passed | No whitespace errors after the daemon cancellation slice. |
| `bash scripts/check-architecture-ownership.sh` | Passed | Architecture ownership/path/Swift/provider/patch-engine guards all passed after the daemon cancellation slice. |
| `cargo run -p xtask -- architecture-graph --check` | Passed | `architecture-graph: ok` after the daemon cancellation slice. |
| `cargo run -p xtask -- direct-io-audit --check` | Passed | `direct-io-audit: ok` after the daemon cancellation slice. |
| `sks wiki refresh` | Passed | Post-reflection TriWiki refresh wrote `.sneakoscope/wiki/context-pack.json` with 9 hydrated claims, 20 coordinate anchors, 20 metadata rows, average trust 0.8572, and validation `ok` with 20/20 trust anchors. |
| `sks wiki validate .sneakoscope/wiki/context-pack.json` | Passed | Explicit wiki validation passed: 20 anchors checked and 20/20 trust anchors. |
| `sks validate-artifacts latest` | Passed | Latest explicit SKS artifact validation target was `.sneakoscope/missions/M-20260624-215524-b8dc`. |
| `jq empty .sneakoscope/missions/M-20260624-214421-03e3/reflection-gate.json && git diff --check` | Passed | Reflection gate JSON is valid and no whitespace errors were present after adding cancellation reflection artifacts. |
| `sks naruto run "Continue working toward the active thread goal: implement the OpenSKS 0e9dad0 provider control center and hyperparallel production runtime work order while preserving user changes." --json` | Blocked | Naruto mission `.sneakoscope/missions/M-20260624-213204-40df` materialized dashboard/work graph artifacts, but `naruto-gate.json` reports `status: blocked` and `naruto-finalizer.json` reports `ok: false`, `local_participated: false`, `apply_finalized: false`, `final_patch_source: deterministic_no_local`. Dashboard evidence shows 32 agents, target concurrency 25, `Proof: blocked`, and repeated `codex_sdk_event_stream_missing`, `codex_sdk_structured_output_invalid`, `schema:structured_output_missing`, `write_capable_no_patch_envelope`, repeated Codex Exec failures, and Supabase MCP auth errors. Parent-owned scoped integration continued with this native-lane failure recorded. |
| `cargo test -p opensks-daemon conversation_supervisor_tick_executes_claimed_local_test_turn --locked -- --test-threads=1` | Passed | Daemon supervisor tick now asserts the claimed `fencing_token` is exposed and the execution result includes `lease_heartbeat.started = true`, `initial_heartbeat_ok = true`, the same fencing token, and a 333ms heartbeat interval for a 1000ms lease TTL. |
| `cargo test -p opensks-conversation --locked` | Passed | 32 conversation tests passed after adding the daemon heartbeat guard on top of the existing fenced conversation-supervisor lease contract. |
| `cargo test -p opensks-cli conversation --locked` | Passed | 6 CLI conversation tests and the filtered PR-099 integration-test binary passed after the heartbeat guard changed daemon tick execution JSON. |
| `cargo fmt --all --check` | Passed | Rust formatting is clean after the PR-088 daemon heartbeat slice. |
| `cargo clippy -p opensks-conversation -p opensks-daemon -p opensks-cli --all-targets -- -D warnings` | Passed | Focused clippy passed after the daemon heartbeat interval helper and guard were added. |
| `git diff --check` | Passed | No whitespace errors after the daemon heartbeat slice. |
| `bash scripts/check-architecture-ownership.sh` | Passed | Architecture ownership/path/Swift/provider/patch-engine guards all passed after the daemon heartbeat slice. |
| `cargo run -p xtask -- architecture-graph --check` | Passed | `architecture-graph: ok` after the daemon heartbeat slice. |
| `cargo run -p xtask -- direct-io-audit --check` | Passed | `direct-io-audit: ok` after the daemon heartbeat slice. |
| `sks wiki refresh` | Passed | Post-reflection TriWiki refresh wrote `.sneakoscope/wiki/context-pack.json` with 9 hydrated claims, 21 coordinate anchors, 21 metadata rows, average trust 0.8572, and validation `ok` with 21/21 trust anchors. |
| `sks wiki validate .sneakoscope/wiki/context-pack.json` | Passed | Explicit wiki validation passed: 21 anchors checked and 21/21 trust anchors. |
| `sks validate-artifacts latest` | Passed | Latest explicit SKS artifact validation target was `.sneakoscope/missions/M-20260624-214158-e260`. |
| `jq empty .sneakoscope/missions/M-20260624-213204-40df/reflection-gate.json && git diff --check` | Passed | Reflection gate JSON is valid and no whitespace errors were present after adding heartbeat reflection artifacts. |
| `sks naruto run "Continue working toward the active thread goal: implement the OpenSKS 0e9dad0 provider control center and hyperparallel production runtime work order while preserving user changes." --json` | Blocked | Naruto mission `.sneakoscope/missions/M-20260624-205726-ec1e` materialized work graph/worktree artifacts, but command returned `ok: false`: 0 completed, 50 blocked worker closeouts with repeated `codex_sdk_event_stream_missing`, `codex_sdk_structured_output_invalid`, `schema:structured_output_missing`, `write_capable_no_patch_envelope`, repeated Codex Exec failures, and one Supabase MCP auth error. Parent-owned scoped integration continued with this native-lane failure recorded. |
| `cargo test -p opensks-conversation --locked` | Passed | 32 conversation tests passed after PR-088 added monotonic conversation-supervisor `fencing_token` leases. Regression coverage proves first claim token `1`, reclaimed token `2`, stale token heartbeat rejection, and stale token finalization rejection after lease recovery/reclaim. |
| `cargo test -p opensks-daemon conversation_supervisor_tick_executes_claimed_local_test_turn --locked -- --test-threads=1` | Passed | Daemon supervisor tick still executes a claimed local-test turn after finalization moved to owner+fencing-token validation and claimed tick receipts expose `fencing_token`. |
| `cargo test -p opensks-cli conversation --locked` | Passed | 6 CLI conversation tests passed after supervisor tick claimed-lease JSON began exposing `fencing_token` and conversation repository claim/finalize APIs enforced fenced leases. |
| `cargo fmt --all --check` | Passed | Rust formatting is clean after the PR-088 conversation-supervisor fencing slice. |
| `cargo clippy -p opensks-conversation -p opensks-daemon -p opensks-cli --all-targets -- -D warnings` | Passed | Clippy passed after finalization was tightened to accept a `TurnSupervisorLease` object instead of loose owner/token/run arguments. |
| `git diff --check` | Passed | No whitespace errors after the PR-088 conversation-supervisor fencing slice. |
| `bash scripts/check-architecture-ownership.sh` | Passed | Architecture ownership/path/Swift/provider/patch-engine guards all passed after the PR-088 conversation-supervisor fencing slice. |
| `cargo run -p xtask -- architecture-graph --check` | Passed | `architecture-graph: ok` after the PR-088 conversation-supervisor fencing slice. |
| `cargo run -p xtask -- direct-io-audit --check` | Passed | `direct-io-audit: ok` after the PR-088 conversation-supervisor fencing slice. |
| `sks wiki refresh` | Passed | Post-reflection TriWiki refresh wrote `.sneakoscope/wiki/context-pack.json` with 9 hydrated claims, 21 coordinate anchors, 21 metadata rows, average trust 0.855, and validation `ok` with 21/21 trust anchors. |
| `sks wiki validate .sneakoscope/wiki/context-pack.json` | Passed | Explicit wiki validation passed after reflection: 21 anchors checked and 21/21 trust anchors. |
| `sks validate-artifacts latest` | Passed | Latest explicit SKS artifact validation target was `.sneakoscope/missions/M-20260624-213000-64f7`. |
| `jq empty .sneakoscope/missions/M-20260624-205726-ec1e/reflection-gate.json && git diff --check` | Passed | Reflection gate JSON is valid and no whitespace errors were present after adding reflection artifacts. |
| `sks naruto run "Continue working toward the active OpenSKS 0e9dad0 provider control center and hyperparallel production runtime goal." --json` | Blocked | Naruto mission `.sneakoscope/missions/M-20260624-204130-85b5` materialized work graph/worktree artifacts, but command returned `ok: false`: 0 completed, 50 blocked worker closeouts with repeated `codex_sdk_event_stream_missing`, `codex_sdk_structured_output_invalid`, `schema:structured_output_missing`, `write_capable_no_patch_envelope`, plus one Supabase MCP auth error. Parent-owned scoped integration continued with this native-lane failure recorded. |
| `cargo test -p opensks-conversation --locked` | Passed | 32 conversation tests passed after PR-096 moved compatibility state/sequence paths to projection authority; new regressions prove `run_last_event_sequence` reads `run_projections.last_sequence` instead of stale `runs.last_event_sequence`, and `set_turn_run_state_with_last_sequence` updates projection state/checkpoint plus conversation status. |
| `cargo test -p opensks-daemon conversation_supervisor_tick_executes_claimed_local_test_turn --locked -- --test-threads=1` | Passed | Daemon supervisor lifecycle path still passes after compatibility state/sequence updates began writing projection state/checkpoints transactionally. |
| `cargo test -p opensks-cli conversation --locked` | Passed | 6 CLI conversation tests passed after `run_last_event_sequence` moved to the projection checkpoint. |
| `cargo fmt --all --check` | Passed | Rust formatting is clean after the PR-096 projection checkpoint/setter slice. |
| `cargo clippy -p opensks-conversation -p opensks-daemon -p opensks-cli --all-targets -- -D warnings` | Passed | Clippy passed for projection checkpoint reads, compatibility setter projection updates, and downstream daemon/CLI consumers. |
| `git diff --check` | Passed | No whitespace errors after the PR-096 projection checkpoint/setter slice. |
| `bash scripts/check-architecture-ownership.sh` | Passed | Architecture ownership/path/Swift/provider/patch-engine guards all passed after the PR-096 projection checkpoint/setter slice. |
| `cargo run -p xtask -- architecture-graph --check` | Passed | `architecture-graph: ok` after the PR-096 projection checkpoint/setter slice. |
| `cargo run -p xtask -- direct-io-audit --check` | Passed | `direct-io-audit: ok` after the PR-096 projection checkpoint/setter slice. |
| `sks wiki refresh` | Passed | Post-reflection TriWiki refresh wrote `.sneakoscope/wiki/context-pack.json` with 9 hydrated claims, 21 coordinate anchors, 21 metadata rows, and validation `ok` with 21/21 trust anchors. |
| `sks wiki validate .sneakoscope/wiki/context-pack.json` | Passed | Explicit wiki validation passed after reflection: 21 anchors checked and 21/21 trust anchors. |
| `sks validate-artifacts latest` | Passed | SKS artifact validation passed during post-reflection closeout for `.sneakoscope/missions/M-20260624-205447-198e`. |
| `jq empty .sneakoscope/missions/M-20260624-204130-85b5/reflection-gate.json && git diff --check` | Passed | Reflection gate JSON is valid and no whitespace errors were present after adding reflection artifacts. |
| `cargo test -p opensks-conversation --locked` | Passed | 30 conversation tests passed after PR-096 made projection state authoritative in both directions for claim/heartbeat/recovery; new regressions prove projection-queued work is claimable even when legacy `turns`/`runs` are stale terminal, projection-running leases accept heartbeat/recovery even when legacy `runs.state` is stale terminal, and stale legacy running rows cannot heartbeat after the projection becomes terminal. |
| `cargo test -p opensks-daemon conversation_supervisor_tick_executes_claimed_local_test_turn --locked -- --test-threads=1` | Passed | Daemon supervisor claim/finish lifecycle path still passes after claim, heartbeat, and recovery decisions stopped using duplicate legacy lifecycle rows as eligibility gates. |
| `cargo test -p opensks-cli conversation --locked` | Passed | 6 CLI conversation tests passed after projection state became the claim/heartbeat/recovery lifecycle authority, including supervisor tick claim and turn-start persistence coverage. |
| `cargo fmt --all --check` | Passed | Rust formatting is clean after the PR-096 bidirectional projection-authority slice. |
| `cargo clippy -p opensks-conversation -p opensks-daemon -p opensks-cli --all-targets -- -D warnings` | Passed | Clippy passed for projection-authoritative claim/heartbeat/recovery plus downstream daemon/CLI consumers. |
| `git diff --check` | Passed | No whitespace errors after the PR-096 bidirectional projection-authority slice. |
| `bash scripts/check-architecture-ownership.sh` | Passed | Architecture ownership/path/Swift/provider/patch-engine guards all passed after the PR-096 bidirectional projection-authority slice. |
| `cargo run -p xtask -- architecture-graph --check` | Passed | `architecture-graph: ok` after the PR-096 bidirectional projection-authority slice. |
| `cargo run -p xtask -- direct-io-audit --check` | Passed | `direct-io-audit: ok` after the PR-096 bidirectional projection-authority slice. |
| `sks validate-artifacts latest` | Passed | SKS artifact validation passed for `.sneakoscope/missions/M-20260623-182047-ac57`. |
| `sks wiki validate .sneakoscope/wiki/context-pack.json` | Passed | Wiki coordinate index validated: 21 anchors checked, trust anchors 21/21. |
| `cargo test -p opensks-conversation --locked` | Passed | 27 conversation tests passed after PR-096 run-state SSOT tightening; new regressions prove stale legacy `runs.state = queued` cannot be claimed when `run_projections.state` is terminal, stale legacy running leases cannot requeue a terminal projection, and migration status repair reads projections instead of legacy runs. |
| `cargo test -p opensks-daemon conversation_supervisor_tick_executes_claimed_local_test_turn --locked -- --test-threads=1` | Passed | Daemon supervisor claim/finish lifecycle path still passes after claim and recovery decisions gained projection-state gating. |
| `cargo test -p opensks-cli conversation --locked` | Passed | 6 CLI conversation tests passed after the PR-096 projection-authority tightening, including supervisor tick claim and turn-start persistence coverage. |
| `cargo fmt --all --check` | Passed | Rust formatting is clean after the PR-096 projection-authority slice. |
| `cargo clippy -p opensks-conversation -p opensks-daemon -p opensks-cli --all-targets -- -D warnings` | Passed | Clippy passed for conversation projection-state gating plus downstream daemon/CLI consumers. |
| `git diff --check` | Passed | No whitespace errors after the PR-096 projection-authority slice. |
| `bash scripts/check-architecture-ownership.sh` | Passed | Architecture ownership/path/Swift/provider/patch-engine guards all passed after the PR-096 projection-authority slice. |
| `cargo run -p xtask -- architecture-graph --check` | Passed | `architecture-graph: ok` after the PR-096 projection-authority slice. |
| `cargo run -p xtask -- direct-io-audit --check` | Passed | `direct-io-audit: ok` after the PR-096 projection-authority slice. |
| `sks validate-artifacts latest` | Blocked | SKS validation is still blocked by `SKS update migration blocked: update_migration_lock_held`; the suggested `sks doctor --fix --yes` was not run without explicit approval. |
| `sks wiki validate .sneakoscope/wiki/context-pack.json` | Blocked | TriWiki validation is still blocked by `SKS update migration blocked: update_migration_lock_held`; the suggested `sks doctor --fix --yes` was not run without explicit approval. |
| `cargo test -p opensks-context --locked` | Passed | 5 context tests passed after adding `build_worker_context_pack`; coverage proves compact worker packs stay within budget, include worker scope, selected fresh editor context, CodeGraph record IDs, and worker-context evidence, and avoid embedding full TriWiki prose entries. |
| `cargo test -p opensks-scheduler --locked` | Passed | 21 scheduler tests passed after scoped worker refs became deterministic worker artifact refs; the parallel-dispatch regression proves each admitted worker receives a distinct `artifact://...--worker-*.json#work_item_id=...` ref. |
| `cargo test -p opensks-daemon conversation_supervisor_tick_dispatches_provider_registry_route_through_chat_adapter --locked -- --test-threads=1` | Passed | Provider-backed role-worker regression passed after the daemon materialized per-worker context-pack artifacts and inserted their bodies into role model requests; assertions read all four worker context-pack JSON artifacts and prove `worker_context_pack_materialized`, worker scope, scoped evidence, redacted path handling, and prompt-body inclusion. |
| `cargo fmt --all --check` | Passed | Rust formatting is clean after the PR-097 worker context-pack materialization slice. |
| `cargo clippy -p opensks-context -p opensks-scheduler -p opensks-daemon --all-targets -- -D warnings` | Passed | Clippy passed for the worker pack builder, scheduler artifact-ref derivation, daemon materialization/write path, role prompt inclusion, and daemon regression assertions. |
| `git diff --check` | Passed | No whitespace errors after the PR-097 worker context-pack materialization slice. |
| `bash scripts/check-architecture-ownership.sh` | Passed | Architecture ownership/path/Swift/provider/patch-engine guards all passed after the PR-097 worker context-pack materialization slice. |
| `cargo run -p xtask -- architecture-graph --check` | Passed | `architecture-graph: ok` after the PR-097 worker context-pack materialization slice. |
| `cargo run -p xtask -- direct-io-audit --check` | Passed | `direct-io-audit: ok` after the PR-097 worker context-pack materialization slice. |
| `sks validate-artifacts latest` | Blocked | SKS validation is still blocked by `SKS update migration blocked: update_migration_lock_held`; the suggested `sks doctor --fix --yes` was not run without explicit approval. |
| `sks wiki validate .sneakoscope/wiki/context-pack.json` | Blocked | TriWiki validation is still blocked by `SKS update migration blocked: update_migration_lock_held`; the suggested `sks doctor --fix --yes` was not run without explicit approval. |
| `swift test --package-path swift --filter OpenSKSStudioTests.ConversationTurnTests` | Passed | 26 ConversationTurn tests passed after Swift turn-start began sending the durable thread settings snapshot plus `context.refs`; new coverage proves pinned/fallback model settings, reasoning, pipeline, parallelism, verifier count, tool/approval policies, image model id, and selected `editor://...#Lx-Ly#hash` context refs reach the mock daemon request, and the composer renders stale context attachments. |
| `swift test --package-path swift --filter OpenSKSStudioTests.ConflictDiffTests` | Passed | 21 ConflictDiff tests passed after editor selection tracking and context wire formatting were added; new coverage proves `EditorContextRef.wireReference` uses the backend `Lx-Ly` format and editor document selected-line state can capture the active range. |
| `swift test --package-path swift` | Passed | 344 Swift tests passed after Swift selected context refs, stale composer attachments, editor selected-line tracking, and turn-start settings/context forwarding were added. |
| `cargo fmt --all --check` | Passed | Rust formatting remains clean; this slice did not require Rust edits. |
| `bash scripts/check-architecture-ownership.sh` | Passed | Architecture ownership/path/Swift/provider/patch-engine guards all passed after the Swift selected-context slice. |
| `cargo run -p xtask -- architecture-graph --check` | Passed | `architecture-graph: ok` after the Swift selected-context slice. |
| `cargo run -p xtask -- direct-io-audit --check` | Passed | `direct-io-audit: ok` after the Swift selected-context slice. |
| `git diff --check` | Passed | No whitespace errors after the Swift selected-context slice and ledger update. |
| `swift-format lint --recursive swift/Sources swift/Tests` | Blocked | `swift-format` is not installed in this shell (`command not found`). Swift build/tests passed. |
| `sks validate-artifacts latest` | Blocked | SKS validation is still blocked by `SKS update migration blocked: update_migration_lock_held`; the suggested `sks doctor --fix --yes` was not run without explicit approval. |
| `sks wiki validate .sneakoscope/wiki/context-pack.json` | Blocked | TriWiki validation is still blocked by `SKS update migration blocked: update_migration_lock_held`; the suggested `sks doctor --fix --yes` was not run without explicit approval. |
| `swift test --package-path swift --filter OpenSKSStudioTests.ConversationTurnTests` | Passed | 24 ConversationTurn tests passed after Swift Chat send/load moved to observer mode by default: send accepts a queued durable handle and subscribes without supervisor ticks, explicit `drainSupervisorQueue` remains a controller action, default load does not drain, and opt-in load recovery can still drain queued turns. |
| `swift test --package-path swift` | Passed | 340 Swift tests passed after the observer-mode send/load change and assistant timeline card slice. |
| `bash scripts/check-architecture-ownership.sh` | Passed | Architecture ownership/path/Swift/provider/patch-engine guards all passed after the observer-mode send/load change. |
| `cargo run -p xtask -- architecture-graph --check` | Passed | `architecture-graph: ok` after the observer-mode send/load change. |
| `cargo run -p xtask -- direct-io-audit --check` | Passed | `direct-io-audit: ok` after the observer-mode send/load change. |
| `git diff --check` | Passed | No whitespace errors after the observer-mode send/load change and ledger update. |
| `swift-format lint --recursive swift/Sources swift/Tests` | Blocked | `swift-format` is not installed in this shell (`command not found`). Swift tests passed. |
| `cargo test -p opensks-conversation execution_event_replay_flattens_assistant_text_event_details --locked` | Passed | Focused PR-095 assistant-event regression passed: durable event-journal replay now projects assistant delta/completed events as assistant timeline items with `streaming`/`completed` state, assistant message id, redacted assistant snippet/delta, provider/model ids, response hash/bytes, and completion reason. |
| `cargo test -p opensks-conversation --locked` | Passed | 25 conversation tests passed after the assistant timeline event slice, including secret redaction of promoted assistant text and the prior tool/worker/patch detail regressions. |
| `cargo clippy -p opensks-conversation --all-targets -- -D warnings` | Passed | Conversation clippy passed after assistant timeline event projection and generic provider/model detail promotion. |
| `swift test --package-path swift --filter OpenSKSStudioTests.ContractsTests` | Passed | 37 Contracts tests passed, including durable `ConversationTimelineItem` decoding for assistant event fields. |
| `swift test --package-path swift --filter OpenSKSStudioTests.ConversationTurnTests` | Passed | 22 ConversationTurn tests passed, including live assistant delta/completed event extraction, live redaction, and `AssistantTimelineEventCell` render coverage. |
| `swift test --package-path swift` | Passed | 338 Swift tests passed after adding the assistant event timeline card and DTO fields. |
| `cargo fmt --all --check` | Passed | Rust formatting remained clean after the assistant event timeline slice. |
| `bash scripts/check-architecture-ownership.sh` | Passed | Architecture ownership/path/Swift/provider/patch-engine guards all passed after the assistant event timeline slice. |
| `cargo run -p xtask -- architecture-graph --check` | Passed | `architecture-graph: ok` after the assistant event timeline slice. |
| `cargo run -p xtask -- direct-io-audit --check` | Passed | `direct-io-audit: ok` after the assistant event timeline slice. |
| `git diff --check` | Passed | No whitespace errors after the assistant event timeline slice. |
| `swift-format lint --recursive swift/Sources swift/Tests` | Blocked | `swift-format` is not installed in this shell (`command not found`). Swift tests passed. |
| `sks validate-artifacts latest` | Blocked | SKS validation remains blocked by `SKS update migration blocked: update_migration_lock_held`; the suggested `sks doctor --fix --yes` was not run without explicit approval. |
| `sks wiki validate .sneakoscope/wiki/context-pack.json` | Blocked | TriWiki validation remains blocked by `SKS update migration blocked: update_migration_lock_held`; the suggested `sks doctor --fix --yes` was not run without explicit approval. |
| `cargo test -p opensks-conversation execution_event_replay_flattens_tool_worker_patch_details --locked` | Passed | Focused PR-095 projection regression passed: event-journal replay now flattens safe tool, worker, patch, command, exit, duration, target/test, and patch-count details into durable timeline payload fields. |
| `cargo test -p opensks-conversation --locked` | Passed | 24 conversation tests passed after durable timeline detail projection was added. |
| `cargo clippy -p opensks-conversation --all-targets -- -D warnings` | Passed | Conversation clippy passed after timeline detail projection changes. |
| `swift test --package-path swift --filter OpenSKSStudioTests.ContractsTests` | Passed | 36 Contracts tests passed, including durable `ConversationTimelineItem` decoding for execution detail fields. |
| `swift test --package-path swift --filter OpenSKSStudioTests.ConversationTurnTests` | Passed | 21 ConversationTurn tests passed, including live tool/patch event detail extraction and `TimelineItemCell` render coverage. |
| `swift build --package-path swift` | Passed | Swift package built successfully after adding typed event-detail timeline fields and detail rows. |
| `swift test --package-path swift` | Passed | 336 Swift tests passed after the PR-095 tool/worker/patch/test timeline detail slice. |
| `cargo fmt --all --check` | Passed | Rust formatting is clean after the PR-095 timeline detail projection slice. |
| `bash scripts/check-architecture-ownership.sh` | Passed | Architecture ownership/path/Swift/provider/patch-engine guards all passed after the PR-095 timeline detail slice. |
| `cargo run -p xtask -- architecture-graph --check` | Passed | `architecture-graph: ok` after the PR-095 timeline detail slice. |
| `cargo run -p xtask -- direct-io-audit --check` | Passed | `direct-io-audit: ok` after the PR-095 timeline detail slice. |
| `git diff --check` | Passed | No whitespace errors after the PR-095 timeline detail slice. |
| `swift-format lint --recursive swift/Sources swift/Tests` | Blocked | `swift-format` is not installed in this shell (`command not found`). Swift build and full Swift tests passed. |
| `sks validate-artifacts latest` | Blocked | SKS validation remains blocked by `SKS update migration blocked: update_migration_lock_held`; the suggested `sks doctor --fix --yes` was not run without explicit approval. |
| `sks wiki validate .sneakoscope/wiki/context-pack.json` | Blocked | TriWiki validation remains blocked by `SKS update migration blocked: update_migration_lock_held`; the suggested `sks doctor --fix --yes` was not run without explicit approval. |
| `cargo test -p opensks-cli --test pr099_receipt_e2e --locked` | Passed | New local release E2E passed: daemon integration apply writes `final.diff`, CLI stages the matching path, commit preview/receipt preserve integration final-diff evidence, approval-gated push writes to a local bare remote, push status recovers the completed receipt, and repeat execute is idempotent. |
| `cargo test -p opensks-cli --locked` | Passed | 94 CLI unit tests plus the new PR-099 integration test passed after adding the receipt-chain E2E. |
| `cargo test -p opensks-cli push_cli --locked` | Passed | 3 existing push CLI tests passed: happy-path local bare push, protected-ref acknowledgement, and durable failed-push diagnostic recovery. |
| `cargo test -p opensks-git-service commit_preview_and_receipt_link_matching_integration_final_diff --locked` | Passed | Focused Git service final-diff linkage regression still passes after the new E2E. |
| `cargo test -p opensks-daemon integration_candidate_apply --locked -- --test-threads=1` | Passed | 5 daemon integration-apply tests passed after the new receipt-chain E2E. |
| `cargo clippy -p opensks-cli --all-targets -- -D warnings` | Passed | CLI all-targets clippy passed, including the new PR-099 integration test. |
| `cargo fmt --all --check` | Passed | Rust formatting is clean after adding the PR-099 receipt-chain E2E. |
| `bash scripts/check-architecture-ownership.sh` | Passed | Architecture ownership/path/Swift/provider/patch-engine guards all passed after the PR-099 receipt-chain E2E. |
| `cargo run -p xtask -- architecture-graph --check` | Passed | `architecture-graph: ok` after the PR-099 receipt-chain E2E. |
| `cargo run -p xtask -- direct-io-audit --check` | Passed | `direct-io-audit: ok` after the PR-099 receipt-chain E2E. |
| `git diff --check` | Passed | No whitespace errors after adding the PR-099 receipt-chain E2E. |
| `swift test --package-path swift --filter OpenSKSStudioTests.PushOutboxTests` | Passed | 25 PushOutbox tests passed after adding the reusable recovered failed-push diagnostics panel and compact Git Studio non-commit-pane surfacing. |
| `swift build --package-path swift` | Passed | Swift package built successfully after the recovered failed-push diagnostics panel and compact Git Studio refresh/surfacing changes. |
| `swift test --package-path swift` | Passed | 334 Swift tests passed after the PR-099 recovered failed-push diagnostics UI polish. |
| `bash scripts/check-architecture-ownership.sh` | Passed | Architecture ownership/path/Swift/provider/patch-engine guards all passed after the recovered failed-push diagnostics UI polish. |
| `cargo run -p xtask -- architecture-graph --check` | Passed | `architecture-graph: ok` after the recovered failed-push diagnostics UI polish. |
| `cargo run -p xtask -- direct-io-audit --check` | Passed | `direct-io-audit: ok` after the recovered failed-push diagnostics UI polish. |
| `git diff --check` | Passed | No whitespace errors after the recovered failed-push diagnostics UI polish. |
| `swift-format lint --recursive swift/Sources swift/Tests` | Blocked | `swift-format` is not installed in this shell (`command not found`). Swift build and full Swift tests passed. |
| `sks validate-artifacts latest` | Blocked | SKS validation remains blocked by `SKS update migration blocked: update_migration_lock_held`; the suggested `sks doctor --fix --yes` was not run without explicit approval. |
| `sks wiki validate .sneakoscope/wiki/context-pack.json` | Blocked | TriWiki validation remains blocked by `SKS update migration blocked: update_migration_lock_held`; the suggested `sks doctor --fix --yes` was not run without explicit approval. |
| `sks hook user-prompt-submit` | Blocked | SKS hook remains blocked by `SKS update migration blocked: update_migration_lock_held`; no new hook mission artifacts were materialized. The suggested `sks doctor --fix --yes` was not run without explicit approval. |
| `swift test --package-path swift --filter OpenSKSStudioTests.ContractsTests` | Passed | 35 Contracts tests passed after Swift `EngineRunStream` began decoding resumable `stream_failed` frames into typed `EngineStreamFailure` summaries. |
| `swift test --package-path swift --filter OpenSKSStudioTests.ConversationTurnTests` | Passed | 20 ConversationTurn tests passed after Chat background run-event subscription began surfacing resumable cursor-gap failures as preserved `live_stream_failure` timeline error rows plus a reconnect banner. |
| `swift build --package-path swift` | Passed | Swift package built successfully after the stream-failure presentation slice. |
| `swift test --package-path swift` | Passed | 332 Swift tests passed after stream-failure decoding and Chat timeline/banner presentation. |
| `bash scripts/check-architecture-ownership.sh` | Passed | Architecture ownership/path/Swift/provider/patch-engine guards all passed after the Swift stream-failure presentation slice. |
| `cargo run -p xtask -- architecture-graph --check` | Passed | `architecture-graph: ok` after the Swift stream-failure presentation slice. |
| `cargo run -p xtask -- direct-io-audit --check` | Passed | `direct-io-audit: ok` after the Swift stream-failure presentation slice. |
| `git diff --check` | Passed | No whitespace errors after the Swift stream-failure presentation slice. |
| `swift-format lint --recursive swift/Sources swift/Tests` | Blocked | `swift-format` is not installed in this shell (`command not found`). Swift build and full Swift tests passed. |
| `sks validate-artifacts latest` | Blocked | SKS validation remains blocked by `SKS update migration blocked: update_migration_lock_held`; the suggested `sks doctor --fix --yes` was not run without explicit approval. |
| `sks wiki validate .sneakoscope/wiki/context-pack.json` | Blocked | TriWiki validation remains blocked by `SKS update migration blocked: update_migration_lock_held`; the suggested `sks doctor --fix --yes` was not run without explicit approval. |
| `cargo test -p opensks-event-store last_sequence --locked` | Passed | 1 focused event-store cursor helper test passed after adding `EventStore::last_sequence` for durable subscription gap detection. |
| `cargo test -p opensks-daemon subscribe_events --locked -- --test-threads=1` | Passed | 4 daemon subscription tests passed, including out-of-range reconnect coverage that now emits a redacted cursor-gap error plus resumable `stream_failed` frame instead of silently completing a tail subscription. |
| `cargo test -p opensks-event-store --locked` | Passed | 11 event-store tests passed after adding the committed cursor helper. |
| `cargo test -p opensks-daemon --locked -- --test-threads=1` | Passed | 34 daemon tests passed after subscription reconnect gap handling, including timeline projection, provider/image/vision, supervisor, and integration apply regressions. |
| `cargo clippy -p opensks-event-store -p opensks-daemon --all-targets -- -D warnings` | Passed | Focused clippy passed for the event-store cursor helper and daemon subscription gap/error frame wiring. |
| `cargo fmt --all --check` | Passed | Rust formatting is clean after the subscription cursor-gap slice. |
| `bash scripts/check-architecture-ownership.sh` | Passed | Architecture ownership/path/Swift/provider/patch-engine guards all passed after the subscription cursor-gap slice. |
| `cargo run -p xtask -- architecture-graph --check` | Passed | `architecture-graph: ok` after the subscription cursor-gap slice. |
| `cargo run -p xtask -- direct-io-audit --check` | Passed | `direct-io-audit: ok` after the subscription cursor-gap slice. |
| `git diff --check` | Passed | No whitespace errors after the subscription cursor-gap slice and ledger update. |
| `sks validate-artifacts latest` | Blocked | SKS validation remains blocked by `SKS update migration blocked: update_migration_lock_held`; the suggested `sks doctor --fix --yes` was not run without explicit approval. |
| `sks wiki validate .sneakoscope/wiki/context-pack.json` | Blocked | TriWiki validation remains blocked by `SKS update migration blocked: update_migration_lock_held`; the suggested `sks doctor --fix --yes` was not run without explicit approval. |
| `cargo test -p opensks-contracts integration_candidate --locked` | Passed | 3 integration-candidate contract tests passed after adding planned verifier counts to candidate/selection receipts and verifier-lane fields to verification receipts. |
| `cargo test -p opensks-daemon integration_candidate_apply --locked -- --test-threads=1` | Passed | 5 integration-coordinator apply tests passed after `integration_candidate_apply` began running bounded read-only verifier lanes from the candidate's planned verifier count; aggregate fixtures now prove three verifier lanes pass, while dirty/conflict fixtures record failed lane evidence and repair refs. |
| `cargo test -p opensks-daemon conversation_supervisor_tick_dispatches_provider_registry_route_through_chat_adapter --locked -- --test-threads=1` | Passed | Provider-route regression passed after daemon turn execution writes planned verifier counts into integration candidate and selection receipts from the immutable turn settings snapshot. |
| `cargo test -p opensks-contracts --locked` | Passed | 65 contract tests passed after `IntegrationVerificationReceipt` gained defaulted planned/passed/failed verifier counts and per-lane verifier receipt payloads. |
| `cargo test -p opensks-daemon --locked -- --test-threads=1` | Passed | 33 daemon tests passed with bounded read-only integration verifier lanes enabled. |
| `cargo run -p xtask -- schemas` | Passed | Regenerated schemas; integration candidate, candidate-selection, and verification schemas now expose planned verifier count and verification lane evidence. |
| `cargo test -p opensks-contracts capability --locked` | Passed | 6 capability tests passed after `agent.parallel_build` truth gained `integration:read-only-verifier-lane` evidence and the remaining reason narrowed to planner shard policy. |
| `cargo test -p opensks-cli capability --locked` | Passed | 2 CLI capability tests passed after the runtime matrix/report exposed read-only verifier-lane evidence. |
| `cargo run -p xtask -- capability-matrix --runtime-fixture release` | Passed | Regenerated `docs/runtime-truth-matrix.generated.md`; `agent.parallel_build` now reports `candidate_selection_and_verifier_lanes_present_but_planner_shard_policy_pending`. |
| `cargo clippy -p opensks-contracts -p opensks-daemon -p opensks-cli --all-targets -- -D warnings` | Passed | Focused clippy passed for verifier-lane contracts, daemon verifier execution, schema/capability updates, and CLI capability reporting. |
| `cargo fmt --all --check` | Passed | Rust formatting is clean after the verifier-lane slice. |
| `bash scripts/check-architecture-ownership.sh` | Passed | Architecture guard still passes after bounded verifier-lane runtime changes. |
| `cargo run -p xtask -- architecture-graph --check` | Passed | Architecture graph still reports `architecture-graph: ok` after verifier-lane changes. |
| `cargo run -p xtask -- direct-io-audit --check` | Passed | Product direct-I/O guard still reports `direct-io-audit: ok` after verifier-lane changes. |
| `git diff --check` | Passed | No whitespace errors after verifier-lane changes and regenerated artifacts. |
| `sks validate-artifacts latest` | Blocked | SKS validation remains blocked by `SKS update migration blocked: update_migration_lock_held`; the suggested `sks doctor --fix --yes` was not run without explicit approval. |
| `sks wiki validate .sneakoscope/wiki/context-pack.json` | Blocked | TriWiki validation remains blocked by `SKS update migration blocked: update_migration_lock_held`; the suggested `sks doctor --fix --yes` was not run without explicit approval. |
| `cargo test -p opensks-contracts integration_candidate --locked` | Passed | 3 integration-candidate contract tests passed after adding the schema-backed `IntegrationCandidateSelectionReceipt`, additive candidate `selection_ref`, and selection receipt roundtrip coverage. |
| `cargo test -p opensks-contracts capability --locked` | Passed | 6 capability tests passed after `agent.parallel_build` gained `integration:candidate-selection-receipt` and `schema:integration-candidate-selection-receipt` evidence while remaining Foundation pending planner shard/verifier policy. |
| `cargo test -p opensks-daemon conversation_supervisor_tick_executes_claimed_local_test_turn --locked -- --test-threads=1` | Passed | Focused daemon single-source integration test passed: the tick output now exposes `integration_selection_ref`, `candidate.json` links `selection_ref`, and `selection.json` records the selected turn-supervisor candidate plus required verification gates. |
| `cargo test -p opensks-daemon conversation_supervisor_tick_dispatches_provider_registry_route_through_chat_adapter --locked -- --test-threads=1` | Passed | Focused provider aggregate regression passed: root and Code-role source candidates now produce a durable selection receipt with deterministic all-ready-source policy, selected source ids matching the aggregate candidate pool, target coverage for `ROUTED_NOTE.md` and `ROLE_CODE_NOTE.md`, and redacted selection evidence. |
| `cargo test -p opensks-daemon integration_candidate_apply --locked -- --test-threads=1` | Passed | 5 integration-coordinator apply tests remained green with optional candidate selection refs; single and aggregate apply, persisted approval, dirty-target refusal, and patch-conflict repair receipts still behave correctly. |
| `cargo test -p opensks-cli capability --locked` | Passed | 2 CLI capability tests passed after the runtime capability report/matrix exposed candidate-selection receipt evidence. |
| `cargo run -p xtask -- schemas` | Passed | Regenerated schemas, including `schemas/integration-candidate-selection-receipt.schema.json` and the additive `selection_ref` on integration candidate receipts. |
| `cargo run -p xtask -- capability-matrix --runtime-fixture release` | Passed | Regenerated `docs/runtime-truth-matrix.generated.md`; `agent.parallel_build` now reports reason `candidate_selection_receipt_present_but_planner_shard_verifier_policy_pending` and includes candidate-selection evidence. |
| `cargo clippy -p opensks-contracts -p opensks-daemon -p opensks-cli --all-targets -- -D warnings` | Passed | Focused clippy passed for the selection receipt contract, daemon candidate writer/event payloads, tests, schema generation, and capability truth updates. |
| `cargo fmt --all --check` | Passed | Rust formatting is clean after the candidate-selection receipt slice. |
| `bash scripts/check-architecture-ownership.sh` | Passed | Architecture guard still passes after the candidate-selection receipt slice. |
| `cargo run -p xtask -- architecture-graph --check` | Passed | Architecture graph still reports `architecture-graph: ok` after the candidate-selection receipt slice. |
| `cargo run -p xtask -- direct-io-audit --check` | Passed | Product direct-I/O guard still reports `direct-io-audit: ok` after writing selection receipts through the existing runtime artifact path. |
| `git diff --check` | Passed | No whitespace errors after the candidate-selection receipt slice. |
| `cargo test -p opensks-contracts --locked` | Passed | 65 contract tests passed after adding `IntegrationCandidateSelectionReceipt` and regenerating schemas. |
| `cargo test -p opensks-daemon --locked -- --test-threads=1` | Passed | 33 daemon tests passed after selection receipt emission, including provider image/vision E2E, provider route aggregate candidate selection, and all integration candidate apply paths. |
| `sks validate-artifacts latest` | Blocked | SKS validation remains blocked by `SKS update migration blocked: update_migration_lock_held`; the suggested `sks doctor --fix --yes` was not run without explicit approval. |
| `sks wiki validate .sneakoscope/wiki/context-pack.json` | Blocked | TriWiki validation remains blocked by `SKS update migration blocked: update_migration_lock_held`; the suggested `sks doctor --fix --yes` was not run without explicit approval. |
| `cargo metadata --format-version 1 --no-deps` | Passed | Workspace metadata resolved after `opensks-daemon` gained a product dependency on `opensks-graph` for objective-planner run starts. |
| `cargo test -p opensks-contracts --locked` | Passed | 64 contract tests passed after adding schema-backed `ObjectivePlanRequest` and `ObjectivePlanReceipt` with defaulted planner options and receipt refs. |
| `cargo test -p opensks-graph --locked` | Passed | 10 graph tests passed after adding objective-derived DAG generation for isolated worktree, role router, worker pool, verifier pool, approval-gated apply, optional image/research lanes, final seal, proof requirements, and objective-plan receipts. |
| `cargo test -p opensks-engine --locked` | Passed | 18 engine tests still pass with objective-planner graphs using the existing compile and scheduler planning boundary. |
| `cargo test -p opensks-cli graph --locked` | Passed | 8 CLI graph/codegraph tests passed after extracting graph command implementation into `graph_command.rs` and adding `opensks graph plan ... <objective>` artifact generation for graph, compiled plan, and receipt. |
| `cargo test -p opensks-cli capability --locked` | Passed | 2 CLI capability tests passed after `pipeline.graph` truth gained `graph:objective-planner` evidence while remaining Foundation pending live model planner generation. |
| `cargo test -p opensks-daemon run_start_request --locked -- --test-threads=1` | Passed | 5 daemon run-start tests passed, including `pipeline_id = objective-planner` generating an objective-derived graph, emitting `graph_source = objective_planner`, preserving workspace path redaction, and keeping saved graph-path validation intact. |
| `cargo run -p xtask -- schemas` | Passed | Regenerated schemas, including `schemas/objective-plan-request.schema.json` and `schemas/objective-plan-receipt.schema.json`. |
| `cargo run -p xtask -- capability-matrix --runtime-fixture release` | Passed | Regenerated `docs/runtime-truth-matrix.generated.md`; `pipeline.graph` now lists `graph:objective-planner` and reason `objective_planner_artifact_apply_seal_runtime_present_live_model_planner_pending`. |
| `cargo fmt --all --check` | Passed | Rust formatting is clean after the objective-planner slice and CLI graph-command extraction. |
| `cargo clippy -p opensks-contracts -p opensks-graph -p opensks-engine -p opensks-daemon -p opensks-cli --all-targets -- -D warnings` | Passed | Focused clippy passed for objective planner contracts, graph generation, daemon run-start integration, CLI artifacts, and capability truth updates. |
| `bash scripts/check-architecture-ownership.sh` | Passed | Architecture guard passes after moving graph command implementation out of the budget-sensitive CLI root while preserving public wrapper functions. |
| `cargo run -p xtask -- architecture-graph --check` | Passed | Architecture graph reports `architecture-graph: ok` after objective planner and CLI extraction changes. |
| `cargo run -p xtask -- direct-io-audit --check` | Passed | Product direct-I/O guard reports `direct-io-audit: ok`; new graph artifacts use existing atomic CLI artifact writes. |
| `git diff --check` | Passed | No whitespace errors after the objective-planner slice and ledger update. |
| `sks validate-artifacts latest` | Blocked | SKS validation remains blocked by `SKS update migration blocked: update_migration_lock_held`; the suggested `sks doctor --fix --yes` was not run without explicit approval. |
| `sks wiki validate .sneakoscope/wiki/context-pack.json` | Blocked | TriWiki validation remains blocked by `SKS update migration blocked: update_migration_lock_held`; the suggested `sks doctor --fix --yes` was not run without explicit approval. |
| `sks hook user-prompt-submit` | Blocked | SKS hook remains blocked by `SKS update migration blocked: update_migration_lock_held`; no new hook mission artifacts were materialized. The suggested `sks doctor --fix --yes` was not run without explicit approval. |
| `cargo test -p opensks-graph --locked` | Passed | 8 graph compiler tests passed after `CompiledPlan` gained a deterministic bounded `repair_plan`; invalid entry/duplicate/cycle diagnostics now produce targeted repair actions grouped into `entry_path`, `graph_integrity`, and `control_flow_conflict`, while valid default templates carry an explicit `no_repairs_required` plan with no groups. |
| `cargo test -p opensks-contracts --locked` | Passed | 63 contract tests passed after adding defaulted `CompileRepairPlan`, `CompileRepairAction`, and `CompileRepairGroup` payloads to `CompiledPlan`. |
| `cargo test -p opensks-engine --locked` | Passed | 18 engine tests passed with the additive compiled-plan repair contract preserved through graph compile and scheduler planning boundaries. |
| `cargo test -p opensks-cli graph --locked` | Passed | 7 CLI graph/codegraph tests passed after compiled plan artifacts began carrying the defaulted repair plan. |
| `cargo test -p opensks-cli capability --locked` | Passed | 2 CLI capability tests passed after `pipeline.graph` truth gained `graph:bounded-repair-plan` and `graph:repair-groups` evidence while remaining Foundation pending live planner generation. |
| `cargo run -p xtask -- schemas` | Passed | Regenerated schemas; `schemas/compiled-plan.schema.json` now includes the defaulted bounded repair plan, repair action contract, and repair groups. |
| `cargo run -p xtask -- capability-matrix --runtime-fixture release` | Passed | Regenerated `docs/runtime-truth-matrix.generated.md`; `pipeline.graph` now lists `graph:bounded-repair-plan` and `graph:repair-groups` with the updated live-planner pending reason code. |
| `cargo fmt --all --check` | Passed | Rust formatting is clean after the DAG grouped repair-plan slice. |
| `cargo clippy -p opensks-graph -p opensks-engine -p opensks-contracts -p opensks-cli --all-targets -- -D warnings` | Passed | Focused clippy passed for graph diagnostic-to-repair mapping, repair grouping, additive contract schema, engine compatibility, and capability truth updates. |
| `bash scripts/check-architecture-ownership.sh` | Passed | Architecture guard still passes after the DAG grouped repair-plan slice. |
| `cargo run -p xtask -- architecture-graph --check` | Passed | Architecture graph still reports `architecture-graph: ok` after the DAG grouped repair-plan slice. |
| `cargo run -p xtask -- direct-io-audit --check` | Passed | Product direct-I/O guard still reports `direct-io-audit: ok` after the DAG grouped repair-plan slice. |
| `git diff --check` | Passed | No whitespace errors after the DAG grouped repair-plan slice and ledger update. |
| `sks validate-artifacts latest` | Blocked | SKS validation remains blocked by `SKS update migration blocked: update_migration_lock_held`; the suggested `sks doctor --fix --yes` was not run without explicit approval. |
| `sks wiki validate .sneakoscope/wiki/context-pack.json` | Blocked | TriWiki validation remains blocked by `SKS update migration blocked: update_migration_lock_held`; the suggested `sks doctor --fix --yes` was not run without explicit approval. |
| `cargo test -p opensks-graph --locked` | Passed | 8 graph compiler tests passed after node config `requirement_ids`, structured `requirements`, and `acceptance_criteria` now compile into `WorkTemplate.requirement_ids` plus `CompletionContract.required_requirement_ids`/`requirements`, with malformed requirement config producing blocking diagnostics. |
| `cargo test -p opensks-engine --locked` | Passed | 18 engine tests passed after `plan_graph_for_scheduler` now forwards compiled requirement ids into scheduler work items; the new regression proves explicit and generated acceptance requirement ids survive the graph-to-scheduler boundary. |
| `cargo test -p opensks-cli graph --locked` | Passed | 7 CLI graph/codegraph tests passed after the compiled-plan schema gained structured proof requirements. |
| `cargo test -p opensks-contracts --locked` | Passed | 63 contract tests passed after `CompletionContract` gained defaulted structured `ProofRequirement` entries and schemas were regenerated. |
| `cargo test -p opensks-cli capability --locked` | Passed | 2 CLI capability tests passed after `pipeline.graph` truth changed to report DAG proof-contract evidence while remaining Foundation pending live planner generation. |
| `cargo run -p xtask -- schemas` | Passed | Regenerated schemas; `schemas/compiled-plan.schema.json` now includes structured proof requirements under the completion contract. |
| `cargo run -p xtask -- capability-matrix --runtime-fixture release` | Passed | Regenerated `docs/runtime-truth-matrix.generated.md`; `pipeline.graph` now lists graph compiler/proof-contract and engine scheduler propagation evidence. |
| `cargo fmt --all --check` | Passed | Rust formatting is clean after the DAG proof-contract requirement slice. |
| `cargo clippy -p opensks-graph -p opensks-engine -p opensks-contracts -p opensks-cli --all-targets -- -D warnings` | Passed | Focused clippy passed for graph requirement extraction/diagnostics, engine requirement propagation, contract schema updates, and capability truth changes. |
| `bash scripts/check-architecture-ownership.sh` | Passed | Architecture guard still passes after the DAG proof-contract requirement slice. |
| `cargo run -p xtask -- direct-io-audit --check` | Passed | Product direct-I/O guard still reports `direct-io-audit: ok` after the DAG proof-contract requirement slice. |
| `cargo run -p xtask -- architecture-graph --check` | Passed | Architecture graph still reports `architecture-graph: ok` after the DAG proof-contract requirement slice. |
| `git diff --check` | Passed | No whitespace errors after the DAG proof-contract requirement slice and ledger update. |
| `sks validate-artifacts latest` | Blocked | SKS validation remains blocked by `SKS update migration blocked: update_migration_lock_held`; the suggested `sks doctor --fix --yes` was not run automatically. |
| `sks wiki validate .sneakoscope/wiki/context-pack.json` | Blocked | TriWiki validation remains blocked by `SKS update migration blocked: update_migration_lock_held`; the suggested `sks doctor --fix --yes` was not run automatically. |
| `cargo fmt --all --check` | Passed | Rust formatting is clean after the aggregate integration-candidate receipt slice. |
| `cargo test -p opensks-contracts --locked` | Passed | 63 contract tests passed after adding generated schema-backed `IntegrationCandidateReceipt`, `RoleSubcontractCandidateReceipt`, and source-candidate refs. |
| `cargo test -p opensks-daemon conversation_supervisor_tick --locked -- --test-threads=1` | Passed | 5 daemon supervisor tests passed; the provider-route regression now proves the integration candidate receipt aggregates both the root `ROUTED_NOTE.md` patch and the Code-role `ROLE_CODE_NOTE.md` subcontract patch, exposes two source candidates, keeps both patches isolated from the main workspace, and keeps credential/workspace paths out of events. |
| `cargo test -p opensks-daemon integration_candidate_apply --locked -- --test-threads=1` | Passed | 5 integration-coordinator tests passed after adding aggregate candidate final-seal coverage: a two-source/two-target candidate verifies before approval, applies after a persisted approval event, writes both files to the main workspace, captures a multi-file `final.diff`, and seals both target paths. |
| `cargo test -p opensks-cli capability --locked` | Passed | 2 CLI capability tests passed after the runtime truth matrix changed `agent.parallel_build` to include aggregate integration-candidate evidence while remaining Foundation pending planner shard/verifier selection. |
| `cargo run -p xtask -- schemas` | Passed | Regenerated schemas including `schemas/integration-candidate-receipt.schema.json` and `schemas/role-subcontract-candidate-receipt.schema.json`. |
| `cargo run -p xtask -- capability-matrix --runtime-fixture release` | Passed | Regenerated `docs/runtime-truth-matrix.generated.md`; `agent.parallel_build` now lists `integration:role-candidate-aggregate`, `integration:aggregate-candidate-ready`, and `schema:integration-candidate-receipt`. |
| `cargo clippy -p opensks-contracts -p opensks-daemon -p opensks-cli --all-targets -- -D warnings` | Passed | Focused clippy passed for typed candidate receipts, daemon role-candidate aggregation, aggregate apply tests, schema generation, and capability truth updates. |
| `bash scripts/check-architecture-ownership.sh` | Passed | Architecture guard still passes after aggregate candidate receipt/schema changes. |
| `cargo run -p xtask -- direct-io-audit --check` | Passed | Product direct-I/O guard still reports `direct-io-audit: ok` after aggregate candidate receipt/schema changes. |
| `cargo run -p xtask -- architecture-graph --check` | Passed | Architecture graph still reports `architecture-graph: ok` after aggregate candidate receipt/schema changes. |
| `git diff --check` | Passed | No whitespace errors after aggregate candidate receipt/schema changes and ledger update. |
| `sks validate-artifacts latest` | Blocked | SKS validation remains blocked by `SKS update migration blocked: update_migration_lock_held`; the suggested `sks doctor --fix --yes` was not run automatically. |
| `sks wiki validate .sneakoscope/wiki/context-pack.json` | Blocked | TriWiki validation remains blocked by `SKS update migration blocked: update_migration_lock_held`; the suggested `sks doctor --fix --yes` was not run automatically. |
| `cargo test -p opensks-daemon integration_candidate_apply --locked -- --test-threads=1` | Passed | 4 daemon integration-coordinator tests passed after `integration_candidate_apply` gained explicit pre-approval `verification.json` receipts: approved applies link verification into `integration.json`, `final.diff`, `seal.json`, and timeline events; awaiting-approval receipts now show verifier passed while main workspace remains untouched; dirty targets and patch conflicts fail at verification with repair receipts and no final seal. |
| `cargo test -p opensks-git --locked` | Passed | 21 git tests passed after adding the read-only `check_unified_diff_apply` helper used by the integration verifier before approval. |
| `cargo test -p opensks-git-service commit_preview_and_receipt_link_matching_integration_final_diff --locked -- --test-threads=1` | Passed | Git-service final-diff linkage still works with the optional `verification_ref` field on integration apply receipts. |
| `cargo test -p opensks-contracts integration --locked` | Passed | Contract integration request roundtrip passed after adding generated `opensks.integration-verification-receipt.v1` and verification links on apply/final seal receipts. |
| `cargo run -p xtask -- schemas` | Passed | Regenerated schemas, including `schemas/integration-verification-receipt.schema.json`, updated `integration-apply-receipt`, and updated `integration-final-seal`. |
| `cargo test -p opensks-contracts capability --locked` | Passed | 6 capability tests passed after `agent.parallel_build` gained `integration:verification-receipt` evidence and a narrower remaining-gap reason. |
| `cargo test -p opensks-cli capability --locked` | Passed | 2 CLI capability tests passed after the runtime capability report/matrix exposed integration verification evidence. |
| `cargo run -p xtask -- capability-matrix --runtime-fixture release` | Passed | Regenerated `docs/runtime-truth-matrix.generated.md`; `agent.parallel_build` now lists `integration:verification-receipt` while remaining Foundation pending planner shard/aggregate orchestration. |
| `cargo test -p opensks-daemon --locked -- --test-threads=1` | Passed | 31 daemon tests passed after the explicit integration verification receipt slice, including provider route/image E2E, scheduler role subcalls, and all integration candidate apply paths. |
| `cargo test -p opensks-contracts --locked` | Passed | 62 contract tests passed after adding `IntegrationVerificationReceipt`, `verification_ref` links, and regenerated schemas. |
| `cargo fmt --all --check` | Passed | Rust formatting passed after applying rustfmt to the integration verifier slice. |
| `cargo clippy -p opensks-daemon -p opensks-git -p opensks-contracts -p opensks-git-service -p opensks-cli --all-targets -- -D warnings` | Passed | Focused clippy passed for explicit integration verification receipts, read-only git apply-check helper, optional receipt links, schema/capability updates, and downstream git-service compatibility. |
| `git diff --check` | Passed | No whitespace errors after the explicit integration verification receipt slice and ledger update. |
| `cargo run -p xtask -- architecture-graph --check` | Passed | Architecture graph still reports `architecture-graph: ok`. |
| `bash scripts/check-architecture-ownership.sh` | Failed | Current dirty worktree still violates architecture ownership gates unrelated to this slice: `crates/opensks-cli/src/lib.rs` is 8273 lines > 8000; `crates/opensks-adapter/src/lib.rs` is 1020 lines > 900; adapter public function count is 10 > 8 with unexpected `apply_file_writes_with_path_lease` and `from_scheduler_lease`. |
| `cargo run -p xtask -- direct-io-audit --check` | Failed | Current dirty worktree still fails direct-I/O audit at `crates/opensks-adapter/src/agentic.rs:1161`: product `std::fs::write` in `artifact.write` must be routed through the approved write path or otherwise reconciled with the release guard. |
| `sks validate-artifacts latest` | Blocked | SKS migration lock still blocks validation after the explicit integration verification receipt slice: `SKS update migration blocked: update_migration_lock_held`. Suggested `sks doctor --fix --yes` was not run automatically. |
| `sks wiki validate .sneakoscope/wiki/context-pack.json` | Blocked | SKS migration lock still blocks validation after the explicit integration verification receipt slice: `SKS update migration blocked: update_migration_lock_held`. Suggested `sks doctor --fix --yes` was not run automatically. |
| `cargo test -p opensks-daemon conversation_supervisor_tick_dispatches_provider_registry_route_through_chat_adapter --locked -- --test-threads=1` | Passed | Focused daemon provider-route regression passed after the Code role child began running a role-scoped agentic tool loop in its own isolation: the test proves a `ROLE_CODE_NOTE.md` role candidate patch under `.opensks/runtime/role-candidates/`, durable role-specific `file_patch_applied` timeline evidence, no main-workspace role file, and continued provider-side role overlap. |
| `cargo test -p opensks-daemon conversation_supervisor_tick --locked -- --test-threads=1` | Passed | 5 daemon supervisor tests passed after the Code role candidate path was added; provider image/vision E2E still completes when the Code role does not produce a patch candidate. |
| `cargo test -p opensks-daemon --locked -- --test-threads=1` | Passed | 31 daemon tests passed after Code role subcontracts gained isolated tool-loop execution and role-candidate artifact writing. |
| `cargo test -p opensks-contracts capability --locked` | Passed | 6 capability tests passed after `agent.parallel_build` evidence gained `daemon:role-worker-code-candidate` and the reason code narrowed to planner/shard/verifier/integration gaps. |
| `cargo test -p opensks-cli capability --locked` | Passed | 2 CLI capability tests passed after the runtime report/matrix exposed Code-role candidate subcontract evidence. |
| `cargo run -p xtask -- capability-matrix --runtime-fixture release` | Passed | Regenerated `docs/runtime-truth-matrix.generated.md`; `agent.parallel_build` now lists `daemon:role-worker-code-candidate` while remaining Foundation. |
| `cargo clippy -p opensks-daemon -p opensks-scheduler -p opensks-provider -p opensks-contracts -p opensks-cli --all-targets -- -D warnings` | Passed | Focused clippy passed for role-scoped agentic Code subcontract execution, event-sink worker-id rewriting, role-candidate artifact writing, capability truth updates, and existing scheduler/provider/CLI integration. |
| `cargo fmt --all --check` | Passed | Rust formatting passed after the Code-role subcontract candidate slice. |
| `git diff --check` | Passed | No whitespace errors after the Code-role subcontract candidate slice and ledger update. |
| `sks validate-artifacts latest` | Blocked | SKS migration lock still blocks validation after the Code-role subcontract candidate slice: `SKS update migration blocked: update_migration_lock_held`. Suggested `sks doctor --fix --yes` was not run automatically. |
| `sks wiki validate .sneakoscope/wiki/context-pack.json` | Blocked | SKS migration lock still blocks validation after the Code-role subcontract candidate slice: `SKS update migration blocked: update_migration_lock_held`. Suggested `sks doctor --fix --yes` was not run automatically. |
| `cargo test -p opensks-daemon conversation_supervisor_tick_dispatches_provider_registry_route_through_chat_adapter --locked -- --test-threads=1` | Passed | Focused daemon provider-route regression passed after role child workers began using `WorkerDriver::execute_batch` with scoped threads: the local OpenAI-compatible fixture observed overlapping role subcalls (`max_active_role_calls >= 2`) under the registry-derived per-model cap, scheduler role completions carry `daemon:role-worker-parallel-batch`, and timeline role events expose redacted batch size/lane metadata. |
| `cargo test -p opensks-daemon conversation_supervisor_tick --locked -- --test-threads=1` | Passed | 5 daemon supervisor tests passed with product role-worker batch parallelism enabled; provider image/vision E2E still completes root chat/image/vision tool loops plus post-root role subcalls. |
| `cargo test -p opensks-daemon --locked -- --test-threads=1` | Passed | 31 daemon tests passed after role worker subcalls moved from sequential default batch execution to scoped parallel dispatch for admitted role child batches. |
| `cargo test -p opensks-scheduler --locked` | Passed | 21 scheduler tests remained green; daemon role-worker parallelism uses scheduler-admitted batches and does not weaken lease, dependency, duplicate-outcome, semaphore, or recovery invariants. |
| `cargo test -p opensks-contracts capability --locked` | Passed | 6 capability tests passed after `agent.parallel_build` evidence gained `daemon:role-worker-parallel-batch` and `daemon:role-worker-model-call`, while the capability remains Foundation pending code-producing subcontracts. |
| `cargo test -p opensks-cli capability --locked` | Passed | 2 CLI capability tests passed after the runtime capability report/matrix picked up the daemon role-worker parallel evidence and updated reason code. |
| `cargo run -p xtask -- capability-matrix --runtime-fixture release` | Passed | Regenerated `docs/runtime-truth-matrix.generated.md`; `agent.parallel_build` now lists daemon role-worker parallel/model-call evidence and a narrower remaining gap reason. |
| `cargo clippy -p opensks-daemon -p opensks-scheduler -p opensks-provider -p opensks-contracts -p opensks-cli --all-targets -- -D warnings` | Passed | Focused clippy passed for scoped role-worker batch execution, provider-backed role subcalls, capability truth updates, scheduler/provider invariants, and CLI integration. |
| `cargo fmt --all --check` | Passed | Rust formatting passed after the product role-worker parallel dispatch slice. |
| `git diff --check` | Passed | No whitespace errors after the product role-worker parallel dispatch slice and ledger update. |
| `sks validate-artifacts latest` | Blocked | SKS migration lock still blocks validation after the product role-worker parallel dispatch slice: `SKS update migration blocked: update_migration_lock_held`. Suggested `sks doctor --fix --yes` was not run automatically. |
| `sks wiki validate .sneakoscope/wiki/context-pack.json` | Blocked | SKS migration lock still blocks validation after the product role-worker parallel dispatch slice: `SKS update migration blocked: update_migration_lock_held`. Suggested `sks doctor --fix --yes` was not run automatically. |
| `cargo test -p opensks-daemon --locked -- --test-threads=1` | Passed | 31 daemon tests passed after role child work items began performing bounded provider-backed no-tool chat subcalls; provider-route and provider image/vision E2E fixtures now prove root tool loops plus 4 post-root role model calls without credential/workspace path leakage. |
| `cargo test -p opensks-daemon conversation_supervisor_tick --locked -- --test-threads=1` | Passed | 5 daemon supervisor tests passed with role subcalls enabled; setup/read-only/local-test paths remain isolated from role draining, while provider-backed code and image turns complete role calls after the root turn succeeds. |
| `cargo test -p opensks-scheduler --locked` | Passed | 21 scheduler tests remained green; daemon role subcalls continue using scheduler dependency release, leases, and provider/model semaphore admission without changing scheduler invariants. |
| `cargo clippy -p opensks-daemon -p opensks-scheduler -p opensks-provider -p opensks-contracts -p opensks-cli --all-targets -- -D warnings` | Passed | Focused clippy passed for provider-backed role subcall dispatch, redacted role worker timeline payloads, scheduler role children, provider role router, capability evidence, and CLI integration. |
| `cargo fmt --all --check` | Passed | Rust formatting passed after the provider-backed role subcall slice. |
| `git diff --check` | Passed | No whitespace errors after the provider-backed role subcall slice and ledger update. |
| `sks validate-artifacts latest` | Blocked | SKS migration lock still blocks validation after the provider-backed role subcall slice: `SKS update migration blocked: update_migration_lock_held`. Suggested `sks doctor --fix --yes` was not run automatically. |
| `sks wiki validate .sneakoscope/wiki/context-pack.json` | Blocked | SKS migration lock still blocks validation after the provider-backed role subcall slice: `SKS update migration blocked: update_migration_lock_held`. Suggested `sks doctor --fix --yes` was not run automatically. |
| `cargo test -p opensks-daemon conversation_supervisor_tick_dispatches_provider_registry_route_through_chat_adapter --locked -- --test-threads=1` | Passed | Focused daemon provider-route regression passed after the scheduler now dispatches role child work items after the root code-edit turn completes; the event log proves 4 scheduler `WorkItemCompleted` role-child records, 4 timeline `worker_completed` role observations, selected provider/model ids, `daemon:role-worker-executed` evidence, and no credential/workspace path leak. |
| `cargo test -p opensks-daemon conversation_supervisor_tick --locked -- --test-threads=1` | Passed | 5 daemon supervisor tests passed after role child execution was added; failed/setup/read-only root turns do not try to drain dependent role children. |
| `cargo test -p opensks-scheduler --locked` | Passed | 21 scheduler tests remained green after daemon role-child execution began using existing scheduler dependency release and `dispatch_until_idle` semantics. |
| `cargo clippy -p opensks-daemon -p opensks-scheduler -p opensks-provider -p opensks-contracts -p opensks-cli --all-targets -- -D warnings` | Passed | Focused clippy passed for root-vs-role worker dispatch, role worker timeline events, provider role-router, scheduler role children, capability evidence, and CLI integration. |
| `cargo fmt --all --check` | Passed | Rust formatting passed after the daemon role-child execution slice. |
| `git diff --check` | Passed | No whitespace errors after the daemon role-child execution slice. |
| `sks validate-artifacts latest` | Blocked | SKS migration lock still blocks validation after the daemon role-child execution slice: `SKS update migration blocked: update_migration_lock_held`. Suggested `sks doctor --fix --yes` was not run automatically. |
| `sks wiki validate .sneakoscope/wiki/context-pack.json` | Blocked | SKS migration lock still blocks validation after the daemon role-child execution slice: `SKS update migration blocked: update_migration_lock_held`. Suggested `sks doctor --fix --yes` was not run automatically. |
| `cargo test -p opensks-scheduler conversation_turn_bootstrap_records_scheduler_root_item_idempotently --locked -- --test-threads=1` | Passed | Focused scheduler bootstrap regression passed after provider role-plan assignments began materializing as durable child `SchedulerWorkItem`s (`turn-role-*`) that depend on the root planning item, carry provider/model selectors, role-specific work kinds/capabilities, and emit `conversation.role_plan` `WorkItemQueued` events. |
| `cargo test -p opensks-daemon conversation_supervisor_tick_dispatches_provider_registry_route_through_chat_adapter --locked -- --test-threads=1` | Passed | Focused daemon provider-route regression passed after the provider-derived hyperparallel role plan began queuing 4 durable child role work items under the root scheduler item; the replayed event log proves parent/dependency wiring, selected provider/model ids, `scheduler:role-work-item-queued` evidence, single-model reuse evidence, and no credential/workspace path leak. |
| `cargo test -p opensks-scheduler --locked` | Passed | 21 scheduler tests passed after role-plan child work-item materialization; existing parallel dispatch, dependency release, semaphore, lease/fence, duplicate outcome, and recovery tests stayed green. |
| `cargo test -p opensks-daemon conversation_supervisor_tick --locked -- --test-threads=1` | Passed | 5 daemon supervisor tests passed after claimed-turn scheduler bootstrap started constructing the in-memory scheduler with root plus role child items. |
| `cargo clippy -p opensks-daemon -p opensks-scheduler -p opensks-provider -p opensks-contracts -p opensks-cli --all-targets -- -D warnings` | Passed | Focused clippy passed for scheduler role child item materialization, daemon scheduler construction from `bootstrap.work_items`, provider role-router, capability evidence, and CLI integration. |
| `cargo fmt --all --check` | Passed | Rust formatting passed after the role child work-item slice. |
| `git diff --check` | Passed | No whitespace errors after the role child work-item slice. |
| `sks validate-artifacts latest` | Blocked | SKS migration lock still blocks validation after the role child work-item slice: `SKS update migration blocked: update_migration_lock_held`. Suggested `sks doctor --fix --yes` was not run automatically. |
| `sks wiki validate .sneakoscope/wiki/context-pack.json` | Blocked | SKS migration lock still blocks validation after the role child work-item slice: `SKS update migration blocked: update_migration_lock_held`. Suggested `sks doctor --fix --yes` was not run automatically. |
| `cargo test -p opensks-scheduler conversation_turn_bootstrap_records_scheduler_root_item_idempotently --locked -- --test-threads=1` | Passed | Focused scheduler bootstrap regression passed after `ConversationTurnSchedulerInput` gained an optional compact provider role-plan summary; the queued root event and work item now carry `provider:role-routing`, `provider:health-cost-concurrency-scoring`, and single-model reuse evidence when supplied. |
| `cargo test -p opensks-daemon conversation_supervisor_tick_dispatches_provider_registry_route_through_chat_adapter --locked -- --test-threads=1` | Passed | Focused daemon provider-route regression passed with local mock-server binding approval; the durable `WorkItemQueued` payload now includes `role_plan_source = provider_registry`, a hyperparallel role plan, `distinct_model_count = 1`, `reused_model_count = 3`, and no credential/workspace path leak. |
| `cargo test -p opensks-scheduler --locked` | Passed | 21 scheduler tests passed after the scheduler bootstrap payload began carrying optional provider role-plan summaries. |
| `cargo test -p opensks-daemon conversation_supervisor_tick --locked -- --test-threads=1` | Passed | 5 daemon supervisor tests passed with local mock-server binding approval after accepted and claimed turn scheduler bootstrap began deriving provider role-plan summaries from the provider registry. |
| `cargo clippy -p opensks-daemon -p opensks-scheduler -p opensks-provider -p opensks-contracts -p opensks-cli --all-targets -- -D warnings` | Passed | Focused clippy passed for daemon role-plan derivation, scheduler role-plan payloads, provider role-router, capability evidence, and CLI matrix integration. |
| `cargo fmt --all --check` | Passed | Rust formatting passed after the daemon/scheduler role-plan receipt slice. |
| `git diff --check` | Passed | No whitespace errors after the daemon/scheduler role-plan receipt slice. |
| `sks validate-artifacts latest` | Blocked | SKS migration lock still blocks validation after the daemon/scheduler role-plan receipt slice: `SKS update migration blocked: update_migration_lock_held`. Suggested `sks doctor --fix --yes` was not run without explicit approval. |
| `sks wiki validate .sneakoscope/wiki/context-pack.json` | Blocked | SKS migration lock still blocks validation after the daemon/scheduler role-plan receipt slice: `SKS update migration blocked: update_migration_lock_held`. Suggested `sks doctor --fix --yes` was not run without explicit approval. |
| `cargo test -p opensks-provider role_routing --locked -- --test-threads=1` | Passed | Focused provider role-router regressions passed: hyperparallel planner/code/verifier/arbiter roles allocate distinct registry specialists when available, reuse a single model when no distinct candidate exists, and block only the role missing required capability. |
| `cargo test -p opensks-provider close_role_scores_consider_cost_and_model_concurrency --locked -- --test-threads=1` | Passed | Focused routing score regression passed: close role-score candidates consider provider pricing and model concurrency, selecting the cheaper/higher-concurrency model while retaining registry route receipts. |
| `cargo test -p opensks-provider --locked -- --test-threads=1` | Passed | 17 provider tests passed after rerunning with approval for local mock HTTP server binding; the sandboxed first run failed only at `TcpListener::bind` with `Operation not permitted`. |
| `cargo run -p xtask -- capability-matrix --runtime-fixture release` | Passed | Regenerated `docs/runtime-truth-matrix.generated.md`; `agent.parallel_build` evidence now includes provider role-routing and health/cost/concurrency scoring while remaining Foundation. |
| `cargo test -p opensks-contracts capability --locked` | Passed | 6 focused capability tests passed after `agent.parallel_build` evidence gained provider role-routing and health/cost/concurrency scoring. |
| `cargo test -p opensks-cli capability --locked` | Passed | CLI capability report/matrix tests passed after the release runtime matrix exposed provider role-routing evidence. |
| `cargo clippy -p opensks-provider -p opensks-contracts -p opensks-cli --all-targets -- -D warnings` | Passed | Focused clippy passed for provider role-routing, capability evidence, and CLI matrix integration. |
| `cargo fmt --all --check` | Passed | Rust formatting passed after the provider role-router slice. |
| `git diff --check` | Passed | No whitespace errors after the provider role-router slice. |
| `sks validate-artifacts latest` | Blocked | SKS migration lock still blocks validation: `SKS update migration blocked: update_migration_lock_held`. Suggested `sks doctor --fix --yes` was not run without explicit approval. |
| `sks wiki validate .sneakoscope/wiki/context-pack.json` | Blocked | SKS migration lock still blocks validation: `SKS update migration blocked: update_migration_lock_held`. Suggested `sks doctor --fix --yes` was not run without explicit approval. |
| `cargo test -p opensks-daemon conversation_supervisor_tick_dispatches_provider_registry_route_through_chat_adapter --locked -- --test-threads=1` | Passed | Focused daemon provider-route regression passed after turn scheduler bootstrap began deriving effective provider/model worker caps from the persisted provider registry; the queued root work item records `resource_limit_source = provider_registry`, `provider_max_workers = 2`, `per_provider_max_workers = 2`, `per_model_max_workers = 2`, and `scheduler:provider-registry-concurrency` evidence while thread settings request `max_parallelism = 8`. |
| `cargo test -p opensks-daemon conversation_supervisor_tick --locked -- --test-threads=1` | Passed | 5 supervisor tests passed after registry-derived scheduler caps were wired through turn acceptance and claimed-turn scheduler replay. |
| `cargo test -p opensks-scheduler --locked` | Passed | 21 scheduler tests passed after bootstrap accepted optional `ConversationTurnSchedulerResourceLimits`, event payloads gained effective provider/model worker caps, and existing semaphore/parallel/lease tests stayed green. |
| `cargo run -p xtask -- schemas` | Passed | Regenerated schemas after scheduler/daemon registry-concurrency wiring. |
| `cargo run -p xtask -- capability-matrix --runtime-fixture release` | Passed | Regenerated `docs/runtime-truth-matrix.generated.md`; `agent.parallel_build` evidence now includes `scheduler:provider-registry-concurrency`. |
| `cargo test -p opensks-contracts capability --locked` | Passed | 6 focused capability tests passed after `agent.parallel_build` evidence gained `scheduler:provider-registry-concurrency`. |
| `cargo test -p opensks-cli capability --locked` | Passed | CLI capability report/matrix tests passed after the release runtime matrix exposed scheduler registry-concurrency evidence. |
| `cargo clippy -p opensks-daemon -p opensks-scheduler -p opensks-contracts -p opensks-cli --all-targets -- -D warnings` | Passed | Focused clippy passed for daemon registry-derived scheduler limits, scheduler bootstrap/resource-limit config, capability evidence, and CLI matrix integration. |
| `cargo fmt --all --check` | Passed | Rust formatting passed after the registry-derived scheduler limits slice. |
| `git diff --check` | Passed | No whitespace errors after the registry-derived scheduler limits slice. |
| `sks validate-artifacts latest` | Blocked | SKS migration lock still blocks validation: `SKS update migration blocked: update_migration_lock_held`. Suggested `sks doctor --fix --yes` was not run without explicit approval. |
| `sks wiki validate .sneakoscope/wiki/context-pack.json` | Blocked | SKS migration lock still blocks validation: `SKS update migration blocked: update_migration_lock_held`. Suggested `sks doctor --fix --yes` was not run without explicit approval. |
| `cargo test -p opensks-scheduler worker_dispatch_respects_provider_and_model_semaphores --locked -- --test-threads=1` | Passed | Focused PR-093 regression passed: a scheduler batch with `per_provider_max_workers = 2` and `per_model_max_workers = 1` admits independent provider/model work, skips oversubscribed same-model/same-provider items, leaves skipped items `Ready`, and records `scheduler:provider-model-semaphore` evidence on running/result events and report outcomes. |
| `cargo test -p opensks-scheduler --locked` | Passed | 21 scheduler tests passed after provider/model semaphore admission and `provider_selector` work-item schema support were added; existing parallel batch, scoped worker context, lease/fence, duplicate outcome, and recovery tests still pass. |
| `cargo test -p opensks-contracts scheduler --locked` | Passed | Contract crate compiled with the defaulted `SchedulerWorkItem.provider_selector` field; no scheduler-filtered tests exist in this crate. |
| `cargo test -p opensks-cli scheduler --locked` | Passed | 5 CLI scheduler tests passed after CLI scheduler configs gained compatibility-preserving per-provider/per-model caps. |
| `cargo test -p opensks-contracts capability --locked` | Passed | 6 focused capability tests passed after `agent.parallel_build` evidence gained `scheduler:provider-model-semaphore`. |
| `cargo test -p opensks-cli capability --locked` | Passed | CLI capability report/matrix tests passed after the release runtime matrix exposed scheduler resource-semaphore evidence. |
| `cargo clippy -p opensks-scheduler -p opensks-contracts -p opensks-cli --all-targets -- -D warnings` | Passed | Focused clippy passed for scheduler semaphore admission, contract schema/evidence, and CLI config updates. |
| `cargo fmt --all --check` | Passed | Rust formatting passed after the provider/model semaphore slice. |
| `cargo run -p xtask -- schemas` | Passed | Regenerated schemas; `schemas/scheduler-work-item.schema.json` now includes defaulted `provider_selector`. |
| `cargo run -p xtask -- capability-matrix --runtime-fixture release` | Passed | Regenerated `docs/runtime-truth-matrix.generated.md`; `agent.parallel_build` evidence now includes `scheduler:provider-model-semaphore`. |
| `git diff --check` | Passed | No whitespace errors after the provider/model semaphore slice. |
| `sks validate-artifacts latest` | Blocked | SKS migration lock still blocks validation: `SKS update migration blocked: update_migration_lock_held`. Suggested `sks doctor --fix --yes` was not run without explicit approval. |
| `sks wiki validate .sneakoscope/wiki/context-pack.json` | Blocked | SKS migration lock still blocks validation: `SKS update migration blocked: update_migration_lock_held`. Suggested `sks doctor --fix --yes` was not run without explicit approval. |
| `cargo test -p opensks-daemon conversation_supervisor_tick_executes_claimed_local_test_turn --locked -- --test-threads=1` | Passed | Focused daemon regression passed after claimed turn execution moved behind `DurableScheduler::dispatch_ready_batch`; the scheduler root `WorkItemCompleted` event carries `daemon:turn-scheduler-worker-route`, `scheduler:lease-visible-to-worker`, `adapter:request-patch-lease`, and `patch-engine:fence-token-bound`, while the isolated PatchEngine journal redacts the raw scheduler lease/fence token. |
| `cargo test -p opensks-daemon conversation_supervisor_tick --locked -- --test-threads=1` | Passed | 5 supervisor tests passed: local-test edits, provider registry dispatch, provider image tools, setup-required failure, and read-only policy failure all still complete/fail correctly through the scheduler worker wrapper. |
| `cargo test -p opensks-contracts capability --locked` | Passed | 6 focused capability tests passed after `agent.code_edit` evidence gained `daemon:turn-scheduler-worker-route`. |
| `cargo test -p opensks-cli capability --locked` | Passed | CLI capability report/matrix tests passed after the release runtime overlay began exposing daemon scheduler-worker route evidence. |
| `cargo clippy -p opensks-daemon -p opensks-contracts -p opensks-cli --all-targets -- -D warnings` | Passed | Focused clippy passed for daemon scheduler-worker routing, capability evidence, and CLI runtime overlay. |
| `cargo fmt --all --check` | Passed | Rust formatting passed after the daemon scheduler-worker route slice. |
| `cargo run -p xtask -- schemas` | Passed | Regenerated schemas after the daemon scheduler-worker route slice; no public schema drift was introduced. |
| `cargo run -p xtask -- capability-matrix --runtime-fixture release` | Passed | Regenerated `docs/runtime-truth-matrix.generated.md`; `agent.code_edit` evidence now includes `daemon:turn-scheduler-worker-route`. |
| `git diff --check` | Passed | No whitespace errors after the daemon scheduler-worker route slice. |
| `sks validate-artifacts latest` | Blocked | SKS migration lock still blocks validation: `SKS update migration blocked: update_migration_lock_held`. Suggested `sks doctor --fix --yes` was not run without explicit approval. |
| `sks wiki validate .sneakoscope/wiki/context-pack.json` | Blocked | SKS migration lock still blocks validation: `SKS update migration blocked: update_migration_lock_held`. Suggested `sks doctor --fix --yes` was not run without explicit approval. |
| `cargo test -p opensks-patch-engine --locked` | Passed | 18 PatchEngine tests passed after adding attempt-aware journal entries and `PatchEngine::recovery_report`; repeated applies under one proposal now recover two attempts and identify the latest terminal applied state. |
| `cargo test -p opensks-patch-engine recovery_report_tracks_repeated_attempts_and_latest_terminal_state --locked -- --test-threads=1` | Passed | Focused attempt-aware recovery regression passed: attempt 1 is a stale rejection, attempt 2 applies, the journal carries `attempt_index`/`attempt_id`, and the report returns `latest_attempt = attempt-2` without operator review. |
| `cargo test -p opensks-contracts capability --locked` | Passed | 6 focused capability tests passed after `agent.code_edit` and local test edit evidence gained `patch-engine:attempt-aware-recovery`. |
| `cargo test -p opensks-cli capability --locked` | Passed | CLI capability report/matrix tests passed after the release runtime overlay began exposing `patch-engine:attempt-aware-recovery`. |
| `cargo clippy -p opensks-patch-engine -p opensks-contracts -p opensks-cli --all-targets -- -D warnings` | Passed | Focused clippy passed for PatchEngine attempt-aware recovery, capability evidence, and CLI runtime overlay. |
| `cargo fmt --all --check` | Passed | Rust formatting passed after the attempt-aware recovery slice. |
| `git diff --check` | Passed | No whitespace errors after the attempt-aware recovery slice. |
| `cargo run -p xtask -- schemas` | Passed | Regenerated schemas after capability evidence updates; no public contract schema drift was introduced. |
| `cargo run -p xtask -- capability-matrix --runtime-fixture release` | Passed | Regenerated `docs/runtime-truth-matrix.generated.md`; `agent.code_edit` evidence now includes `patch-engine:attempt-aware-recovery`. |
| `sks validate-artifacts latest` | Blocked | SKS migration lock still blocks validation: `SKS update migration blocked: update_migration_lock_held`. Suggested `sks doctor --fix --yes` was not run without explicit approval. |
| `sks wiki validate .sneakoscope/wiki/context-pack.json` | Blocked | SKS migration lock still blocks validation: `SKS update migration blocked: update_migration_lock_held`. Suggested `sks doctor --fix --yes` was not run without explicit approval. |
| `sks hook user-prompt-submit` | Blocked | SKS hook remains blocked by `update_migration_lock_held`; no new hook mission artifacts were materialized. |
| `cargo test -p opensks-patch-engine --locked` | Passed | 17 PatchEngine tests passed after adding deterministic apply-stage fault injection; coverage proves a mid-transaction IO fault rolls back a previously modified file, leaves the faulted file unchanged, returns `io_rolled_back`, emits `patch-engine:rollback-attempted`, and journals `rolled_back` with the prior applied path. |
| `cargo test -p opensks-patch-engine injected_apply_io_fault_rolls_back_prior_write_and_journals --locked -- --test-threads=1` | Passed | Focused rollback fault-injection regression passed. |
| `cargo test -p opensks-contracts capability --locked` | Passed | 6 focused capability tests passed after `agent.code_edit` and local test edit evidence gained `patch-engine:rollback-fault-injected`. |
| `cargo test -p opensks-cli capability --locked` | Passed | CLI capability report/matrix tests passed after the release runtime overlay began exposing `patch-engine:rollback-fault-injected`. |
| `cargo clippy -p opensks-patch-engine -p opensks-contracts -p opensks-cli --all-targets -- -D warnings` | Passed | Focused clippy passed for PatchEngine apply fault hook/test, capability evidence, and CLI runtime overlay. |
| `cargo fmt --all --check` | Passed | Rust formatting passed after the rollback fault-injection slice. |
| `git diff --check` | Passed | No whitespace errors after the rollback fault-injection slice. |
| `cargo run -p xtask -- schemas` | Passed | Regenerated schemas after capability evidence updates; no public schema drift was introduced. |
| `cargo run -p xtask -- capability-matrix --runtime-fixture release` | Passed | Regenerated `docs/runtime-truth-matrix.generated.md`; `agent.code_edit` evidence now includes `patch-engine:rollback-fault-injected`. |
| `cargo test -p opensks-adapter --locked -- --test-threads=1` | Passed | 67 adapter tests passed with 1 ignored live OpenRouter smoke after `AgentRunRequest` gained an optional scheduler patch lease; local-test and agentic-loop write tests prove scheduler lease ids are hashed into PatchEngine journal context, raw fence tokens are redacted, and writes still land through PatchEngine. |
| `cargo test -p opensks-scheduler --locked` | Passed | 20 scheduler tests passed after adding coverage that `WorkerDriver::execute` receives a post-acquisition `SchedulerWorkItem` with the active lease/fence token attached for downstream PatchEngine binding. |
| `cargo test -p opensks-adapter local_test_adapter_uses_request_scheduler_patch_lease_without_leaking_raw_fence --locked -- --test-threads=1` | Passed | Focused local-test adapter regression passed: request-level scheduler lease drives PatchEngine context hashing without raw token leakage. |
| `cargo test -p opensks-scheduler worker_execute_receives_scheduler_lease_for_downstream_patch_fencing --locked` | Passed | Focused scheduler regression passed: holder selection happens before lease acquisition, while worker execution receives the active scheduler lease. |
| `cargo test -p opensks-adapter loop_reads_then_edits_then_observes_the_change_then_finishes --locked -- --test-threads=1` | Passed | Agentic loop regression passed with request-level scheduler lease binding and journal redaction assertions. |
| `cargo test -p opensks-contracts capability --locked` | Passed | 6 focused capability tests passed after `agent.code_edit` evidence gained `adapter:request-patch-lease` and `scheduler:lease-visible-to-worker`. |
| `cargo test -p opensks-cli capability --locked` | Passed | CLI capability report/matrix tests passed after the release runtime overlay began exposing scheduler/request patch-lease evidence. |
| `cargo clippy -p opensks-adapter -p opensks-scheduler -p opensks-contracts -p opensks-cli --all-targets -- -D warnings` | Passed | Focused clippy passed for request-level patch lease propagation, scheduler worker lease visibility, capability evidence, and CLI runtime overlay. |
| `cargo fmt --all --check` | Passed | Rust formatting passed after the request-level scheduler patch lease slice. |
| `git diff --check` | Passed | No whitespace errors after the request-level scheduler patch lease slice. |
| `cargo run -p xtask -- schemas` | Passed | Regenerated schemas after the request-level lease slice; no public schema drift was introduced. |
| `cargo run -p xtask -- capability-matrix --runtime-fixture release` | Passed | Regenerated `docs/runtime-truth-matrix.generated.md`; `agent.code_edit` evidence now includes `adapter:request-patch-lease` and `scheduler:lease-visible-to-worker`. |
| `sks validate-artifacts latest` | Blocked | SKS migration lock still blocks validation: `SKS update migration blocked: update_migration_lock_held`. Suggested `sks doctor --fix --yes` was not run without explicit approval. |
| `sks wiki validate .sneakoscope/wiki/context-pack.json` | Blocked | SKS migration lock still blocks validation: `SKS update migration blocked: update_migration_lock_held`. Suggested `sks doctor --fix --yes` was not run without explicit approval. |
| `sks hook user-prompt-submit` | Blocked | SKS hook remains blocked by `update_migration_lock_held`; no new hook mission artifacts were materialized. |
| `cargo test -p opensks-patch-engine --locked` | Passed | 16 PatchEngine tests passed after adding context-bound path lease/fence enforcement; coverage proves successful applies record redacted hashed lease/fence metadata, raw fence tokens are not journaled, and unleased touched paths reject with `path_lease_missing` before writing. |
| `cargo test -p opensks-adapter --locked -- --test-threads=1` | Passed | 66 adapter tests passed with 1 ignored live OpenRouter smoke; local-test and agentic tool-loop writes now bind PatchEngine applies to run/proposal path leases. The image helper regression was fixed so a registry-default `Ask` entry can be explicitly overridden to `Allow` in tests. |
| `cargo test -p opensks-adapter image_generate --locked -- --test-threads=1` | Passed | 4 focused image generation tests passed after the allow-helper override fix. |
| `cargo test -p opensks-adapter loop_reads_then_edits_then_observes_the_change_then_finishes --locked -- --test-threads=1` | Passed | Agentic loop write evidence includes `patch-engine:path-lease-bound` and `patch-engine:fence-token-bound`. |
| `cargo test -p opensks-adapter local_test_adapter_really_edits_a_file_on_disk --locked -- --test-threads=1` | Passed | Local test adapter write evidence includes `patch-engine:path-lease-bound` and `patch-engine:fence-token-bound`. |
| `cargo test -p opensks-contracts capability --locked` | Passed | 6 focused capability tests passed after `agent.code_edit` and local test edit capability evidence gained path-lease/fence binding refs. |
| `cargo test -p opensks-cli capability --locked` | Passed | CLI capability report/matrix tests passed after the runtime overlay began exposing path-lease/fence binding refs. |
| `cargo clippy -p opensks-patch-engine -p opensks-adapter -p opensks-contracts -p opensks-cli --all-targets -- -D warnings` | Passed | Focused clippy passed for PatchEngine context-bound apply, adapter lease helpers, agentic write binding, and capability evidence. |
| `cargo fmt --all --check` | Passed | Rust formatting passed after the context-bound path lease/fence slice. |
| `cargo run -p xtask -- schemas` | Passed | Regenerated schemas after capability evidence updates; no new public PatchEngine schema field was required. |
| `cargo run -p xtask -- capability-matrix --runtime-fixture release` | Passed | Regenerated `docs/runtime-truth-matrix.generated.md`; `agent.code_edit` evidence now includes `patch-engine:path-lease-bound` and `patch-engine:fence-token-bound`. |
| `cargo test -p opensks-patch-engine --locked` | Passed | 14 PatchEngine tests passed after adding conservative stale sibling temp scavenging; coverage proves only old PatchEngine-owned temp names for the active target are removed, malformed/other-target temps stay, and fresh matching temps are preserved. |
| `cargo test -p opensks-contracts capability --locked` | Passed | 6 focused capability tests passed after capability evidence gained `patch-engine:stale-temp-scavenger`. |
| `cargo test -p opensks-cli capability --locked` | Passed | CLI capability report/matrix tests passed after the runtime overlay began exposing `patch-engine:stale-temp-scavenger`. |
| `cargo clippy -p opensks-patch-engine -p opensks-contracts -p opensks-cli --all-targets -- -D warnings` | Passed | Focused clippy passed for the stale temp scavenger, engine-local test clock, capability evidence, and CLI report overlay. |
| `cargo fmt --all --check` | Passed | Rust formatting passed after the PatchEngine stale-temp scavenger slice. |
| `cargo run -p xtask -- schemas` | Passed | Regenerated schemas after capability evidence updates; no new PatchEngine schema field was required. |
| `cargo run -p xtask -- capability-matrix --runtime-fixture release` | Passed | Regenerated `docs/runtime-truth-matrix.generated.md`; `agent.code_edit` evidence now includes `patch-engine:stale-temp-scavenger`. |
| `cargo test -p opensks-patch-engine --locked` | Passed | 12 PatchEngine tests passed after adding immediate pre-apply preimage/destination revalidation; the new deterministic TOCTOU regression mutates a target after batch preflight, verifies `toctou_precondition`, rolls back the prior applied write, preserves the concurrent change, and journals the rollback. |
| `cargo test -p opensks-contracts capability --locked` | Passed | 6 focused capability tests passed after capability evidence gained `patch-engine:pre-apply-revalidated`. |
| `cargo test -p opensks-cli capability --locked` | Passed | CLI capability report/matrix tests passed after the runtime overlay began exposing `patch-engine:pre-apply-revalidated`. |
| `cargo clippy -p opensks-patch-engine -p opensks-contracts -p opensks-cli --all-targets -- -D warnings` | Passed | Focused clippy passed for the PatchEngine TOCTOU revalidation path, reason-code helpers, capability evidence, and CLI report overlay. |
| `cargo fmt --all --check` | Passed | Rust formatting passed after the PatchEngine revalidation slice. |
| `cargo run -p xtask -- schemas` | Passed | Regenerated schemas after capability evidence updates; no new PatchEngine schema field was required. |
| `cargo run -p xtask -- capability-matrix --runtime-fixture release` | Passed | Regenerated `docs/runtime-truth-matrix.generated.md`; `agent.code_edit` evidence now includes `patch-engine:pre-apply-revalidated`. |
| `cargo test -p opensks-contracts --locked` | Passed | 62 contract tests passed after promoting `image.generate`/`image.inspect` registry descriptors to available provider-backed tools while keeping capability availability false until an enabled image/vision route exists. |
| `cargo test -p opensks-adapter tool_definitions --locked` | Passed | 2 adapter tests passed proving default provider tool schemas still do not advertise runtime-gated image tools, while daemon-prepared image/vision executors can selectively advertise `image__generate` and `image__inspect`. |
| `cargo test -p opensks-cli capability --locked` | Passed | CLI capability report/matrix tests passed after runtime reports began exposing `image.generate` and `image.inspect` with provider route-required reason codes. |
| `cargo clippy -p opensks-contracts -p opensks-adapter -p opensks-cli --all-targets -- -D warnings` | Passed | Focused clippy passed after removing the stale `unavailable_tool` helper and adding runtime-gated image schema filtering. |
| `cargo run -p xtask -- schemas` | Passed | Regenerated schemas after image/vision tool registry reason codes and capability truth changed. |
| `cargo run -p xtask -- capability-matrix --runtime-fixture release` | Passed | Regenerated `docs/runtime-truth-matrix.generated.md`; image generation/inspection now show Degraded maturity, `Available = no`, and route-required evidence, while Tool Registry rows show provider executor availability. |
| `swift test --package-path swift --filter OpenSKSStudioTests.CapabilityTests` | Passed | 3 Swift capability tests still passed against the optional Tool Registry decode and disabled-row fixture after registry truth changed. |
| `swift test --package-path swift --filter OpenSKSStudioTests.ReleaseReadinessTests/testSettingsWorkspaceRendersAndFillsWidth` | Passed | Settings route still renders after the generated capability report/matrix change. |
| `git diff --check` | Passed | No whitespace errors after the image/vision registry truth slice. |
| `swift test --package-path swift --filter OpenSKSStudioTests.CapabilityTests` | Passed | 3 Swift capability tests passed after Settings gained a Tool Registry status view: available/unavailable labels, counts, availability-first sorting, older-report compatibility, and offscreen rendering for available plus disabled tool rows. |
| `swift test --package-path swift --filter OpenSKSStudioTests.ReleaseReadinessTests/testSettingsWorkspaceRendersAndFillsWidth` | Passed | Settings route still renders non-nil and fills 1024/1440 widths after loading the full capability report and showing registry-backed tool rows. |
| `swift build --package-path swift` | Passed | Swift package builds after wiring `RuntimeCapabilityReport.toolRegistry` into Settings. |
| `git diff --check` | Passed | No whitespace errors after the Swift Tool Registry Settings UI slice. |
| `swift-format lint swift/Sources/SharedUI/Capability.swift swift/Sources/SharedUI/SettingsWorkspaceView.swift swift/Tests/OpenSKSStudioTests/CapabilityTests.swift` | Blocked | `swift-format` is not installed in this shell (`command not found`); Swift syntax/compile was still verified by focused Swift tests and `swift build`. |
| `cargo test -p opensks-contracts capability --locked` | Passed | 6 focused capability tests passed after `RuntimeCapabilityReport` gained the canonical `tool_registry` snapshot, registry validation, and generated matrix Tool Registry coverage. |
| `cargo test -p opensks-cli capability --locked` | Passed | CLI capability report/matrix tests passed, proving runtime reports emit available `mcp.invoke`, route-gated provider-backed `image.generate`, and matrix `Tool Registry` rows from the canonical registry. |
| `swift test --package-path swift --filter OpenSKSStudioTests.CapabilityTests` | Passed | 3 Swift capability DTO tests passed, including decoding the new optional `tool_registry`, ignoring `input_schema` safely, and preserving compatibility with older reports that omit the registry. |
| `cargo run -p xtask -- schemas` | Passed | Regenerated schemas, including `schemas/runtime-capability-report.schema.json` with the defaulted `tool_registry` field and embedded `ToolRegistry` definitions. |
| `cargo run -p xtask -- capability-matrix --runtime-fixture release` | Passed | Regenerated `docs/runtime-truth-matrix.generated.md` with a Tool Registry section listing availability, permission, reason code, and evidence for each runtime tool. |
| `cargo test -p opensks-contracts --locked` | Passed | 62 contract tests passed after adding ToolRegistry validation and runtime capability report embedding. |
| `cargo clippy -p opensks-contracts -p opensks-cli --all-targets -- -D warnings` | Passed | Clippy passed for the embedded registry report contract and CLI matrix/report assertions. |
| `cargo fmt --all --check` and `git diff --check` | Passed | Rust formatting and whitespace checks passed after the runtime capability report slice. |
| `swift-format lint swift/Sources/SharedUI/Capability.swift swift/Tests/OpenSKSStudioTests/CapabilityTests.swift` | Blocked | `swift-format` is not installed in this shell (`command not found`); Swift syntax/compile was still verified by the focused Swift capability test build. |
| `sks validate-artifacts latest` | Blocked | SKS migration lock blocks validation: `SKS update migration blocked: update_migration_lock_held`. Suggested `sks doctor --fix --yes` was not run without explicit approval. |
| `sks wiki validate .sneakoscope/wiki/context-pack.json` | Blocked | SKS migration lock blocks validation: `SKS update migration blocked: update_migration_lock_held`. Suggested `sks doctor --fix --yes` was not run without explicit approval. |
| `cargo test -p opensks-adapter skill --locked` | Passed | 4 focused tests passed for canonical `skill__invoke` parsing, default approval blocking, safe local skill-context loading, payload/content redaction, missing-skill handling, and pathlike skill-name rejection. |
| `cargo test -p opensks-adapter --locked` | Passed | 66 adapter tests passed with 1 ignored live OpenRouter smoke after adding the local skill registry executor. |
| `cargo test -p opensks-contracts --locked` | Passed | 61 contract tests passed after marking `skill.invoke` available through the ToolRegistry with `Ask` permission and `local_skill_registry_executable` evidence. |
| `cargo clippy -p opensks-adapter -p opensks-contracts --all-targets -- -D warnings` | Passed | Clippy passed for the local skill registry executor, provider parser, registry descriptor, and effectful-tool read-only guard. |
| `cargo fmt --all --check` and `git diff --check` | Passed | Rust formatting and whitespace checks passed after the skill executor slice. |
| `cargo test -p opensks-adapter mcp --locked` | Passed | 5 focused tests passed for canonical `mcp__invoke` parsing, default approval blocking, allowlisted local `opensks.repo.search` execution, unknown MCP tool rejection, and read-only blocking for effectful command/MCP tools. |
| `cargo test -p opensks-adapter --locked` | Passed | 62 adapter tests passed with 1 ignored live OpenRouter smoke after adding the local MCP broker executor. |
| `cargo test -p opensks-contracts --locked` | Passed | 61 contract tests passed after marking `mcp.invoke` available through the ToolRegistry with `Ask` permission and `local_mcp_broker_executable` evidence. |
| `cargo clippy -p opensks-adapter -p opensks-contracts --all-targets -- -D warnings` | Passed | Clippy passed for the local MCP broker executor, provider parser, registry descriptor, and effectful-tool read-only guard. |
| `cargo fmt --all --check` and `git diff --check` | Passed | Rust formatting and whitespace checks passed after the MCP executor slice. |
| `cargo test -p opensks-adapter command --locked` | Passed | 5 focused tests passed for canonical `command__run`/`test__run_targeted` provider parsing, default approval blocking, shell metacharacter rejection, allowed argv command execution, and sensitive argument redaction. |
| `cargo test -p opensks-adapter test_run_targeted --locked` | Passed | 2 focused tests passed for `test.run_targeted` shape enforcement and a real `cargo test --help` argv execution receipt. |
| `cargo test -p opensks-adapter --locked` | Passed | 57 adapter tests passed with 1 ignored live OpenRouter smoke after adding command/test ToolGateway execution. |
| `cargo test -p opensks-contracts --locked` | Passed | 61 contract tests passed after marking `command.run` and `test.run_targeted` available through the ToolRegistry with `Ask` permission. |
| `cargo clippy -p opensks-adapter -p opensks-contracts --all-targets -- -D warnings` | Passed | Clippy passed for the argv command executor, provider parser, and ToolRegistry descriptors. |
| `cargo fmt --all --check` and `git diff --check` | Passed | Rust formatting and whitespace checks passed after the command/test executor slice. |
| `cargo test -p opensks-git-service commit_preview_and_receipt_link_matching_integration_final_diff --locked` | Passed | Git commit preview and commit receipt now auto-link the newest integrated `integration.json` / `final.diff` whose content hash matches the current staged diff. |
| `cargo test -p opensks-contracts -p opensks-git-service --locked` | Passed | 61 contract tests and 31 git-service tests passed after adding optional integration final-diff fields and the content-hash matcher. |
| `cargo test -p opensks-conversation execution_event_replay_materializes_git_receipts_as_typed_timeline_items --locked` | Passed | Git receipt event replay preserves `integration_final_diff_ref`, run id, and candidate id in typed timeline payloads. |
| `cargo test -p opensks-cli receipt_event_append --locked` | Passed | Runless receipt append preserves integration final-diff evidence through the event journal projection. |
| `cargo test -p opensks-cli conversation --locked` | Passed | 6 CLI conversation tests passed after extending git receipt payloads. |
| `swift test --package-path swift --filter OpenSKSStudioTests.GitCommitTests` | Passed | 19 Swift Git commit tests passed, including preview decode and conversation receipt assertions for integration final-diff evidence. |
| `cargo clippy -p opensks-contracts -p opensks-git-service -p opensks-conversation -p opensks-cli --all-targets -- -D warnings` | Passed | Clippy passed for integration final-diff matching and payload propagation. |
| `cargo run -p xtask -- schemas` | Passed | Regenerated `git-commit-preview` and `git-commit` schemas with optional integration final-diff evidence fields. |
| `cargo fmt --all --check` | Passed | Rust formatting is clean after adding integration final-diff matching. |
| `git diff --check` | Passed | No whitespace errors after adding integration final-diff matching. |
| `cargo run -p xtask -- schemas` | Passed | Regenerated `git-commit-preview` and `git-commit` schemas after adding reviewed staged-diff evidence fields. |
| `cargo test -p opensks-contracts -p opensks-git-service --locked` | Passed | 61 contract tests and 30 git-service tests passed, including commit preview/receipt round-trips and commit receipt echoing the reviewed staged-diff hash/ref. |
| `cargo test -p opensks-conversation --locked` | Passed | 22 conversation tests passed, including git receipt event replay preserving reviewed staged-diff evidence in typed timeline payloads. |
| `cargo test -p opensks-cli conversation --locked` | Passed | 6 CLI conversation tests passed after the git receipt payload extension. |
| `cargo test -p opensks-cli receipt_event_append --locked` | Passed | Runless receipt-event append now preserves reviewed staged-diff evidence through the event journal projection. |
| `swift test --package-path swift --filter OpenSKSStudioTests.GitCommitTests` | Passed | 19 Swift Git commit tests passed, including preview decode, commit receipt echo, receipt UI render, and conversation timeline payload evidence assertions. |
| `cargo clippy -p opensks-contracts -p opensks-git-service -p opensks-conversation -p opensks-cli --all-targets -- -D warnings` | Passed | Clippy passed for reviewed staged-diff evidence binding in contracts, git service, conversation projection, and CLI receipt append. |
| `cargo fmt --all --check` | Passed | Rust formatting is clean after adding reviewed staged-diff evidence binding. |
| `git diff --check` | Passed | No whitespace errors after the reviewed staged-diff evidence binding slice. |
| `swift test --package-path swift --filter OpenSKSStudioTests.GitCommitTests/testSuccessfulCommitPostsConversationCardWithExactPaths` | Passed | Commit card sink now verifies the asynchronous event-backed receipt item `timeline-event-git-commit:<sha>` after the optimistic local card. |
| `swift test --package-path swift --filter OpenSKSStudioTests.PushOutboxTests/testSuccessfulPushPostsSeparatePushCard` | Passed | Push card sink now verifies separate event-backed commit and push receipt items after approved push completion. |
| `swift test --package-path swift --filter OpenSKSStudioTests.ConversationTurnTests` | Passed | 18 conversation-turn tests passed, including live git receipt event rendering and background live-timeline subscription coverage. |
| `cargo test -p opensks-cli receipt_event_append --locked` | Passed | `conversation receipt-event-append` creates a hidden conversation anchor, appends one git receipt event to the durable event journal, projects the typed timeline item, and is idempotent on retry. |
| `cargo test -p opensks-conversation --locked` | Passed | 22 conversation tests passed after adding hidden external receipt anchors and replayable git receipt timeline projection. |
| `cargo test -p opensks-cli conversation --locked` | Passed | 6 CLI conversation tests passed after adding the receipt-event append subcommand. |
| `cargo fmt --all --check` | Passed | Rust formatting is clean after adding runless Git receipt event append. |
| `cargo clippy -p opensks-conversation -p opensks-cli --all-targets -- -D warnings` | Passed | Clippy passed for the external receipt anchor and CLI event append path. |
| `git diff --check` | Passed | No whitespace errors after adding event-backed Git Studio receipt posting and tests. |
| `cargo run -p xtask -- schemas` | Passed | Regenerated schemas after adding `git_commit_receipt`, `git_push_receipt`, and `git_push_failed` to `opensks.execution-event-envelope.v1`. |
| `cargo test -p opensks-contracts --locked` | Passed | 60 contract tests passed, including stable parsing/serialization for the new git receipt execution-event labels. |
| `cargo test -p opensks-conversation --locked` | Passed | 22 conversation tests passed, including event-journal replay materializing commit, push, and failed-push receipt events as typed timeline items with idempotent replay. |
| `cargo test -p opensks-engine projection --locked` | Passed | 5 engine projection tests passed after classifying git receipt events as informational/non-state-changing projection events. |
| `cargo clippy -p opensks-contracts -p opensks-conversation -p opensks-engine --all-targets -- -D warnings` | Passed | Clippy passed for the new event labels, timeline projection flattening, and engine projection classification. |
| `cargo test -p opensks-daemon subscribe_events --locked` | Passed | 3 daemon subscribe-events tests passed, including conversation timeline projection materialization from durable execution events. |
| `cargo test -p opensks-cli conversation --locked` | Passed | 6 CLI conversation tests passed after schema/event changes. |
| `swift test --package-path swift --filter OpenSKSStudioTests.ConversationTurnTests` | Passed | 18 Swift conversation-turn tests passed, including live git receipt events rendering commit/push receipt cards before durable reload. |
| `swift test --package-path swift --filter OpenSKSStudioTests.ContractsTests` | Passed | 34 Swift contract tests passed after adding the new `ExecutionEventKind` values. |
| `cargo run -p xtask -- schemas` | Passed | Regenerated schemas after adding `opensks.push-failure-diagnostic.v1` and the defaulted `PushStatus.failures` recovery surface. |
| `cargo test -p opensks-contracts -p opensks-git --locked` | Passed | 60 contract tests and 21 git tests passed, including secretless push-failure diagnostic roundtrip and durable failed-push recovery after reopening the outbox. |
| `cargo test -p opensks-cli push_cli --locked` | Passed | 3 CLI push tests passed, including failed `push-execute` returning `push_failed` and `push-status` recovering one `opensks.push-failure-diagnostic.v1` with the reviewed redacted remote, retryable approved intent, and no completed receipt. |
| `swift test --package-path swift --filter OpenSKSStudioTests.PushOutboxTests` | Passed | 23 Swift PushOutbox tests passed after surfacing recovered push failure diagnostics in `PushOutboxSummary` and keeping failure-only outboxes visible. |
| `cargo fmt --all --check` | Passed | Rust formatting is clean after adding git receipt execution-event projection. |
| `cargo clippy -p opensks-contracts -p opensks-git -p opensks-cli --all-targets -- -D warnings` | Passed | Clippy passed for the push diagnostic contract, SQLite outbox persistence, and CLI status surface. |
| `swift-format lint --recursive swift/Sources swift/Tests` | Blocked | `swift-format` is not installed in this shell (`command not found`); Swift syntax was still verified by the focused Swift test build. |
| `git diff --check` | Passed | No whitespace errors after adding git receipt event projection and Swift live receipt cards. |
| `bash scripts/check-architecture-ownership.sh` | Passed | Architecture budget/path/Swift/provider/patch-engine ownership guards all passed after adding git receipt event projection. |
| `cargo run -p xtask -- architecture-graph --check` | Passed | `architecture-graph: ok` after adding git receipt event projection. |
| `cargo run -p xtask -- direct-io-audit --check` | Passed | `direct-io-audit: ok` after adding git receipt event projection. |
| `cargo test -p opensks-scheduler --locked` | Passed | 19 scheduler tests passed, including the new parallel-dispatch regression proving each admitted worker receives a distinct `worker_context_pack_ref`, dispatch/running events carry that ref, and `context:worker-context-pack` evidence is recorded. |
| `cargo run -p xtask -- schemas` | Passed | Regenerated `schemas/scheduler-work-item.schema.json` after adding backward-compatible `SchedulerWorkItem.worker_context_pack_ref`. |
| `cargo fmt --all --check` | Passed | Rust formatting is clean after adding worker-scoped context dispatch refs. |
| `cargo test -p opensks-contracts --locked` | Passed | 59 contract tests passed after extending `SchedulerWorkItem` with defaulted `worker_context_pack_ref`. |
| `cargo test -p opensks-engine --locked` | Passed | 17 engine tests passed after scheduler work items gained worker-scoped context dispatch refs. |
| `cargo test -p opensks-daemon conversation_turn_start --locked` | Passed | 4 daemon turn-start tests passed, confirming the product path still writes the root context artifact and queues the root work item correctly. |
| `cargo clippy -p opensks-contracts -p opensks-scheduler --all-targets -- -D warnings` | Passed | Clippy passed for the scheduler work-item contract and worker-scoped dispatch handoff. |
| `git diff --check` | Passed | No whitespace errors after adding worker-scoped context dispatch refs and updating the ledger. |
| `bash scripts/check-architecture-ownership.sh` | Passed | Architecture budget/path/Swift/provider/patch-engine ownership guards all passed after adding worker-scoped context dispatch refs. |
| `cargo run -p xtask -- architecture-graph --check` | Passed | `architecture-graph: ok` after adding worker-scoped context dispatch refs. |
| `cargo run -p xtask -- direct-io-audit --check` | Passed | `direct-io-audit: ok` after adding worker-scoped context dispatch refs. |
| `cargo run -p xtask -- schemas` | Passed | Regenerated `schemas/context-pack.schema.json` after adding optional `ContextPackBranchFreshness` / `branch_freshness` metadata to context packs. |
| `cargo test -p opensks-context --locked` | Passed | 5 context tests passed, including a feature-branch fixture proving branch/base freshness metadata, committed paths changed since base, ahead/behind counts, and branch-diff paths feeding worker context selection. |
| `cargo test -p opensks-contracts --locked` | Passed | 59 contract tests passed after adding backward-compatible `ContextPackBranchFreshness` and `ContextPack.branch_freshness`. |
| `cargo test -p opensks-cli context --locked` | Passed | 2 CLI context tests passed after context packs gained branch freshness metadata. |
| `cargo test -p opensks-daemon conversation_turn_start --locked` | Passed | 4 daemon conversation-turn-start tests passed after generated turn context artifacts gained branch freshness metadata. |
| `cargo clippy -p opensks-contracts -p opensks-context -p opensks-cli -p opensks-daemon --all-targets -- -D warnings` | Passed | Clippy passed for the branch freshness contract, Git comparison helper, context pack constructor, and downstream context consumers. |
| `cargo fmt --all --check` | Passed | Rust formatting is clean after adding branch freshness metadata. |
| `git diff --check` plus trailing-whitespace scan on touched ledger/contract/context/schema files | Passed | No whitespace errors after adding branch freshness metadata. |
| `bash scripts/check-architecture-ownership.sh` | Passed | Architecture budget/path/Swift/provider/patch-engine ownership guards all passed after adding branch freshness metadata. |
| `cargo run -p xtask -- architecture-graph --check` | Passed | `architecture-graph: ok` after adding branch freshness metadata. |
| `cargo run -p xtask -- direct-io-audit --check` | Passed | `direct-io-audit: ok` after adding branch freshness metadata. |
| `cargo run -p xtask -- schemas` | Passed | Regenerated schemas after adding generated CodeGraph import/call/test edge refresh behavior; no schema drift was required. |
| `cargo test -p opensks-codegraph --locked` | Passed | 8 CodeGraph tests passed, including coverage that indexing generates `imports`, `calls`, and `tests` edges and that reference lookup surfaces the caller file adjacent to the called helper symbol. |
| `cargo test -p opensks-context --locked` | Passed | 5 context tests passed, including coverage that a changed file calling `helper_dependency` selects the helper symbol through generated CodeGraph call adjacency. |
| `cargo test -p opensks-cli context --locked` | Passed | 2 CLI context tests passed after generated CodeGraph relationship edges began feeding context pack selection. |
| `cargo test -p opensks-daemon conversation_turn_start --locked` | Passed | 4 daemon conversation-turn-start tests passed after generated CodeGraph relationship edges began feeding generated turn context artifacts. |
| `cargo clippy -p opensks-codegraph -p opensks-context -p opensks-cli -p opensks-daemon --all-targets -- -D warnings` | Passed | Clippy passed for generated CodeGraph relationship refresh, context-pack adjacency consumption, and downstream CLI/daemon context consumers. |
| `cargo fmt --all --check` | Passed | Rust formatting is clean after adding generated CodeGraph relationship edges. |
| `git diff --check` plus trailing-whitespace scan on touched ledger/codegraph/context files | Passed | No whitespace errors after adding generated CodeGraph relationship edges. |
| `bash scripts/check-architecture-ownership.sh` | Passed | Architecture budget/path/Swift/provider/patch-engine ownership guards all passed after adding generated CodeGraph relationship edges. |
| `cargo run -p xtask -- architecture-graph --check` | Passed | `architecture-graph: ok` after adding generated CodeGraph relationship edges. |
| `cargo run -p xtask -- direct-io-audit --check` | Passed | `direct-io-audit: ok` after adding generated CodeGraph relationship edges. |
| `cargo run -p xtask -- schemas` | Passed | Regenerated schemas after adding adjacent CodeGraph selection behavior; no new contract field was required for this slice. |
| `cargo test -p opensks-context --locked` | Passed | 5 context tests passed, including coverage that one-hop CodeGraph edge neighbors are included in selected records with `opensks-context:codegraph-adjacent` and edge-kind evidence. |
| `cargo test -p opensks-cli context --locked` | Passed | 2 CLI context tests passed after context packs began selecting adjacent CodeGraph records. |
| `cargo test -p opensks-daemon conversation_turn_start --locked` | Passed | 4 daemon conversation-turn-start tests passed after context packs began selecting adjacent CodeGraph records for generated turn artifacts. |
| `cargo clippy -p opensks-context -p opensks-cli -p opensks-daemon --all-targets -- -D warnings` | Passed | Clippy passed for the adjacent CodeGraph selection helper and downstream context consumers. |
| `cargo fmt --all --check` | Passed | Rust formatting is clean after adding adjacent CodeGraph selection. |
| `git diff --check` plus trailing-whitespace scan on touched ledger/context files | Passed | No whitespace errors after adding adjacent CodeGraph selection. |
| `bash scripts/check-architecture-ownership.sh` | Passed | Architecture budget/path/Swift/provider/patch-engine ownership guards all passed after adding adjacent CodeGraph selection. |
| `cargo run -p xtask -- architecture-graph --check` | Passed | `architecture-graph: ok` after adding adjacent CodeGraph selection. |
| `cargo run -p xtask -- direct-io-audit --check` | Passed | `direct-io-audit: ok` after adding adjacent CodeGraph selection. |
| `cargo run -p xtask -- schemas` | Passed | Regenerated `schemas/context-pack.schema.json` after adding `ContextPackConversationSummary` and defaulted optional `conversation_summary` metadata to context packs. |
| `cargo test -p opensks-context --locked` | Passed | 4 context tests passed, including structured/budgeted conversation summary inclusion and defensive re-redaction of secret-like summary text. |
| `cargo test -p opensks-conversation --locked` | Passed | 21 conversation tests passed after adding typed `ConversationDigest` retrieval with source sequence/generated timestamp while preserving stored-summary redaction. |
| `cargo test -p opensks-daemon conversation_turn_start --locked` | Passed | 4 daemon conversation-turn-start tests passed, including coverage that the generated turn context artifact includes the redacted conversation summary while scheduler events keep only the artifact ref. |
| `cargo test -p opensks-cli context --locked` | Passed | 2 CLI context tests passed after the context-pack schema gained optional conversation summary metadata. |
| `cargo test -p opensks-contracts --locked` | Passed | 59 contract tests passed after adding backward-compatible `ContextPackConversationSummary` and `ContextPack.conversation_summary`. |
| `cargo clippy -p opensks-contracts -p opensks-context -p opensks-conversation -p opensks-daemon -p opensks-cli --all-targets -- -D warnings` | Passed | Clippy passed for the context-pack summary contract, packer helper, conversation digest reader, daemon artifact inclusion, and CLI schema consumers. |
| `cargo fmt --all --check` | Passed | Rust formatting is clean after adding conversation summary inclusion to context packs. |
| `git diff --check` plus trailing-whitespace scan on touched ledger/context/conversation/daemon/contract/schema files | Passed | No whitespace errors after adding conversation summary inclusion to context packs. |
| `bash scripts/check-architecture-ownership.sh` | Passed | Architecture budget/path/Swift/provider/patch-engine ownership guards all passed after adding conversation summary inclusion to context packs. |
| `cargo run -p xtask -- architecture-graph --check` | Passed | `architecture-graph: ok` after adding conversation summary inclusion to context packs. |
| `cargo run -p xtask -- direct-io-audit --check` | Passed | `direct-io-audit: ok` after adding conversation summary inclusion to context packs. |
| `cargo run -p xtask -- schemas` | Passed | Regenerated `schemas/context-pack.schema.json` after adding structured `TurnContextItem` entries and defaulted `turn_context_items` to context packs. |
| `cargo test -p opensks-context --locked` | Passed | 3 context tests passed, including fresh editor-range selected-context resolution, stale captured-hash fail-closed behavior, and secret-like selected text redaction without raw secret text in the pack body. |
| `cargo test -p opensks-daemon conversation_turn_start --locked` | Passed | 4 daemon conversation-turn-start tests passed, including coverage that an `editor://...#fnv1a64:...` selected ref resolves into the generated turn context artifact and scheduler handoff remains artifact-ref only. |
| `cargo test -p opensks-cli context --locked` | Passed | 2 CLI context tests passed after the context-pack schema gained structured selected-context items. |
| `cargo test -p opensks-contracts --locked` | Passed | 59 contract tests passed after adding backward-compatible `TurnContextItem` and `ContextPack.turn_context_items`. |
| `cargo clippy -p opensks-contracts -p opensks-context -p opensks-daemon -p opensks-cli --all-targets -- -D warnings` | Passed | Clippy passed for the selected-context item contract, fail-closed resolver, daemon artifact assertions, and CLI schema consumers. |
| `cargo fmt --all --check` | Passed | Rust formatting is clean after adding structured selected-context items. |
| `git diff --check` plus trailing-whitespace scan on touched ledger/context/daemon/contract files | Passed | No whitespace errors after adding structured selected-context items. |
| `bash scripts/check-architecture-ownership.sh` | Passed | Architecture budget/path/Swift/provider/patch-engine ownership guards all passed after adding structured selected-context items. |
| `cargo run -p xtask -- architecture-graph --check` | Passed | `architecture-graph: ok` after adding structured selected-context items. |
| `cargo run -p xtask -- direct-io-audit --check` | Passed | `direct-io-audit: ok` after adding structured selected-context items. |
| `cargo run -p xtask -- schemas` | Passed | Regenerated `schemas/context-pack.schema.json` after adding `turn_context_refs` for user-selected turn context refs. |
| `cargo test -p opensks-context --locked` | Passed | 2 context tests passed, including coverage that selected editor/symbol refs are preserved in `turn_context_refs` and rendered into the pack body as `Turn Context Refs`. |
| `cargo test -p opensks-daemon conversation_turn_start --locked` | Passed | 4 daemon conversation-turn-start tests passed, including coverage that turn-start-selected context refs are written into the generated context-pack artifact along with freshness metadata. |
| `cargo test -p opensks-cli context --locked` | Passed | 2 CLI context tests passed after the context-pack schema gained `turn_context_refs`. |
| `cargo test -p opensks-contracts --locked` | Passed | 59 contract tests passed after extending `ContextPack` with defaulted turn context refs. |
| `cargo clippy -p opensks-contracts -p opensks-context -p opensks-daemon --all-targets -- -D warnings` | Passed | Clippy passed for the selected-context-ref context packer and daemon turn-start integration. |
| `cargo fmt --all --check` | Passed | Rust formatting is clean after adding `turn_context_refs`. |
| `git diff --check` plus trailing-whitespace scan on touched ledger/context/daemon/contract files | Passed | No whitespace errors after adding `turn_context_refs`. |
| `bash scripts/check-architecture-ownership.sh` | Passed | Architecture budget/path/Swift/provider/patch-engine ownership guards all passed after adding selected context refs to turn context packs. |
| `cargo run -p xtask -- architecture-graph --check` | Passed | `architecture-graph: ok` after adding selected context refs to turn context packs. |
| `cargo run -p xtask -- direct-io-audit --check` | Passed | `direct-io-audit: ok` after adding selected context refs to turn context packs. |
| `cargo run -p xtask -- schemas` | Passed | Regenerated `schemas/scheduler-work-item.schema.json` after adding `context_pack_ref` to scheduler work items. |
| `cargo fmt --all --check` | Passed | Rust formatting is clean after wiring daemon conversation-turn context packs into scheduler work items. |
| `cargo clippy -p opensks-contracts -p opensks-scheduler -p opensks-daemon -p opensks-context --all-targets -- -D warnings` | Passed | Clippy passed for the scheduler work-item contract, scheduler bootstrap, daemon context-pack artifact creation, and context packer dependency path. |
| `cargo test -p opensks-scheduler --locked` | Passed | 18 scheduler tests passed, including the conversation-turn bootstrap regression asserting `context_pack_ref` and `context:turn-context-pack` evidence on the queued root work item/event. |
| `cargo test -p opensks-daemon conversation_turn_start --locked` | Passed | 4 daemon conversation-turn-start tests passed, including coverage that turn start writes a context-pack artifact, queues a root planning work item with an artifact ref, includes freshness metadata, and does not leak the absolute artifact path. |
| `cargo test -p opensks-contracts --locked` | Passed | 59 contract tests passed after extending `SchedulerWorkItem` with a defaulted context-pack artifact ref. |
| `cargo test -p opensks-engine --locked` | Passed | 17 engine tests passed after scheduler work items gained the defaulted context-pack ref. |
| `git diff --check` | Passed | No whitespace errors after wiring context-pack refs into scheduler/daemon. |
| `bash scripts/check-architecture-ownership.sh` | Passed | Architecture budget/path/Swift/provider/patch-engine ownership guards all passed after wiring context-pack refs into scheduler/daemon. |
| `cargo run -p xtask -- architecture-graph --check` | Passed | `architecture-graph: ok` after wiring context-pack refs into scheduler/daemon. |
| `cargo run -p xtask -- direct-io-audit --check` | Passed | `direct-io-audit: ok` after wiring context-pack refs into scheduler/daemon. |
| `cargo run -p xtask -- schemas` | Passed | Regenerated `schemas/context-pack.schema.json` after adding CodeGraph, changed-path, selected-test-target, and freshness metadata to the context-pack contract. |
| `cargo fmt --all --check` | Passed | Rust formatting is clean after the PR-097 context-pack enrichment change. |
| `cargo clippy -p opensks-contracts -p opensks-context -p opensks-cli --all-targets -- -D warnings` | Passed | Clippy passed for the context-pack contract, enriched context packer, and CLI surface. |
| `cargo test -p opensks-context --locked` | Passed | 2 context tests passed, including a git workspace regression that refreshes changed CodeGraph symbols in memory, attaches freshness, records changed paths, and selects the related test target. |
| `cargo test -p opensks-contracts --locked` | Passed | 59 contract tests passed after extending `ContextPack` with backward-compatible defaulted fields. |
| `cargo test -p opensks-cli context --locked` | Passed | 2 CLI context tests passed, including artifact coverage for `codegraph_record_ids` and `freshness`. |
| `cargo test -p opensks-codegraph --locked` | Passed | 7 CodeGraph tests passed for index/query/reference/incremental-update behavior used by context packs. |
| `cargo test -p opensks-intel --locked` | Passed | 10 Intel tests passed for freshness and paged codegraph query behavior used by context packs. |
| `git diff --check` plus trailing-whitespace scan on touched ledger/context/CLI/contract files | Passed | No whitespace errors after the PR-097 context-pack enrichment change. |
| `bash scripts/check-architecture-ownership.sh` | Passed | Architecture budget/path/Swift/provider/patch-engine ownership guards all passed after the context-pack enrichment change. |
| `cargo run -p xtask -- architecture-graph --check` | Passed | `architecture-graph: ok` after the context-pack enrichment change. |
| `cargo run -p xtask -- direct-io-audit --check` | Passed | `direct-io-audit: ok` after the context-pack enrichment change. |
| `cargo fmt --all --check` | Passed | Rust formatting is clean after the PR-096 `run_completed` lifecycle event change. |
| `cargo clippy -p opensks-contracts -p opensks-engine -p opensks-conversation -p opensks-daemon --all-targets -- -D warnings` | Passed | Clippy passed for the contract event kind, engine/conversation reducers, and daemon terminal-event emission. |
| `cargo test -p opensks-contracts --locked` | Passed | 59 contract tests passed, including `run_completed` label/parse roundtrip coverage. |
| `cargo test -p opensks-engine --locked` | Passed | 17 engine tests passed, including projection replay deriving `RunProjectionState::Completed` from `RunCompleted`. |
| `cargo test -p opensks-conversation --locked` | Passed | 21 conversation tests passed, including replay/rebuild deriving completed run projection, stream cursor terminal kind, conversation status, and timeline rows from `run_completed`. |
| `cargo test -p opensks-daemon conversation_supervisor_tick_executes_claimed_local_test_turn --locked` | Passed | Successful supervisor execution appends durable `RunCompleted` with `conversation:run-completed` evidence and cursor-0 subscription replay repairs a stale `failed` projection back to `completed`. |
| `cargo test -p opensks-daemon subscribe_events --locked` | Passed | 3 daemon subscribe-events tests passed after adding `run_completed` and keeping cursor-0 rebuild semantics. |
| `cargo test -p opensks-cli conversation --locked` | Passed | 6 CLI conversation tests passed after the lifecycle contract change. |
| `cargo run -p opensks-contracts --bin schema-gen \| rg -n "run_completed"` | Passed | Schema generator output includes `run_completed`; `schemas/execution-event-envelope.schema.json` is aligned. |
| `git diff --check` | Passed | No whitespace errors after the PR-096 `run_completed` lifecycle event change. |
| `bash scripts/check-architecture-ownership.sh` | Passed | Architecture budget/path/Swift/provider/patch-engine ownership guards all passed after the lifecycle event change. |
| `cargo run -p xtask -- architecture-graph --check` | Passed | `architecture-graph: ok` after the lifecycle event change. |
| `cargo run -p xtask -- direct-io-audit --check` | Passed | `direct-io-audit: ok` after the lifecycle event change. |
| `cargo fmt --all --check` | Passed | Rust formatting is clean after the PR-096 cursor-0 event-journal rebuild projection change. |
| `cargo clippy -p opensks-conversation -p opensks-daemon --all-targets -- -D warnings` | Passed | Clippy passed for the conversation rebuild projector and daemon cursor-0 subscription replay integration. |
| `cargo test -p opensks-conversation --locked` | Passed | 21 conversation tests passed, including new coverage that full replay rebuild recomputes run projection, stream cursor, conversation status, and event-derived timeline rows from queued + events instead of stale projection state. |
| `cargo test -p opensks-daemon subscribe_events --locked` | Passed | 3 daemon subscribe-events tests passed, including cursor-0 conversation replay repairing stale `run_projections` from the event journal. |
| `cargo test -p opensks-cli conversation --locked` | Passed | 6 CLI conversation tests passed after the PR-096 rebuild projection update. |
| `git diff --check` | Passed | No whitespace errors after the PR-096 rebuild projection update. |
| `bash scripts/check-architecture-ownership.sh` | Passed | Architecture budget/path/Swift/provider/patch-engine ownership guards all passed after the PR-096 rebuild projection update. |
| `cargo run -p xtask -- architecture-graph --check` | Passed | `architecture-graph: ok` after the PR-096 rebuild projection update. |
| `cargo run -p xtask -- direct-io-audit --check` | Passed | `direct-io-audit: ok` after the PR-096 rebuild projection update. |
| `cargo fmt --all --check` | Passed | Rust formatting is clean after the PR-096 event-replay conversation-status projection change. |
| `cargo clippy -p opensks-conversation --all-targets -- -D warnings` | Passed | Clippy passed for the conversation repository projection refresh helper and replay-status regressions. |
| `cargo test -p opensks-conversation --locked` | Passed | 20 conversation tests passed, including new coverage that replayed terminal lifecycle events update the conversation summary status and that another active projected run keeps the summary running. |
| `cargo test -p opensks-daemon subscribe_events --locked` | Passed | 3 daemon subscribe-events tests passed after replay began refreshing conversation status from `run_projections`. |
| `cargo test -p opensks-cli conversation --locked` | Passed | 6 CLI conversation tests passed after the PR-096 replay-status projection update. |
| `git diff --check` | Passed | No whitespace errors after the PR-096 replay-status projection update. |
| `bash scripts/check-architecture-ownership.sh` | Passed | Architecture budget/path/Swift/provider/patch-engine ownership guards all passed after the PR-096 replay-status projection update. |
| `cargo run -p xtask -- architecture-graph --check` | Passed | `architecture-graph: ok` after the PR-096 replay-status projection update. |
| `cargo run -p xtask -- direct-io-audit --check` | Passed | `direct-io-audit: ok` after the PR-096 replay-status projection update. |
| `cargo fmt --all --check` | Passed | Rust formatting is clean after the PR-096 event-replay run-state projection change. |
| `cargo clippy -p opensks-conversation --all-targets -- -D warnings` | Passed | Clippy passed for the conversation repository reducer helpers and lifecycle projection tests. |
| `cargo test -p opensks-conversation --locked` | Passed | 19 conversation tests passed, including new coverage that execution-event replay projects run lifecycle state (`running`, `paused`, `failed`), keeps terminal states sticky, and does not let `snapshot_written` overwrite run state. |
| `cargo test -p opensks-daemon subscribe_events --locked` | Passed | 3 daemon subscribe-events tests passed after event replay began updating the run projection state as well as timeline/cursor rows. |
| `cargo test -p opensks-cli conversation --locked` | Passed | 6 CLI conversation tests passed after the PR-096 event-replay projection update. |
| `git diff --check` | Passed | No whitespace errors after the PR-096 event-replay projection update. |
| `bash scripts/check-architecture-ownership.sh` | Passed | Architecture budget/path/Swift/provider/patch-engine ownership guards all passed after the PR-096 event-replay projection update. |
| `cargo run -p xtask -- architecture-graph --check` | Passed | `architecture-graph: ok` after the PR-096 event-replay projection update. |
| `cargo run -p xtask -- direct-io-audit --check` | Passed | `direct-io-audit: ok` after the PR-096 event-replay projection update. |
| `cargo fmt --all --check` | Passed | Rust formatting is clean after the PR-096 accepted-replay projection integrity change. |
| `cargo clippy -p opensks-conversation --all-targets -- -D warnings` | Passed | Clippy passed for the touched conversation repository and its new corruption-path tests. |
| `cargo test -p opensks-conversation --locked` | Passed | 18 conversation tests passed, including new regressions proving idempotent accepted-turn replay fails closed when the run projection row is missing or has an unrecognized state instead of fabricating `queued`. |
| `cargo test -p opensks-daemon conversation_turn_start_idempotency_reuses_accepted_handle --locked` | Passed | Daemon accepted-handle idempotency still reuses the same turn/run/message ids when the projection is valid. |
| `cargo test -p opensks-cli conversation --locked` | Passed | 6 CLI conversation tests passed after the conversation repository projection integrity change. |
| `git diff --check` plus `rg -n "[ \t]+$" docs/implementation/0e9dad0-provider-hyperparallel-ledger.md` | Passed | No whitespace errors in tracked diffs or the untracked implementation ledger after the PR-096 projection integrity update. |
| `bash scripts/check-architecture-ownership.sh` | Passed | Architecture budget/path/Swift/provider/patch-engine ownership guards all passed after the PR-096 change. |
| `cargo run -p xtask -- architecture-graph --check` | Passed | `architecture-graph: ok` after the PR-096 change. |
| `cargo run -p xtask -- direct-io-audit --check` | Passed | `direct-io-audit: ok` after the PR-096 change. |
| `swift test --package-path swift --filter OpenSKSStudioTests.ConversationTurnTests` | Passed | 17 ConversationTurn tests passed after moving Chat run-event observation to a background per-run cursor subscription, preserving live event cards across durable timeline reloads, and verifying terminal shutdown/dedup behavior. |
| `swift test --package-path swift` | Passed | 325 Swift tests passed after adding the background Chat subscription lifecycle coverage and durable-reload/live-event merge behavior. |
| `git diff --check` | Passed | No whitespace errors after the background Swift live subscription wiring and ledger update. |
| `bash scripts/check-architecture-ownership.sh` | Passed | Architecture budget/path/Swift/provider/patch-engine ownership guards all passed after the background Swift Chat subscription wiring. |
| `cargo run -p xtask -- architecture-graph --check` | Passed | `architecture-graph: ok` after the background Swift Chat subscription wiring. |
| `cargo run -p xtask -- direct-io-audit --check` | Passed | `direct-io-audit: ok` after the background Swift Chat subscription wiring. |
| `cargo fmt --all --check` | Passed | Rust formatting remained clean after PR-095 durable timeline projection changes. |
| `git diff --check` | Passed | No whitespace errors after PR-095 durable timeline projection changes and ledger update. |
| `cargo clippy -p opensks-conversation -p opensks-daemon -p opensks-cli --all-targets -- -D warnings` | Passed | Clippy passed for the repository-owned timeline reducer plus daemon/CLI callers. |
| `bash scripts/check-architecture-ownership.sh` | Passed | Architecture budget/path/Swift/provider/patch-engine ownership guards all passed after PR-095 changes. |
| `cargo run -p xtask -- architecture-graph --check` | Passed | `architecture-graph: ok` after PR-095 timeline projection changes. |
| `cargo run -p xtask -- direct-io-audit --check` | Passed | `direct-io-audit: ok` after PR-095 timeline projection changes. |
| `cargo test -p opensks-conversation --locked` | Passed | 16 conversation tests passed after PR-095 timeline projection work, including durable event-journal timeline materialization, replay deduplication, stream cursor advancement, and payload redaction. |
| `cargo test -p opensks-daemon subscribe_events --locked` | Passed | 3 daemon subscription tests passed, including `subscribe_events_materializes_conversation_timeline_projection_once`, which proves subscribe replay updates the conversation timeline once and advances the stream cursor without workspace path leakage. |
| `cargo test -p opensks-cli timeline --locked` | Passed | CLI timeline test passed after moving event replay projection into the repository-owned durable timeline reducer. |
| `cargo test -p opensks-cli conversation --locked` | Passed | 6 CLI conversation tests passed, covering conversation persistence plus turn-start and supervisor command paths after the timeline helper change. |
| `cargo fmt --all --check` | Passed | Rust formatting remained clean after PR-093 parallel dispatch changes. |
| `git diff --check` | Passed | No whitespace errors after PR-093 parallel dispatch changes and ledger update. |
| `cargo clippy -p opensks-scheduler -p opensks-engine -p opensks-cli --all-targets -- -D warnings` | Passed | Clippy passed for the touched scheduler crate plus engine/CLI consumers of the dispatch report. |
| `bash scripts/check-architecture-ownership.sh` | Passed | Architecture budget/path/Swift/provider/patch-engine ownership guards all passed; `src/lib.rs` remains within the configured line budget. |
| `cargo run -p xtask -- architecture-graph --check` | Passed | `architecture-graph: ok` after PR-093 scheduler changes. |
| `cargo run -p xtask -- direct-io-audit --check` | Passed | `direct-io-audit: ok` after PR-093 scheduler changes. |
| `cargo test -p opensks-scheduler --locked` | Passed | 18 scheduler tests passed after PR-093 parallel dispatch work, including an 8-way fixture proving 4 overlapping threaded workers, durable `Running` events before first result, batch evidence receipts, and duplicate batch outcome rejection before completion. |
| `cargo test -p opensks-engine --locked` | Passed | 17 engine tests passed after the scheduler dispatch report gained parallel batch evidence fields and batch execution semantics. |
| `cargo test -p opensks-cli scheduler --locked` | Passed | 5 scheduler CLI tests passed; worker dispatch artifact JSON remains compatible while including additive parallel batch evidence fields. |
| `cargo fmt --all --check` | Passed | Rust formatting check passed after PR-094 integration-candidate apply receipt/final seal work. |
| `cargo clippy -p opensks-adapter -p opensks-daemon -p opensks-cli --all-targets -- -D warnings` | Passed | Clippy passed on the directly touched adapter, daemon, and CLI crates. |
| `cargo clippy -p opensks-conversation -p opensks-scheduler -p opensks-daemon --all-targets -- -D warnings` | Passed | Clippy passed after the conversation-turn scheduler bootstrap bridge. |
| `cargo clippy -p opensks-scheduler -p opensks-daemon --all-targets -- -D warnings` | Passed | Clippy passed after PR-088 scheduler fencing changes. |
| `cargo clippy -p opensks-daemon -p opensks-git --all-targets -- -D warnings` | Passed | Clippy passed after daemon worktree-isolation and integration-candidate receipt updates. |
| `cargo clippy -p opensks-contracts -p opensks-daemon -p opensks-git --all-targets -- -D warnings` | Passed | Clippy passed after adding the contract-backed integration candidate apply request, receipt/repair/final-seal schemas, daemon apply path, persistent approval replay check, git-owned final diff capture, repair item writing, final seal writing, and dirty-target guard. |
| `cargo clippy -p opensks-contracts -p opensks-git-service -p opensks-adapter -p opensks-daemon --all-targets -- -D warnings` | Passed | Clippy passed after the ToolRegistry contract, canonical adapter gateway tools, read-only git status/diff/log executors, registry-backed OpenAI-compatible tool schema generation, and daemon provider-dispatch compatibility check. |
| `cargo clippy -p opensks-contracts -p opensks-codegraph -p opensks-context -p opensks-git-service -p opensks-adapter -p opensks-daemon --all-targets -- -D warnings` | Passed | Clippy passed after adding CodeGraph query/reference executors, the TriWiki-backed context-pack executor, registry availability updates, parser coverage, and daemon compatibility with the expanded available tool registry. |
| `cargo clippy -p opensks-contracts -p opensks-patch-engine -p opensks-adapter -p opensks-cli -p opensks-daemon --all-targets -- -D warnings` | Passed | Clippy passed after PatchEngine typed preflight read errors, read-back verification receipts, capability evidence updates, adapter consumption, CLI capability report evidence, and daemon integration apply compatibility. |
| `cargo test -p opensks-contracts -p opensks-patch-engine -p opensks-adapter -p opensks-cli --locked` | Passed | 59 contracts tests, 11 patch-engine tests, 38 adapter tests with 1 live OpenRouter test ignored, and 90 CLI tests passed. Coverage includes non-UTF8 preflight typed error/no overwrite, read-back verified apply receipts, capability evidence, adapter PatchEngine consumption, and CLI capability report/matrix output. |
| `cargo test -p opensks-daemon integration_candidate_apply --locked` | Passed | 4 focused daemon integration apply tests passed after PatchEngine hardening, covering approval gating, approved main-workspace apply, dirty target refusal, and patch-conflict repair item writing. |
| `cargo test -p opensks-daemon conversation_supervisor_tick_dispatches_provider_registry_route_through_chat_adapter --locked` | Passed | Provider-registry chat dispatch through the adapter tool loop still passes after PatchEngine hardening. |
| `cargo run -p xtask -- capability-matrix --runtime-fixture release` | Passed | Regenerated `docs/runtime-truth-matrix.generated.md`; `agent.code_edit` evidence now includes `patch-engine:typed-preflight-read` and `patch-engine:read-back-verified`. |
| `cargo test -p opensks-graph --locked` | Passed | 6 graph compiler tests passed, including cycle detection, duplicate edge id diagnostics, entry-node validation, disabled-node dependency blocking, and disabled-node pruning from scheduler dependencies. |
| `cargo test -p opensks-engine --locked` | Passed | 17 engine tests passed, including a scheduler-planning boundary test proving cyclic compiled plans return `GraphCompileBlocked` instead of entering the scheduler. |
| `cargo test -p opensks-cli graph --locked` | Passed | 7 CLI graph/codegraph tests passed, including default template writing and compiled plan artifact generation after the stricter DAG compiler checks. |
| `cargo clippy -p opensks-graph -p opensks-engine -p opensks-cli --all-targets -- -D warnings` | Passed | Clippy passed for the stricter DAG compiler, engine scheduler-plan boundary, and CLI graph artifact path. |
| `cargo test -p opensks-contracts -p opensks-codegraph -p opensks-context -p opensks-adapter --locked` | Passed | 59 contracts tests, 7 codegraph tests, 1 context test, and 38 adapter tests passed, with 1 live OpenRouter test ignored; coverage includes registry truth, canonical provider tool schemas, workspace/git/codegraph/context parser mapping, CodeGraph references, and ToolGateway execution for CodeGraph query/references plus TriWiki-backed context packs. |
| `cargo test -p opensks-contracts -p opensks-git-service -p opensks-adapter --locked` | Passed | 59 contracts tests, 30 git-service tests, and 34 adapter tests passed, with 1 live OpenRouter test ignored; coverage includes the canonical ToolRegistry, truthful unavailable descriptors, registry-backed provider tool schemas, canonical workspace/git tool-call parsing, backward-compatible legacy parsing, read-only git log email redaction, and canonical workspace/git gateway behavior. |
| `cargo test -p opensks-provider --locked` | Passed | 12 provider tests passed, including live local `/models` probe, auth rejection classification, repository health update, repository-backed route resolution, and secretless storage. |
| `cargo test -p opensks-cli provider --locked` | Passed | 4 provider CLI tests passed, including `registry-probe` with a mock Keychain command and local OpenAI-compatible model endpoint. |
| `cargo test -p opensks provider --locked` | Passed | 10 root provider facade tests passed. |
| `cargo test -p opensks-conversation --locked` | Passed | 14 conversation tests passed after adding `turn_settings_digest`. |
| `cargo test -p opensks-scheduler --locked` | Passed | 16 scheduler tests passed, including idempotent conversation-turn root work-item bootstrap, fenced worker transitions, stale-token heartbeat rejection, and reacquired-lease stale worker rejection. |
| `cargo test -p opensks-adapter --locked` | Passed | 29 adapter tests passed, 1 live-network test ignored; read-only execution mode blocks agentic workspace writes and provider HTTP diagnostics redact exact bearer tokens. |
| `cargo test -p opensks-cli --locked` | Passed | 90 CLI tests passed, including provider registry, conversation settings, read-only write blocking, and compatibility routing expectations. |
| `cargo test -p opensks-daemon --locked` | Passed | 27 daemon tests passed, including accept-time scheduler root bootstrap, idempotent scheduler replay, registry-backed OpenAI-compatible chat dispatch through a local provider fixture, `Dispatched` route persistence, secret non-disclosure, read-only local-write blocking, worktree-mode writes landing in isolated runtime workspaces instead of the user workspace, isolated edit runs producing durable integration-candidate receipt/patch artifacts, persisted-approval-gated candidate apply to the main git workspace, dirty-target apply refusal, and patch-conflict repair item creation. |
| `cargo test -p opensks-daemon integration_candidate_apply --locked` | Passed | 4 focused daemon tests passed for `integration_candidate_apply`: one waits when approval id is supplied but no approval event is persisted and writes no final seal, one applies an approved candidate and writes `integration.json`, `final.diff`, and `seal.json`, one refuses a dirty main-workspace target without overwriting user content and writes `repair.json` without seal evidence, and one writes `repair.json` for a clean patch conflict without seal evidence. |
| `cargo test -p opensks-contracts -p opensks-daemon --locked` | Passed | 57 contracts tests and 27 daemon tests passed after adding `EngineRequestKind::IntegrationCandidateApply`, `IntegrationApplyReceipt`, `IntegrationRepairItem`, generated schema coverage, daemon apply receipts, and approval-event replay gating. |
| `cargo test -p opensks-git --locked` | Passed | 21 git tests passed, including path-limited working-tree diff capture for final integration evidence. |
| `cargo test -p opensks-contracts -p opensks-daemon -p opensks-git --locked` | Passed | 57 contracts, 27 daemon, and 21 git tests passed after adding final diff evidence capture, repair item creation, and success-only final seal artifacts to integration apply. |
| `cargo test -p opensks-conversation -p opensks-scheduler -p opensks-daemon --locked` | Passed | 14 conversation, 16 scheduler, and 23 daemon tests passed for the scheduler bootstrap, fencing, daemon worktree-isolation, and integration-candidate receipt slice. |
| `cargo test -p opensks-contracts -p opensks-adapter -p opensks-provider -p opensks-image -p opensks-conversation -p opensks-scheduler -p opensks-daemon --locked` | Passed (pre-PR-088) | Prior broad-suite evidence before the scheduler fencing slice: 56 contracts, 29 adapter passed/1 ignored, 12 provider, 3 image, 14 conversation, 13 scheduler, and 23 daemon tests passed. Not rerun after PR-088. |
| `swift test --package-path swift --filter ProviderTests` | Passed | 6 ProviderStore/ProviderService tests passed, including saved-provider probe syncing healthy models and unlocking eligibility. |
| `swift test --package-path swift` | Passed | 322 Swift package tests passed with 0 failures. |
| `cargo run -p xtask -- schemas` | Passed | Generated schemas in `schemas`, including the updated engine request enum, `integration-apply-receipt.schema.json`, `integration-repair-item.schema.json`, `integration-final-seal.schema.json`, `tool-descriptor.schema.json`, `tool-registry.schema.json`, and `git-log.schema.json`. |
| `git diff --check` | Passed | No whitespace errors. |
| `bash scripts/check-architecture-ownership.sh` | Passed | Architecture budget, path hygiene, Swift process-boundary, and adapter transport guards passed after extracting `conversation_turn_tests`; `crates/opensks-cli/src/lib.rs` is 7905 lines under the 8000-line cap. |
| `cargo run -p xtask -- architecture-graph --check` | Passed | `architecture-graph: ok`. |
| `cargo run -p xtask -- direct-io-audit --check` | Passed | `direct-io-audit: ok`. |

## Parallel Audit Lane Findings

| Lane | Status | Key Evidence | Follow-up |
|---|---|---|---|
| Provider contracts/registry/secret store | Completed read-only | `crates/opensks-contracts/src/lib.rs` has lightweight provider/model/routing DTOs only; `crates/opensks-adapter/src/openrouter.rs` is sync/env-oriented; daemon dispatch still needs registry/secret resolution before provider calls. | Add canonical `ProviderConnection`, `SecretRef`, provider repository, async adapter interface, mock OpenAI-compatible adapter, and dispatch-ready route receipts. |
| Swift Provider Center/UI | Completed read-only | `swift/Sources/SharedUI/SettingsWorkspaceView.swift` still advertises provider configuration as later work; `ConversationComposer` model menu is hard-coded to Auto plus existing id; no zero-provider CTA/send blocker. | Add `swift/Sources/Providers/*`, `ProviderStore`, wizard, Keychain secret flow, registry-backed `ModelPicker`, and zero-provider Chat CTA. |
| Async daemon/supervisor/runtime | Completed read-only | Accept returns queued, but Swift `send` still drains supervisor ticks; daemon tick executes one claimed turn inline; runtime uses default `AgenticConfig` rather than persisted settings. | Resident async supervisor, Swift observer-only stream path, settings-to-runtime receipts, long-call heartbeat/cancel propagation. |
| Scheduler/fencing/worktrees/integration | Completed read-only | Chat path bypasses scheduler work-item SSOT; worktree mode passes main workspace to the adapter; leases lack fencing token binding on patch/final writes. | Turn-to-scheduler DAG bridge, monotonic fencing, worktree/snapshot isolation, path leases, IntegrationCoordinator. |
| ToolRegistry/PatchEngine coding tools | Completed read-only | Agent tool loop exposes only read/write/append; OpenRouter tool schemas are hard-coded; PatchEngine foundation has lossy `read_to_string(...).ok()` and lacks fence/worktree/read-back completion. | Registry-owned tool descriptors/executors, canonical provider tool schema generation, command/test/git/context tools, typed PatchEngine preflight/read-back/journal. |
| Streaming/timeline/event SSOT | Completed read-only | Stream primitives exist, but Chat reloads after tick; run lifecycle still has legacy compatibility surfaces even though projections are now authoritative. Swift pipeline projections now preserve unknown run/node states instead of falling back to queued. Commit/push compatibility arrays can still ghost. | Live timeline subscription with replay/dedup, event-journal reducer SSOT, durable commit/push receipt reconciliation. |

## Findings And Work Items

### PR-079 - Baseline Seal And Failing Acceptance Tests

- status: Verified
- owner file/module: `docs/implementation/0e9dad0-provider-hyperparallel-ledger.md`, acceptance tests to be mapped after code scan
- current evidence: baseline gates pass; work order P0/P1 findings still need executable regression coverage. Added regression coverage for daemon registry-backed OpenAI-compatible chat dispatch and extracted one large CLI conversation-turn test into `crates/opensks-cli/src/conversation_turn_tests.rs` to restore architecture line-budget compliance without changing runtime behavior.
- target change: add failing/guarding tests for provider UI absence, registry-backed routing, ignored settings, sequential supervisor behavior, and worktree bypass
- tests: targeted Rust/Swift tests for each acceptance gap
- migration: none
- removal/deletion: none
- final evidence: pending

### PR-080 - Provider/Secret/Model Contracts

- status: In Progress
- owner file/module: `crates/opensks-contracts`, schema generation, Swift DTO fixtures
- current evidence: work order requires canonical `ProviderConnection`, `SecretRef`, `ModelCatalogEntry`, provider route stages, work DAG, and lease/fencing contracts. Added `Requested`, `Resolved`, `DispatchReady`, and `Dispatched` to `RoutingStatus`; added schema-backed `ProviderConnection`, `SecretRef`, `ModelCatalogEntry`, `ProviderProbeReceipt`, and `ProviderMutationReceipt`.
- target change: implement schema-backed provider/model/secret/work contracts and generated Swift fixtures without plaintext secrets
- tests: schema generation, JSON roundtrip, unknown-preserving enum tests, Swift fixture compatibility tests. Focused test evidence: `cargo test -p opensks-contracts -p opensks-provider -p opensks-image -p opensks-daemon --locked` passed after route-stage patch; `cargo test -p opensks-contracts --locked` passed after provider/secret DTO additions; `cargo run -p xtask -- schemas` generated provider schema files.
- migration: schema version update
- removal/deletion: remove any model-id-prefix routing contract ambiguity
- final evidence: route-stage truth and provider/secret/model schema slice verified; repository/storage and generated Swift DTO fixtures pending

### PR-081 - SecretStore And Provider Repository

- status: In Progress
- owner file/module: `crates/opensks-provider`, new `opensks-secrets` or existing vault/security owner, Swift Keychain bridge
- current evidence: current provider path must be audited for env-only assumptions and secret persistence behavior. Added `ProviderRepository` in `crates/opensks-provider` with `.opensks/runtime/providers.sqlite3`, schema `user_version = 1`, secret-ref-only `ProviderConnection` persistence, revision/CAS checks, model catalog sync/enable, provider health updates, and redacted probe receipt storage. Added JSON-only CLI registry commands (`provider registry-list`, `registry-upsert`, `registry-delete`, `registry-set-enabled`, `registry-probe`, `registry-sync-models`, `registry-set-model-enabled`, `registry-record-probe`) and root command dispatch. `registry-probe` resolves the saved `SecretRef` through macOS Keychain, calls an OpenAI-compatible `/models` endpoint with native HTTP, records a redacted `ProviderProbeReceipt`, updates provider health, and syncs returned models without serializing the credential.
- target change: persist provider config with `SecretRef` only, support create/update/delete/revision/CAS, and resolve secrets through macOS Keychain or explicit dev-only secret source
- tests: missing credential, replace/delete lifecycle, no plaintext storage/log/timeline. Current focused tests cover secret-ref-only storage, revision conflict, delete cleanup, model sync/enable with local disabled-state preservation, live `/models` probe over a local HTTP fixture, auth rejection classification without response body leakage, probe receipt storage, root provider command routing, CLI registry round trip, and CLI `registry-probe` keychain-ref-to-model-sync round trip without credential exposure.
- migration: provider registry DB creation/migration
- removal/deletion: env secret as default product path
- final evidence: `cargo test -p opensks-provider --locked` passed with 12 provider tests; `cargo test -p opensks-cli provider --locked` passed with 4 provider CLI tests; `cargo test -p opensks provider --locked` passed with 10 root provider facade tests. macOS Keychain implementation, Swift UI usage, and OpenAI-compatible model-list probe/sync are wired. Daemon provider dispatch now resolves the saved `SecretRef` through macOS Keychain on the product path, with test-memory credentials limited to Rust tests. Non-OpenAI provider protocols and full async dispatch adapters remain pending.

### PR-082 - Provider Center UI

- status: In Progress
- owner file/module: `swift/Sources/SharedUI/SettingsWorkspaceView.swift`, new `swift/Sources/Providers/*`, `swift/Sources/App/AppCoordinator.swift`
- current evidence: work order reports placeholder provider settings; replaced the Settings placeholder with `ProviderCenterView`, added `ProviderStore`, `ProviderModels`, `KeychainSecretStore`, Add Provider wizard, secure credential field, provider/model enable toggles, diagnostics, saved-provider `Test & sync`, and accessibility identifiers. Chat now observes the provider store, blocks Send without a healthy enabled code-capable model, and exposes a zero-provider/add-provider CTA that opens the same provider wizard. `ProviderStore` now binds through `LiveProviderRegistryService` to the Rust provider registry CLI using the same CLI/workspace values as conversations and Git, including `registry-probe`. The Add Provider wizard's `Save & test` action now creates the saved provider/keychain secret first, calls the real saved-provider registry probe for the generated provider id, remembers that id to avoid duplicate providers on retry, and persists trimmed organization/project refs into the provider connection record instead of dropping those wizard fields. The wizard now also uses a `ProviderConnectionFlightGuard` so concurrent Save/Done/Cancel/`Save & test` submissions cannot start while a save/probe operation is already in flight. Pinned text-model selection now carries ordered healthy code-capable fallback model ids from the provider registry instead of discarding `fallbackModelIds`, and Provider Center model catalog rows can be searched by id, remote model id, display name, health, or capability.
- current evidence update: Provider Center now carries registry health snapshot circuit-open, last-checked, and diagnostic-ref fields through `ProviderConnectionViewModel`, renders them in an adaptive detail grid, and keeps the probe fixture aligned with checked/diagnostic metadata.
- target change: add Settings -> Providers, Add Provider wizard, secure credential entry, connection probe, model sync/search, enable/disable, diagnostics, accessibility identifiers, and narrow-window layout
- tests: Swift provider UI/store tests, zero-provider CTA, key-redaction snapshot/log checks. Current evidence: `swift test --package-path swift --filter OpenSKSStudioTests.ProviderTests` passed with 14 tests and 0 failures; `swift test --package-path swift --filter OpenSKSStudioTests.ConversationTurnTests/testSendPassesThreadSettingsAndContextRefs` passed after the model fallback slice. `ProviderTests` cover Keychain secret-ref-only save behavior through an in-memory secret store, live-service refresh from registry state, save-through-service without credential value exposure, saved-provider `connectAndProbe` calling registry probe with the generated provider id, organization/project ref persistence, registry probe syncing healthy live models and unlocking eligibility, model eligibility only after successful probe application, enable/disable behavior, adapter-check blocker redaction/readiness, provider center rendering, non-local HTTP rejection, the wizard flight guard rejecting concurrent save/probe operations, ordered fallback text-model selection, and model catalog search/filter behavior.
- tests update: Added focused coverage for provider health circuit/diagnostic-reference projection plus narrow-width Provider Center render; extended live-probe mock coverage to preserve checked-at and diagnostic-ref metadata after `Test & sync`.
- migration: none
- removal/deletion: provider-coming-later placeholder
- final evidence: Settings Provider Center surface, Add Provider `Save & test` through the saved-provider registry probe, saved-provider detail `Test & sync`, organization/project provider record persistence, Add Provider in-flight double-submission guard, Chat zero-provider/no-ready-model blocker, registry-backed pinned model fallback order, Provider Center model catalog search, provider store eligibility rules, and Swift-to-CLI registry/probe binding verified by Swift tests. Real OpenAI-compatible model sync/probe transport is wired; non-OpenAI provider-specific sync remains pending.

### PR-083 - Canonical Async Provider Adapters

- status: In Progress
- owner file/module: `crates/opensks-adapter`, `crates/opensks-provider`, daemon injection points
- current evidence: work order reports OpenRouter native HTTP foundation with blocking/env-only product assumptions. Added a registry-owned native HTTP OpenAI-compatible `/models` probe and model catalog parser with secret-ref resolution at the CLI boundary. Added repository-backed route resolution before daemon supervisor execution, blocked dispatch on unresolved `Requested` route ids, and added a native OpenAI-compatible chat-completions completer that accepts an in-memory bearer token and redacts exact token echoes from provider diagnostics. The daemon tick path now resolves the registry `SecretRef`, uses the provider endpoint and remote model id, drives the existing model tool-call loop, and records durable failure evidence for unsupported provider kinds or missing credentials. This is still synchronous/blocking, not yet the final async `ProviderAdapter` trait or streaming transport.
- target change: async `ProviderAdapter`, canonical messages/tools/stream, OpenRouter migration, OpenAI-compatible mock adapter, provider-specific wire isolation
- tests: mock provider E2E, tool result protocol, retry/error classification. Current partial tests cover local HTTP model-list success and auth-rejection classification, CLI keychain-ref probe sync, daemon supervisor registry route resolution, and daemon provider chat dispatch through a local OpenAI-compatible fixture that emits a tool call and final answer without leaking the bearer token.
- migration: adapter trait and route receipt schema
- removal/deletion: provider dispatch via model id prefix
- final evidence: partial provider catalog transport, route-resolution, and OpenAI-compatible chat/tool-call dispatch evidence exists; fully async adapters, provider-specific protocol isolation beyond OpenAI-compatible transport, streaming, retry policy, and canonical tool-result protocol remain pending.

### PR-084 - Real Routing And Composer Model Picker

- status: In Progress
- owner file/module: `crates/opensks-provider`, `crates/opensks-conversation`, `swift/Sources/Conversations/Composer/ConversationComposer.swift`
- current evidence: work order reports model menu echoing stored id and `Routed` before registry/health validation. Code audit confirmed `routing_decision_from_turn_settings` marked any non-empty model id as `Routed` and inferred provider id from the prefix before registry validation. Composer now populates model choices from `ProviderStore.eligibleTextModels`, persists the canonical provider model id when pinned, and blocks Send when no healthy enabled code-capable model exists. Backend supervisor now upgrades the accepted turn's `Requested` route to a registry-backed `Resolved` route from `ProviderRepository` immediately before execution, then records `DispatchReady` after provider/model/secret preflight and `Dispatched` on the first provider chat-completion attempt. The legacy CLI compatibility path no longer treats unresolved `Requested` model ids as dispatchable.
- target change: Requested -> Resolved -> DispatchReady -> Dispatched route states, registry-backed composer picker, zero-model blocker, health/capability reasons
- tests: disabled/unhealthy/capability routing, exact provider/model receipt, UI picker reflects registry. Focused tests now assert turn settings record only `Requested`, repository-backed registry route returns `Resolved`, disabled/unhealthy/missing models block, unknown slash-prefixed model ids do not infer provider ids, daemon supervisor persists `Dispatched` after local OpenAI-compatible provider dispatch, and Swift provider eligibility requires a successful probe before a model can unlock Send.
- migration: route receipt version update
- removal/deletion: fake routed state before registry validation; removed `provider_id_from_model_id` prefix inference from unknown explicit model receipts
- final evidence: `cargo test -p opensks-provider --locked`, `cargo test -p opensks-daemon --locked`, `cargo test -p opensks-cli --locked`, and prior Swift provider tests passed for the route-truth slice. `Requested -> Resolved -> DispatchReady -> Dispatched` is now verified on the daemon supervisor path with a local OpenAI-compatible provider fixture. Scheduler/work-item integration, provider semaphores, retry/streaming, and fully async adapter wiring remain pending.

### PR-085 - Thread Settings V2

- status: In Progress
- owner file/module: `crates/opensks-contracts`, `crates/opensks-conversation`, `crates/opensks-engine`, Swift conversation settings
- current evidence: work order reports execution mode, max parallelism, tool policy, pipeline, verifier count, reasoning, image, budgets, and approval policy are not fully consumed. Current change wires persisted `execution_mode` into the agentic runtime config: `read_only` turns write/append tools into denied permissions for the real ToolGateway path, and daemon/CLI local execution fails closed instead of writing when the thread is read-only. The supervisor tick result now includes the effective execution mode.
- target change: revisioned settings snapshot with runtime consumption and receipts for every UI setting
- tests: wire field -> persisted setting -> accepted snapshot -> runtime behavior -> receipt vertical tests. Current partial tests cover `read_only` mode blocking agentic workspace writes, daemon supervisor read-only write refusal with durable event evidence, and CLI compatibility read-only write refusal with timeline replay evidence.
- migration: conversation settings revision/digest migration
- removal/deletion: request default settings as competing SSOT
- final evidence: partial `execution_mode=read_only` behavior verified by `cargo test -p opensks-adapter --locked`, `cargo test -p opensks-daemon --locked`, and `cargo test -p opensks-cli --locked`. The budget fields `token_budget`, `cost_budget_usd`, and `timeout_ms` now round-trip through Rust/Swift durable thread settings, accepted turn settings, daemon turn-start snapshots, scheduler root work-item budget, and scheduler `WorkItemQueued` receipt evidence. Turn-start now also carries optional `thread_settings_updated_at_ms`; Swift product send forwards the durable thread-settings revision, the daemon rejects stale revisions before accepting/idempotency persistence, and default no-settings revision `0` remains accepted for first-run threads. Reasoning effort is now wired for OpenRouter and OpenAI direct dispatch: `ConversationTurnSettings.reasoning_effort` enters `AgenticConfig`, OpenRouter request bodies carry mapped `reasoning.effort`, OpenAI direct request bodies carry scalar `reasoning_effort`, and adapter/daemon/CLI focused tests verify the body shapes. Integration runtime receipts now preserve a sanitized `turn_settings` snapshot across candidate, candidate-selection, apply, and final-seal receipts, including model selection, reasoning, execution mode, pipeline, graph revision, max parallelism, verifier count, tool policy, approval policy, budgets, timeout, and image model id. Focused daemon tests prove the snapshot/evidence survive both awaiting-approval and approved-apply paths without bypassing the existing approval-required gate. Broader all-setting receipt coverage outside the integration chain and live vendor smoke remain unverified.

### PR-086 - Resident Async TurnSupervisor

- status: In Progress
- owner file/module: `crates/opensks-daemon`, `crates/opensks-engine`, Swift conversation store
- current evidence: work order reports Swift-driven supervisor queue draining and one synchronous tick per execution. Current Rust changes improve the synchronous tick path by resolving provider registry routing before execution, rejecting unresolved model requests as dispatch candidates, and dispatching an OpenAI-compatible provider chat/tool loop from registry config when a ready model is pinned. Swift product `load()` no longer has a recovery flag that drains queued/running turns; `drainSupervisorQueue` remains an explicit diagnostic/manual recovery controller path. Daemon streaming stdio now schedules a coalesced resident supervisor drain after accepted `conversation_turn_start` responses, emits durable accepted handles and request terminal markers before execution, drains queued turns under `daemon-resident-turn-supervisor` without explicit Swift ticks, and preserves isolated execution. Overlapping startup/turn-start resident wakeups collapse into one active drain and set a pending request bit for a uniquely identified follow-up drain pass when new work arrives during the active pass. If an explicit stream tick races too early and claims no work, resident drain rescues accepted turns instead of stranding them. Streaming daemon startup also drains existing queued turns from the persisted conversation store without a new `conversation_turn_start`, covering app/daemon relaunch backlog. Each resident drain pass is capped at 8 ticks so a single pass cannot monopolize the daemon; overflow turns remain queued for a later wakeup/pass.
- target change: daemon resident async supervisor, accept-and-return, queue wakeup, Swift observes streams only
- tests: accept returns before execution, app reload resumes durable queue, concurrent runs do not require UI drain. Current partial tests include accept-before-execution behavior, streaming resident drain for multiple live accepted turns without explicit ticks, coalesced rerun past a single drain-pass cap with exactly one claim per accepted run, stream explicit-tick race non-stranding with exactly one claimed execution, startup drain for multiple existing queued turns, bounded-cap overflow preservation, coalescing request-bit behavior when a drain is already active, unique coalesced follow-up request ids, durable queued tick execution, setup-required failure, expired lease recovery, supervisor registry route resolution, and synchronous provider-backed dispatch.
- migration: legacy tick command downgraded to diagnostic/recovery shim
- removal/deletion: Swift product-path queue drain loop
- final evidence: partial synchronous tick hardening plus Swift product-path queue-drain lockdown are verified. `cargo test -p opensks-daemon streaming_conversation_turn_start_coalesced_drain_reruns_past_single_pass_cap --locked -- --test-threads=1`, `cargo test -p opensks-daemon resident_supervisor_coalesced --locked -- --test-threads=1`, `cargo test -p opensks-daemon streaming_ --locked -- --test-threads=1`, `cargo test -p opensks-daemon conversation_turn_start_returns_accepted_handle_without_adapter_execution --locked -- --test-threads=1`, `cargo test -p opensks-daemon conversation_supervisor_tick --locked -- --test-threads=1`, `cargo test -p opensks-daemon subscribe_events --locked -- --test-threads=1`, `cargo clippy -p opensks-daemon --all-targets -- -D warnings`, and `swift test --package-path swift --filter OpenSKSStudioTests.ConversationTurnTests` passed. Remaining gap: this proves coalesced bounded queue-until-idle drain on resident wakeups, not a continuously polling daemon loop or a cross-process worker pool.

### PR-087 - Scheduler Queue/Lease SSOT

- status: In Progress
- owner file/module: `crates/opensks-scheduler`, `crates/opensks-engine`, `crates/opensks-conversation`
- current evidence: work order reports split TurnSupervisor queue and scheduler work item state. Added `bootstrap_conversation_turn_scheduler` in `crates/opensks-scheduler` to convert an accepted conversation turn and its revisioned settings snapshot into a durable root `SchedulerWorkItem` (`WorkKind::Planning`, `WorkState::Ready`) with settings-derived concurrency decision and event-store snapshot. Daemon `conversation_turn_start` now calls this bridge immediately after accept/routing receipt, so product-path accepted turns append an idempotent `WorkItemQueued` event before execution. Existing agent execution events now let the event store assign sequences, preventing collisions with scheduler bootstrap events.
- target change: top-level turns compile into scheduler DAG/work items; event journal and scheduler projection are canonical
- tests: dependency release, retry/backoff, resource governor, event reducer rebuild. Current partial tests prove accept-time root work-item bootstrap, settings-derived max parallelism/verifier concurrency evidence, event-store snapshot write, work-state recovery from replay, and no duplicate `WorkItemQueued` event on idempotent turn-start replay.
- migration: existing runs/turns projection bootstrap
- removal/deletion: duplicate run state mutation helpers
- final evidence: partial bridge verified by `cargo test -p opensks-scheduler --locked`, `cargo test -p opensks-daemon --locked`, and `cargo test -p opensks-conversation -p opensks-scheduler -p opensks-daemon --locked`. Accepted-turn budget settings now enter the scheduler root work-item budget and root `WorkItemQueued` receipt with non-secret budget evidence. Planner DAG expansion, scheduler-owned worker execution, lease/fencing authority for final writes, and duplicate run-state removal remain pending.

### PR-088 - Fencing/Heartbeat/Cancellation

- status: In Progress
- owner file/module: `crates/opensks-scheduler`, `crates/opensks-patch-engine`, `crates/opensks-engine`, `crates/opensks-conversation`, `crates/opensks-daemon`, `crates/opensks-adapter`, `crates/opensks-cli`
- current evidence: work order reports heartbeat APIs without long-call heartbeat and no fencing on writes/finalization. Scheduler now exposes active lease retrieval, token-aware worker transitions, and token-aware heartbeat validation. Lease acquisition now emits a fresh lease id per reacquire using the scheduler transition counter (`lease-{run}-{item}-{counter}`) rather than reusing one item-scoped id. Worker dispatch and deterministic simulation now require the active holder and active lease id for Dispatched, Running, ResultReceived, Verifying, Applying, Completed, and Failed mutations. Completed worker events carry `fencing_token`/`fencing_holder` evidence. Conversation supervisor leases now also carry a monotonic `runs.fencing_token`: claim increments it, heartbeat requires owner+token+running projection, finalization requires the active `TurnSupervisorLease`, and daemon/CLI claimed-lease receipts expose the token. Daemon claimed-turn execution now starts a `TurnLeaseHeartbeatGuard` before scheduler/provider/tool dispatch: it performs an immediate owner+fencing-token heartbeat, fails closed on stale leases, refreshes the lease periodically at TTL/3 capped to 10s while dispatch runs, shares the same `AtomicBool` cancellation token with `AgentRunRequest`, and reports `lease_heartbeat` evidence in the supervisor tick result. Daemon run-control append now immediately projects accepted control events into the conversation timeline/run projection when a run has a conversation anchor, adding `conversation:run-control-projected` evidence for projected control events. The claimed-turn path still checks the durable run-control journal for `RunCancelled` before routing/worktree/provider/tool dispatch as a fallback for raw/unprojected journal events, finalizes the claimed turn as `cancelled` without side effects when cancel is already present, and has the heartbeat guard observe cancellation while dispatch is running so integration/completion can be suppressed. The provider-backed agentic loop now consumes `AgentRunRequest.cancellation_token` before each model/driver step, immediately after a model/driver step returns, before each tool call, and before applying planned writes so cancellation stops later model/tool/apply steps without claiming success. The direct `OpenRouterAdapter::run` path now also honors `AgentRunRequest.cancellation_token` before starting provider HTTP and after a provider response returns, suppressing assistant text and returning `RunProjectionState::Cancelled` if the run was already cancelled.
- target change: monotonic fencing tokens, heartbeat task during provider/tool work, stale write rejection, cancellation propagation
- tests: `cargo test -p opensks-scheduler --locked` passed with 16 scheduler tests, including wrong-token transition rejection, wrong-token heartbeat rejection, reacquired-lease stale worker rejection, and fenced dispatch completion evidence. `cargo test -p opensks-conversation --locked`, `cargo test -p opensks-daemon conversation_supervisor_tick_executes_claimed_local_test_turn --locked -- --test-threads=1`, and `cargo test -p opensks-cli conversation --locked` passed after conversation-supervisor fencing was added. The daemon supervisor tick regression now also asserts claimed `fencing_token = 1`, `lease_heartbeat.started = true`, `initial_heartbeat_ok = true`, matching heartbeat fencing token, and the TTL-derived interval. `conversation_run_control_cancel_projects_before_supervisor_tick` proves normal `run_cancel` immediately projects `RunCancelled` into the durable timeline/projection, prevents a later supervisor tick claim, emits no run-start/run-completed events, creates no execution worktree, and writes no patch. The updated `conversation_supervisor_tick_honors_cancel_before_dispatch` regression proves the fallback path still catches a raw unprojected journal `RunCancelled` before dispatch, skips execution, emits no run-start/run-completed events, creates no execution worktree, writes no patch, and finalizes the projection as `cancelled`. `cargo test -p opensks-adapter loop_stops_before_next_tool_step_when_cancelled --locked` proves the adapter loop stops before a subsequent tool/write step once the shared token is set. `cargo test -p opensks-adapter cancelled_direct_adapter_request_does_not_start_provider_call --locked -- --test-threads=1` proves the direct OpenRouter adapter returns `Cancelled` without credential lookup/network fallback when the run token is already cancelled; `cargo test -p opensks-adapter openrouter --locked -- --test-threads=1`, `cargo test -p opensks-adapter --locked -- --test-threads=1`, `cargo clippy -p opensks-adapter --all-targets -- -D warnings`, `cargo fmt --all --check`, and `git diff --check` passed for that direct-adapter follow-up. Earlier full `cargo test -p opensks-daemon --locked -- --test-threads=1`, focused daemon cancellation regressions, `cargo test -p opensks-cli conversation --locked`, focused clippy, architecture ownership, architecture graph, and direct-IO audit all passed after the adapter/daemon/CLI cancellation-token slice.
- migration: conversation DB `user_version` is now 5 and `runs.fencing_token INTEGER NOT NULL DEFAULT 0` is added idempotently for existing databases; scheduler continues using `Lease.id` as the work-item fencing token.
- removal/deletion: product-path scheduler worker dispatch no longer performs unfenced post-lease state writes; conversation supervisor finalization no longer accepts loose run/owner/token arguments and must receive the active `TurnSupervisorLease`; public compatibility transition API remains for existing non-worker lifecycle/control paths pending broader SSOT migration.
- final evidence: partial scheduler fencing, conversation-supervisor fencing, daemon long-call provider/tool heartbeat guard, immediate conversation run-control cancellation projection, fallback pre-dispatch cancellation propagation, adapter-loop cancellation before later model/tool/apply steps, and direct OpenRouter adapter pre-dispatch/post-return cancellation suppression are verified on local paths. True in-flight provider HTTP abort while a blocking reqwest call is already active, PatchEngine fence E2E across every write path, crash-recovery E2E, and real slow remote-provider heartbeat/cancel smoke remain pending.

### PR-089 - Worktree Isolation

- status: In Progress
- owner file/module: `crates/opensks-git-service`, `crates/opensks-engine`, `crates/opensks-patch-engine`, `crates/opensks-cli`
- current evidence: work order reports agent loop applies PatchEngine to `request.workspace` directly despite worktree setting. Daemon conversation execution now prepares an `opensks-git` isolation for `ExecutionMode::Worktree` before provider-backed or explicit local-test execution, passes the isolated path into `AgentRunRequest`, and keeps conversation/provider/event stores anchored to the real workspace. The run journal records redacted `execution_workspace_prepared` and `integration_candidate_ready` worker events with isolation/candidate metadata but no absolute workspace path. Agent event journal ids now include the worker id so multiple worker lanes can emit sequence `0` without colliding. Isolated applied patches now write `.opensks/runtime/integration-candidates/{run_id}/candidate.json` and `candidate.patch` handoff artifacts, and the assistant message explicitly says main-workspace integration is pending. Successful approval-gated integration apply now writes a schema-backed `cleanup.json` receipt, links it from the apply receipt/final seal/timeline event, and removes the source turn-supervisor/role worktree directories while retaining candidate, patch, final-diff, and seal artifacts. The git lifecycle crate now also exposes schema-backed run isolation inventory and recovery receipts that enumerate `.opensks/runtime/worktrees/{run_id}` via redacted artifact refs, classify git worktree versus snapshot mode, remove all discovered isolations for explicit recovery, and run `git worktree prune` when a repository is available. `opensks worktree inventory <run-id>` now writes the typed inventory receipt to `.opensks/runtime/worktrees/<run-id>/inventory.json`, and `opensks worktree recover <run-id>` writes the pre-recovery inventory plus typed recovery receipt while removing discovered snapshot/git worktree isolations through the same lifecycle functions. CLI run ids are constrained to ASCII letters, numbers, `-`, and `_` before artifact path construction. Scheduler transition events now copy any explicit `SchedulerWorkItem.path_scope` into `work_item_leased`, `Dispatched`, and `Running` event payloads, explicit path-scoped items acquire `LeaseType::PathWrite`, and batch admission serializes overlapping explicit workspace-relative path scopes or any external-write scope while still admitting non-overlapping scoped work in parallel. These events add `scheduler:path-scope-bound` evidence so path-scoped work can be audited before broader crash-recovery enforcement.
- current evidence update: Added a daemon E2E crash-cleanup regression that starts a real conversation turn, runs the supervisor to `candidate_ready`, confirms the resulting `.opensks/runtime/worktrees/{run_id}/turn-supervisor` source isolation exists, injects a secret-like abandoned cleanup fixture into that isolation, then drives the daemon `worktree_recover` request for the same run. The test proves the product recovery path writes redacted inventory/recovery receipts, appends `daemon.worktree_isolation_recovery` event evidence, removes the abandoned turn-supervisor isolation, preserves the candidate receipt, and avoids leaking the workspace path or secret-like value in stdout, receipts, or events.
- current evidence update: The daemon supervisor now also handles the automatic crash-recovery stitch for reclaimed expired leases. Before re-executing a claimed run, it checks for a durable `candidate_ready` integration candidate plus event-journal replay to terminal `completed`; only under that proof does it skip re-execution, run the existing worktree recovery path, refresh the assistant message, finish the fenced lease from projection state, and return `recovered_candidate_ready`.
- target change: per-run/work-item git worktree or non-Git snapshot overlay, path conflict control, integration coordinator handoff
- tests: `cargo test -p opensks-daemon --locked` passed with coverage that both explicit local-test edits and provider-backed OpenAI-compatible tool edits leave the user workspace untouched, write into `.opensks/runtime/worktrees/{run_id}/turn-supervisor`, emit redacted/credential-free candidate events, and write durable candidate receipt/patch artifacts. `cargo test -p opensks-conversation -p opensks-scheduler -p opensks-daemon --locked` and `cargo clippy -p opensks-daemon -p opensks-git --all-targets -- -D warnings` also passed. Cleanup follow-up coverage passed with `cargo test -p opensks-git cleanup_isolation --locked`, `cargo test -p opensks-daemon integration_candidate_apply --locked -- --test-threads=1`, `cargo test -p opensks-contracts integration_cleanup_receipt_roundtrips --locked`, `cargo test -p opensks-contracts integration_candidate --locked`, `cargo test -p opensks-git-service --locked`, and `cargo clippy -p opensks-contracts -p opensks-git -p opensks-git-service -p opensks-daemon --all-targets -- -D warnings`. Inventory/recovery primitive coverage passed with `cargo test -p opensks-contracts worktree_isolation --locked`, `cargo test -p opensks-git isolation --locked`, `cargo run -p xtask -- schemas`, and `cargo clippy -p opensks-contracts -p opensks-git --all-targets -- -D warnings`. CLI invocation coverage passed with `cargo test -p opensks-cli worktree_inventory_writes_redacted_runtime_receipt --locked`, `cargo test -p opensks-cli worktree_recover_writes_receipts_and_removes_snapshot_isolation --locked`, `cargo test -p opensks-cli worktree_usage_preserves_errors --locked`, `cargo test -p opensks-cli worktree --locked`, `cargo clippy -p opensks-cli --all-targets -- -D warnings`, and `cargo fmt --all --check`. The scheduler path-scope follow-up passed `cargo test -p opensks-scheduler path_scoped_work_item_records_path_scope_on_scheduler_lease_events --locked -- --test-threads=1`, `cargo test -p opensks-scheduler worker_dispatch_serializes_overlapping_path_scopes_in_batch_admission --locked -- --test-threads=1`, full `cargo test -p opensks-scheduler --locked -- --test-threads=1` with 24 tests, `cargo clippy -p opensks-scheduler --all-targets -- -D warnings`, `cargo fmt --all --check`, `cargo run -- acceptance audit`, and `git diff --check`.
- tests update: `cargo test -p opensks-daemon conversation_supervisor_candidate_ready_isolation_recovers_after_abandoned_turn --locked -- --test-threads=1`, `cargo test -p opensks-daemon conversation_supervisor --locked -- --test-threads=1`, and `cargo test -p opensks-daemon worktree_isolation --locked -- --test-threads=1` passed after the stitched daemon recovery E2E. `cargo fmt --all --check`, `cargo run -- acceptance audit`, and `git diff --check` also passed.
- tests update: `cargo test -p opensks-daemon conversation_supervisor_tick_recovers_abandoned_candidate_ready_turn_without_reexecuting --locked -- --test-threads=1` passed after the automatic expired-lease recovery hook. The regression proves a stale reclaimed run returns `recovered_candidate_ready`, preserves candidate artifacts, removes the abandoned turn-supervisor isolation, does not modify the main workspace, does not append a second `file_patch_applied` event, and redacts the workspace path plus secret-like fixture from output/events. `cargo clippy -p opensks-daemon --all-targets -- -D warnings` also passed.
- migration: cleanup receipts now have generated `opensks.integration-cleanup-receipt.v1` schema coverage and source-candidate refs now carry optional isolation id/mode metadata. Worktree inventory/recovery receipts now have generated `opensks.worktree-isolation-inventory-receipt.v1` and `opensks.worktree-isolation-recovery-receipt.v1` schema coverage.
- removal/deletion: product-path Worktree-mode daemon execution no longer passes the user workspace to the adapter/PatchEngine write path or reports isolated writes as already integrated into the user workspace; successful approval-gated final integration now removes source worktree/snapshot directories after writing a cleanup receipt, while `ExecutionMode::Local` remains a direct-workspace compatibility mode.
- final evidence: partial daemon execution isolation, candidate handoff, approval-gated main-workspace apply, final diff artifact, source-isolation cleanup receipts/removal, low-level run worktree inventory/recovery receipts, product CLI inventory/recovery invocation, daemon typed inventory/recovery invocation, stitched explicit daemon recovery after a candidate-ready abandoned turn-supervisor isolation, automatic reclaimed expired-lease recovery without re-executing a durable candidate-ready run, integration target base-drift fence, scheduler event-level `path_scope` lease/dispatch observability, `PathWrite` lease typing for explicit scopes, same-batch serialization of overlapping path scopes, and integration-apply event evidence for `path_write_lease` plus `path_scope` on main-workspace mutation are verified. The integration-apply event evidence is proof metadata for the existing approval-gated coordinator path; full path conflict enforcement across every daemon/adapter write path remains pending.

### PR-090 - ToolRegistry And Coding Tools

- status: In Progress
- owner file/module: `crates/opensks-contracts/src/agent.rs`, `crates/opensks-contracts/src/git.rs`, `crates/opensks-git-service/src/lib.rs`, `crates/opensks-adapter/src/agentic.rs`, `crates/opensks-adapter/src/openrouter.rs`
- current evidence: work order requires registry-generated schemas and executors for coding tools beyond read/write/append. Added a canonical `ToolRegistry`/`ToolDescriptor` contract that lists all required work-order tools, including `workspace.list_directory`, `workspace.read_file_range`, `workspace.search_text`, `codegraph.query_symbol`, `codegraph.references`, `context.build_pack`, `workspace.propose_patch`, `workspace.diff_patch`, `command.run`, `test.run_targeted`, `git.status`, `git.diff`, `git.log`, `artifact.read`, `artifact.write`, `image.generate`, `image.inspect`, `mcp.invoke`, and `skill.invoke`. Currently executable workspace, CodeGraph, context-pack, command/test, local MCP broker, local skill registry, read-only git, runtime artifact, and provider-backed image/vision tools are marked `available` in the registry; image/vision descriptors carry route-required reason codes, and the runtime capability matrix keeps `image.generate` / `image.inspect` unavailable until an enabled image/vision model route exists. The OpenAI-compatible provider tool schema is generated from the registry using provider-safe function names, but runtime-gated image tools are advertised only when the daemon has prepared a matching image/vision executor from provider registry routes. Legacy `read_file`/`write_file`/`append_line` parser aliases remain only for fixture/backward compatibility. `git.log` has a typed `opensks.git-log.v1` contract and redacts author email local-parts in the read-only git-service output. `codegraph.query_symbol` and `codegraph.references` now execute through `opensks-codegraph`, and `context.build_pack` now executes through `opensks-context` from TriWiki records. `command.run` and `test.run_targeted` now parse canonical provider tool calls and execute through `ToolGateway` as argv commands, never through a shell; default `Ask` permission blocks execution until explicitly allowed, command strings with shell metacharacters are rejected, allowed programs and targeted-test shapes are allowlisted, absolute/parent path arguments are blocked, process environment inheritance is narrowed to a small build-tool allowlist, timeouts are clamped, and stdout/stderr/argv receipts are bounded and redacted. `mcp.invoke` now parses canonical provider tool calls with MCP-style tool name and JSON arguments, requires explicit `Allow`, rejects unknown/unapproved tools, blocks read-only permission entries as effectful, and currently executes the allowlisted local `opensks.repo.search` tool with bounded/redacted `tools/call` receipts. `skill.invoke` now parses canonical provider tool calls, requires explicit `Allow`, rejects pathlike or missing local skills, blocks read-only permission entries as authority-changing, and loads only project-local `.agents/skills/{skill}/SKILL.md` as bounded/redacted context with a receipt that declares `side_effects = none_instructions_loaded_only`. `artifact.read` and `artifact.write` now parse canonical provider tool calls and execute against `artifact://.opensks/...` refs only; reads reject binary/non-UTF8/path-escape artifacts and writes require explicit `Allow`, redact secret-like lines before persistence, write atomically through a temp file, and return bounded JSON receipts instead of raw filesystem details. `RuntimeCapabilityReport` now embeds the canonical registry as a defaulted `tool_registry` snapshot; report validation checks the registry, `opensks capability matrix` renders a Tool Registry section with availability/permission/reason/evidence rows, and Swift DTOs decode the optional registry while preserving older report compatibility. Settings now keeps the full report, renders a `ToolRegistryStatusView` below runtime capabilities, sorts available tools before disabled tools, shows counts, labels unavailable tools as disabled, dims disabled rows, and exposes per-tool accessibility labels/identifiers from registry truth.
- target change: complete typed executors for workspace/codegraph/context/command/test/git/artifact/image/mcp/skill capabilities with policy metadata, provider schema parity, unavailable capability reporting, and UI disablement.
- tests: `cargo test -p opensks-contracts --locked`, `cargo test -p opensks-adapter tool_definitions --locked`, `cargo test -p opensks-cli capability --locked`, `cargo clippy -p opensks-contracts -p opensks-adapter -p opensks-cli --all-targets -- -D warnings`, `cargo run -p xtask -- schemas`, `cargo run -p xtask -- capability-matrix --runtime-fixture release`, `swift test --package-path swift --filter OpenSKSStudioTests.CapabilityTests`, `swift test --package-path swift --filter OpenSKSStudioTests.ReleaseReadinessTests/testSettingsWorkspaceRendersAndFillsWidth`, and `git diff --check` passed after fixing image/vision registry truth and runtime-gated provider schema advertisement. `swift-format lint swift/Sources/SharedUI/Capability.swift swift/Sources/SharedUI/SettingsWorkspaceView.swift swift/Tests/OpenSKSStudioTests/CapabilityTests.swift` is blocked because `swift-format` is not installed in this shell. `swift test --package-path swift --filter OpenSKSStudioTests.CapabilityTests`, `swift test --package-path swift --filter OpenSKSStudioTests.ReleaseReadinessTests/testSettingsWorkspaceRendersAndFillsWidth`, `swift build --package-path swift`, and `git diff --check` passed after wiring registry-backed tool availability into Settings. `cargo test -p opensks-contracts capability --locked`, `cargo test -p opensks-cli capability --locked`, `cargo test -p opensks-contracts --locked`, `cargo clippy -p opensks-contracts -p opensks-cli --all-targets -- -D warnings`, `cargo fmt --all --check`, and `git diff --check` passed after embedding the registry in runtime capability reports and Swift DTOs. `cargo test -p opensks-adapter skill --locked` passed with canonical skill provider parsing, default approval blocking, local skill context loading, secret redaction, missing-skill handling, and pathlike skill-name rejection. `cargo test -p opensks-adapter mcp --locked` passed with canonical MCP provider parsing, default approval blocking, local `opensks.repo.search` execution, unknown MCP tool rejection, and read-only blocking for effectful command/MCP/skill tools. `cargo test -p opensks-adapter command --locked` passed with canonical command/test provider parsing, default approval blocking, shell metacharacter rejection, allowed argv command execution, and sensitive argument redaction. `cargo test -p opensks-adapter test_run_targeted --locked` passed with targeted-test shape enforcement and a real `cargo test --help` execution receipt. `cargo test -p opensks-adapter --locked` passed with 66 passing tests and one ignored live OpenRouter smoke, including provider schema coverage that `command__run`, `test__run_targeted`, `mcp__invoke`, `skill__invoke`, `artifact__read`, and `artifact__write` are advertised from the registry. Earlier `cargo test -p opensks-contracts -p opensks-codegraph -p opensks-context -p opensks-adapter --locked` passed with registry truth, provider-schema, canonical parsing for workspace/git/codegraph/context tools, legacy parsing, CodeGraph reference coverage, context pack budget coverage, read-only git-service log/status/diff coverage, and gateway behavior coverage for workspace/git/codegraph/context tools. `cargo test -p opensks-daemon conversation_supervisor_tick_dispatches_provider_registry_route_through_chat_adapter --locked` previously passed, proving the existing mock provider E2E still dispatches through the tool loop after registry binding.
- migration: generated `opensks.tool-descriptor.v1`, `opensks.tool-registry.v1`, `opensks.git-log.v1`, and updated `opensks.runtime-capability-report.v1` schemas; regenerated `docs/runtime-truth-matrix.generated.md` with Tool Registry rows.
- removal/deletion: provider-visible hard-coded three-tool schema was replaced by registry-derived available descriptors; legacy tool-call names remain parse-only compatibility aliases and are no longer advertised. Command/test, MCP, skill, artifact, and image/vision tools no longer sit in the registry as `executor_pending`; command/test/MCP/skill/image execution is still approval-gated and route/executor-gated, and artifact writes are still bounded to `.opensks` artifact refs and approval-gated for writes.
- final evidence: partial ToolRegistry, canonical provider schema binding, workspace read/search/patch executors, CodeGraph query/reference executors, TriWiki context-pack executor, command/test argv executors, local MCP broker executor for `opensks.repo.search`, local skill registry context loader, read-only git status/diff/log executors, runtime artifact read/write executors, runtime-gated provider image/vision descriptors, runtime capability report registry embedding, generated Tool Registry matrix rows, Swift registry decode, and Settings registry-backed disabled tool rows verified. Product settings surfaces unavailable tools as disabled from the registry, and image/vision tools are no longer falsely reported as pending; their capability availability still correctly waits for enabled provider routes.

### PR-091 - PatchEngine Commercial Completion

- status: Verified
- owner file/module: `crates/opensks-patch-engine`, capability evidence in `crates/opensks-contracts/src/capability.rs` and `crates/opensks-cli/src/lib.rs`
- current evidence: work order reports typed read/preflight loss and insufficient transaction sequencing. PatchEngine no longer uses lossy `read_to_string(...).ok()` during preflight; UTF-8/permission/IO failures now surface as typed `PatchEngineError::Io { stage: "preflight_read", ... }` instead of being treated as missing files. Successful create/modify/delete/rename operations are read-back verified before the apply receipt reports success; applied receipts carry `patch-engine:pre-apply-revalidated` and `patch-engine:read-back-verified`, and read-back failures roll back the applied operation set with explicit `read_back_mismatch` / `read_back_error` reason codes. Each write is now revalidated immediately before mutation, including rename source hash and destination absence, so a target changed after batch preflight is rejected with `toctou_precondition`; already-applied earlier writes roll back, the concurrent change is preserved, and the transaction journal records the conflict path/reason. Before create/modify temp writes, PatchEngine now scavenges only stale same-directory temp files matching its own `.<target>.<pid>.<stamp>.tmp` pattern for that target; fresh matching temps, malformed names, and other-target temps are preserved. Context-bound applies now reject missing touched-path lease coverage with `path_lease_missing`, bind successful applies to redacted hashed lease/fence journal metadata, and never persist raw fence tokens. Adapter local-test and agentic-loop write paths now supply exact run/proposal-scoped path leases before invoking PatchEngine. `AgentRunRequest` carries an optional scheduler-owned patch lease; when present, adapter/PatchEngine writes use that scheduler lease/fence token instead of the fallback lease, and local-test plus agentic-loop regressions prove the scheduler token is hashed into the journal without leaking raw tokens. Scheduler dispatch has coverage that `WorkerDriver::execute` receives the post-acquisition active lease on the `SchedulerWorkItem`. Daemon claimed conversation-turn execution now reconstructs the accepted turn scheduler root work item, dispatches it through `DurableScheduler::dispatch_ready_batch`, runs provider/local-test/setup policy from a `WorkerDriver`, and injects the active scheduler lease into the cloned adapter request before any PatchEngine write; daemon supervisor regressions prove local-test, provider registry dispatch, provider image tools, setup-required failure, and read-only policy behavior still route through that worker wrapper. A deterministic apply-stage fault-injection regression now proves a mid-transaction IO failure after one file has changed rolls back the prior write, leaves the faulted file unchanged, returns `io_rolled_back`, emits rollback evidence, and journals the rolled-back applied path. Patch journal events now carry `attempt_index` and `attempt_id`, and `PatchEngine::recovery_report` reconstructs repeated proposal attempts, latest terminal state, rollback/operator-review risk, applied paths, and conflicts while tolerating older journal lines without attempt fields. Capability reports and the generated runtime truth matrix now include `adapter:request-patch-lease`, `scheduler:lease-visible-to-worker`, `daemon:turn-scheduler-worker-route`, `patch-engine:typed-preflight-read`, `patch-engine:pre-apply-revalidated`, `patch-engine:path-lease-bound`, `patch-engine:fence-token-bound`, `patch-engine:stale-temp-scavenger`, `patch-engine:rollback-fault-injected`, `patch-engine:attempt-aware-recovery`, and `patch-engine:read-back-verified` evidence.
- target change: worktree/fence binding, path leases, typed read errors, TOCTOU revalidation, read-back hash verify, attempt-aware journal and recovery
- tests: `cargo test -p opensks-patch-engine --locked` passed with 18 tests including non-UTF8 preflight typed-error/no-overwrite coverage, redacted transaction journal coverage, stale preimage rejection, create/modify/delete/rename preconditions, read-back verified apply receipt evidence, deterministic preflight-to-write TOCTOU revalidation/rollback coverage, stale-temp scavenger/fresh-temp preservation coverage, context-bound path lease/fence evidence, raw fence token redaction, unleased-path rejection before writing, deterministic apply-stage IO fault rollback/journal coverage, and repeated-attempt recovery report coverage. `cargo test -p opensks-adapter --locked -- --test-threads=1` passed with 67 passing tests and 1 ignored live OpenRouter smoke, including request-level scheduler patch lease coverage for local-test and agentic-loop writes. `cargo test -p opensks-scheduler --locked` passed with 20 tests, including coverage that worker execution receives the active scheduler lease for downstream patch fencing. `cargo test -p opensks-daemon conversation_supervisor_tick_executes_claimed_local_test_turn --locked -- --test-threads=1` and `cargo test -p opensks-daemon conversation_supervisor_tick --locked -- --test-threads=1` passed after claimed turns moved behind the scheduler worker; the focused local-test regression checks daemon scheduler-worker evidence, adapter request lease evidence, PatchEngine fence evidence, and raw scheduler fence redaction in the isolated patch journal. `cargo test -p opensks-adapter image_generate --locked -- --test-threads=1`, `cargo test -p opensks-adapter loop_reads_then_edits_then_observes_the_change_then_finishes --locked -- --test-threads=1`, `cargo test -p opensks-adapter local_test_adapter_really_edits_a_file_on_disk --locked -- --test-threads=1`, `cargo test -p opensks-adapter local_test_adapter_uses_request_scheduler_patch_lease_without_leaking_raw_fence --locked -- --test-threads=1`, `cargo test -p opensks-scheduler worker_execute_receives_scheduler_lease_for_downstream_patch_fencing --locked`, `cargo test -p opensks-patch-engine injected_apply_io_fault_rolls_back_prior_write_and_journals --locked -- --test-threads=1`, and `cargo test -p opensks-patch-engine recovery_report_tracks_repeated_attempts_and_latest_terminal_state --locked -- --test-threads=1` passed after adapter write paths gained lease-bound PatchEngine apply evidence, request-level scheduler lease binding, rollback fault-injection evidence, attempt-aware recovery evidence, and the stale image allow helper fix. `cargo test -p opensks-contracts capability --locked`, `cargo test -p opensks-cli capability --locked`, `cargo clippy -p opensks-daemon -p opensks-contracts -p opensks-cli --all-targets -- -D warnings`, `cargo clippy -p opensks-patch-engine -p opensks-contracts -p opensks-cli --all-targets -- -D warnings`, `cargo run -p xtask -- schemas`, `cargo run -p xtask -- capability-matrix --runtime-fixture release`, `cargo fmt --all --check`, and `git diff --check` passed after adding rollback fault-injection, attempt-aware recovery, and daemon scheduler-worker route evidence; the adapter-inclusive clippy gate passed in the preceding request-level lease slice where adapter code changed.
- migration: no public contract schema change; generated runtime truth matrix updated for capability evidence. Patch journal schema remains `opensks.patch-transaction-journal.v1`; new journal entries include additive `attempt_index` / `attempt_id` fields, and recovery parsing tolerates older journal lines that omit them.
- removal/deletion: lossy preflight read treatment removed from PatchEngine apply path; stale batch-preflight-only mutation assumptions are no longer accepted because each write must pass immediate pre-apply revalidation before touching disk; stale same-target PatchEngine temp residue is no longer left to accumulate indefinitely before later replacements; context-bound adapter agent writes no longer apply without an exact touched-path lease at the adapter boundary when a request lease is present; rollback claims are no longer based only on ordinary precondition/read-back paths because a deterministic apply fault now proves mid-transaction rollback; repeated proposal attempts are no longer ambiguous in recovery because new journal lines carry attempt identity and the recovery report groups them. Claimed conversation-turn execution no longer calls the provider/local-test adapter workload directly from the supervisor path; it now runs that workload from a scheduler `WorkerDriver` that receives and forwards the active lease. The legacy direct PatchEngine helper remains only for internal compatibility.
- final evidence: typed preflight read errors, pre-apply TOCTOU revalidation/rollback, path lease/fence binding at the adapter/PatchEngine boundary, request-level scheduler lease propagation into PatchEngine journal context, scheduler worker-item lease visibility, daemon-resident scheduler worker dispatch into provider/local-test adapter workload, stale temp scavenging, apply-stage rollback fault injection, attempt-aware recovery report, and read-back hash verification verified. Broader multi-work-item daemon worker-pool/provider semaphore integration remains tracked under PR-093 rather than PR-091.

### PR-092 - Planner/DAG Compiler

- status: In Progress
- owner file/module: `crates/opensks-graph`, `crates/opensks-engine`, `crates/opensks-contracts`
- current evidence: work order requires schema-validated DAG output, repair diagnostics, and verification contracts. The existing `PipelineGraph` -> `CompiledPlan` path now enforces core runtime DAG invariants before scheduler planning: at least one enabled entry node is required; unknown/disabled entry nodes are diagnostics; duplicate edge ids are diagnostics; enabled cycles are diagnosed with `cycle_detected`; enabled nodes cannot depend on disabled node outputs; unreachable enabled nodes are diagnosed; dependency indexes and work templates now omit disabled nodes and disabled-node prerequisites. The compiler now also promotes node config `requirement_ids`, structured `requirements`, and plain `acceptance_criteria` into deterministic proof requirements on `CompletionContract` and per-node `WorkTemplate.requirement_ids`. Malformed requirement/acceptance config produces blocking diagnostics, and the engine boundary forwards compiled requirement ids into scheduler work items after rejecting invalid compiled plans with `GraphCompileBlocked`. `CompiledPlan` now carries a defaulted bounded `repair_plan`: valid plans report `no_repairs_required`, while blocking diagnostics produce deterministic `CompileRepairAction` entries with node/edge targets, action codes, descriptions, evidence requirements, and a three-iteration repair bound. The repair plan also groups actions by repair domain, including entry paths, graph identity/integrity, control-flow conflicts, dependency paths, port-type conflicts, final-seal paths, loop bounds, approval policy, and proof-contract config. `ObjectivePlanRequest` now generates a schema-backed objective-derived `PipelineGraph` plus `ObjectivePlanReceipt`: the generated DAG includes objective input, decomposition, optional research/image lanes, Git worktree isolation, role router, parallel worker pool, verifier pool, approval-gated apply, final seal, proof requirements, and deterministic objective/graph/plan hashes. `opensks graph plan ... <objective>` writes graph/compiled-plan/receipt artifacts, and daemon `run_start` supports `pipeline_id = objective-planner` with `graph_source = objective_planner`.
- target change: planner output schema, DAG compiler, dependency/conflict validation, proof/acceptance contract generation, bounded repair loop
- tests: `cargo test -p opensks-graph --locked` passed with cycle, duplicate-edge, entry-node, disabled dependency, disabled pruning, acceptance-to-proof-contract, malformed-requirement diagnostics coverage, bounded repair-plan coverage, grouped repair coverage, and objective-planner DAG coverage for both integration and read-only/research plans. `cargo test -p opensks-engine --locked` passed with an engine boundary regression for cyclic scheduler plans and a scheduler propagation regression proving explicit/generated requirement ids survive graph compilation into work items. `cargo test -p opensks-cli graph --locked` passed, verifying default template compile plus objective graph/compiled-plan/receipt artifact generation. `cargo test -p opensks-daemon run_start_request --locked -- --test-threads=1` passed, verifying daemon objective-planner run-start output and existing graph-path validation. `cargo metadata --format-version 1 --no-deps`, `cargo test -p opensks-contracts --locked`, `cargo test -p opensks-cli capability --locked`, `cargo run -p xtask -- schemas`, `cargo run -p xtask -- capability-matrix --runtime-fixture release`, `cargo clippy -p opensks-contracts -p opensks-graph -p opensks-engine -p opensks-daemon -p opensks-cli --all-targets -- -D warnings`, `cargo fmt --all --check`, architecture ownership, architecture graph, direct-I/O audit, and `git diff --check` also passed after the proof-contract, repair-plan, repair-group, objective-planner, and CLI extraction slices.
- migration: `CompletionContract` now has an additive defaulted `requirements` array of structured proof requirements; `CompiledPlan` now has an additive defaulted `repair_plan` with `CompileRepairPlan`, `CompileRepairAction`, and `CompileRepairGroup` payloads; `ObjectivePlanRequest` and `ObjectivePlanReceipt` add generated `opensks.objective-plan-request.v1` and `opensks.objective-plan-receipt.v1` schemas; `schemas/compiled-plan.schema.json`, new objective-plan schemas, and the runtime truth matrix were regenerated.
- removal/deletion: disabled nodes are no longer retained as hidden scheduler dependencies; invalid cyclic graphs no longer proceed to scheduler planning; acceptance criteria no longer disappear at compile time because they become durable proof requirements and scheduler work-item requirement ids; compile diagnostics are no longer string-only evidence because every diagnostic now gets a bounded repair action and grouped repair domain for planner/UI follow-up; static-template-only graph planning is no longer the only CLI/daemon path because objective-derived graph generation now writes/run-starts a schema-backed DAG; CLI graph implementation moved out of `crates/opensks-cli/src/lib.rs` to preserve architecture budgets while keeping public wrappers.
- final evidence: schema-backed DAG compiler, proof/acceptance contract generation, scheduler requirement propagation, engine block, bounded repair-plan generation, grouped path/conflict repair domains, objective-derived planner DAG artifact generation, deterministic planner shard-policy metadata, scheduler work-item shard-policy id/selection/count propagation, engine policy validation, daemon objective-planner run-start path, accepted-turn objective-planner scheduler bootstrap, and provider-backed live planner directive ingestion verified. Real vendor planner smoke remains pending; policy-tagged source/verifier-count enforcement is tracked under PR-093/PR-094.

### PR-093 - Parallel Dispatch And Model Subcontracting

- status: In Progress
- owner file/module: `crates/opensks-engine`, `crates/opensks-scheduler`, `crates/opensks-provider`
- current evidence: work order requires true parallel workers, single-model fallback, multi-model role routing, governor evidence. Scheduler worker dispatch now admits an entire governor-capped ready batch, records all admitted items as `Leased -> Dispatched -> Running` with fencing token and deterministic `batch_id`/`batch_size`/`batch_lane_index` payloads before collecting results, then applies outcomes in a second phase. `WorkerDriver` now has an additive `execute_batch` hook; default drivers remain compatible, while worker-pool implementations can execute the admitted batch concurrently. `WorkerDispatchReport` now records `max_parallel_batch_size` and `parallel_batches`, and dispatch snapshots include `scheduler:parallel-batch-dispatch` evidence when overlap was observed. Batch outcome folding rejects duplicate, missing, or unknown work-item outcomes instead of allowing repeated/stale completion writes. `SchedulerWorkItem` now has a defaulted `provider_selector` alongside `model_selector`; conversation-turn root items derive a conservative provider selector from registry-shaped model ids for scheduling only. `SchedulerConfig` carries `provider_max_workers`, `per_provider_max_workers`, and `per_model_max_workers`; dispatch admission skips ready items that would oversubscribe those provider/model semaphores while admitting later independent work. Daemon turn-start and claimed-turn scheduler bootstrap now derive optional `ConversationTurnSchedulerResourceLimits` from the persisted provider registry for pinned models: provider `max_concurrent_requests` controls provider/per-provider caps and model `limits.max_concurrency` controls the per-model cap, all clamped to requested `max_parallelism`. Queued scheduler root events record the effective caps, `resource_limit_source = provider_registry`, and `scheduler:provider-registry-concurrency` evidence. Provider `ModelRegistry::route_roles` now creates a registry-snapshot role allocation plan for planner/code/verifier/arbiter/image/vision/general roles, prefers distinct models when available, reuses one model when necessary, blocks only roles without matching capability, and scores close candidates with role score, health, pricing, and model concurrency. Daemon accepted-turn and claimed-turn scheduler bootstrap now derive a compact role-plan summary from the provider registry, persist it on the durable root `WorkItemQueued` event, and materialize each resolved role assignment as a child `SchedulerWorkItem` with parent/dependency links to the root, role-specific work kind/capability requirements, selected provider/model selectors, `conversation.role_plan` `WorkItemQueued` events, and single-model reuse evidence. Effective `objective-planner` turns now also derive the objective DAG from the accepted user message, compile it through the engine scheduler planner, write durable graph/compiled-plan/receipt artifacts under `.opensks/runtime/objective-plans/{run_id}/`, and persist each compiled graph work item as a root-dependent `conversation.objective_plan` scheduler child with objective graph/source/hash/count metadata, artifact refs, context/model selectors, `scheduler:objective-plan-turn-bootstrap`, and `daemon:objective-plan-artifact` evidence. Objective-generated graphs now add a planner/DAG path that encodes role router, worker pool, verifier pool, and approval-gated integration nodes from an objective before scheduler planning, and objective receipts/compiled plans/work templates now carry a deterministic `opensks.planner-shard-policy.v1` with implementation/verifier shard counts, required gates, candidate-selection policy, and `planner:shard-policy` evidence. Engine graph planning validates objective planner shard-policy consistency and copies the compiled shard-policy id, selection policy, required source count, and required verifier count onto generated `SchedulerWorkItem`s. After the root adapter/tool-loop turn completes, the daemon now drains ready role children through scheduler dependency release and `dispatch_until_idle`; admitted role-only batches run through scoped parallel worker threads. Non-code advisory roles still perform bounded no-tool OpenAI-compatible chat subcalls with redacted response hash/byte metadata and batch lane metadata. Verification role workers now persist a schema-backed semantic verifier judgment receipt under `.opensks/runtime/semantic-verifiers/{run_id}/{work_item_id}/judgment.json`; receipts store only redacted verdict/gate metadata, provider/model ids, response hash, and byte count, not raw model text, credentials, or workspace paths. The Code role now runs a role-scoped agentic tool loop in its own isolation, rewrites nested agent events to a role-specific worker id, writes a schema-backed role candidate patch and receipt under `.opensks/runtime/role-candidates/{run_id}/{work_item_id}/`, copies policy-tagged shard selection/count metadata from the scheduler item into the role candidate receipt, and leaves the main workspace untouched. The integration coordinator now aggregates Code-role candidates with the root turn-supervisor candidate into a schema-backed `opensks.integration-candidate.v1` receipt, unified aggregate patch, and `opensks.integration-candidate-selection-receipt.v1` receipt that records the selected candidate pool, policy-tagged selected/required source counts, required verifier count, and selection evidence when a real planner policy is present. Candidate and selection receipts now carry planned verifier counts from the immutable turn settings snapshot or planner policy, and integration apply executes bounded read-only git-apply verifier lanes plus any persisted semantic verifier judgment lanes while failing closed before approval/apply when policy-tagged required shards are missing, the policy-required verifier count exceeds the runtime lane cap, or a semantic verifier verdict is missing/failing. Role worker payloads stay credential/path/content redacted. Running/result events and worker reports carry `scheduler:provider-model-semaphore` evidence for selector-bound work, and `agent.parallel_build` capability evidence plus the generated runtime truth matrix expose scheduler semaphore, daemon registry-derived cap, provider role-routing, daemon role-worker parallel batches, provider-backed model subcalls, Code-role candidate subcontract evidence, aggregate integration-candidate evidence, candidate-selection receipt evidence, read-only verifier-lane evidence, semantic verifier gate evidence, planner shard-policy metadata/enforcement, objective-plan artifact evidence, and health/cost/concurrency scoring proofs.
- target change: role router, worker pool, provider/model semaphores, candidate/shard/verifier patterns, overlap receipts
- current evidence update: Accepted objective-planner turns now optionally route the Planning role through the provider registry, call an OpenAI-compatible planner model for a compact JSON directive, clamp the directive into the existing `ObjectivePlanRequest`, and persist only provider/model id plus response hash/byte count in `ObjectivePlanReceipt`; raw planner text is not written to objective artifacts. Live model requests for optional image/research lanes are clamped off with `daemon:objective-plan-optional-lanes-clamped` evidence until claimed-turn executors exist for those lanes. Claimed-turn dispatch rehydrates a dispatchable objective-plan subset, executes local objective control nodes, maps objective `workers` through the existing Code-role subcontract path, maps objective `verifier` through the semantic-verifier path, materializes the integration candidate before objective `apply`, runs objective `apply` through the existing approval-gated integration coordinator, and lets objective `seal` observe the durable final seal or pending approval state. Objective-planner mode now suppresses the duplicate regular role-plan children so objective DAG workers are the policy-tagged source of truth for apply/seal. Runtime evidence includes `daemon:objective-plan-live-model-planner`, `daemon:objective-plan-child-runtime`, `daemon:objective-plan-apply-runtime`, and `daemon:objective-plan-seal-runtime`.
- tests: `cargo test -p opensks-scheduler --locked` passed with an 8-way threaded worker-pool fixture proving admitted concurrency cap 4, at least 4 overlapping worker executions, four `to=Running` events in the first batch before the first `ResultReceived`, `max_parallel_batch_size = 4`, `parallel_batches = 2`, and zero duplicate outcome ids across 8 completions. A negative duplicate-outcome worker test proves a repeated outcome is rejected with no `WorkItemCompleted` event. The focused `worker_dispatch_respects_provider_and_model_semaphores` regression proves `per_provider_max_workers = 2` and `per_model_max_workers = 1` admit independent provider/model work, leave oversubscribed items ready, and emit semaphore evidence. `cargo test -p opensks-scheduler conversation_turn_objective_plan_queues_compiled_work_items_idempotently --locked` now proves objective-plan children are root-dependent, inherit context/model selectors, carry objective evidence, emit `conversation.objective_plan` queued events, and replay idempotently. `cargo test -p opensks-daemon conversation_turn_start_objective_planner_bootstraps_compiled_work_dag --locked -- --test-threads=1` proves persisted objective-planner thread settings cause accepted turns to derive and queue compiled objective DAG work items with graph/source/hash/count metadata and no workspace-path leak. `cargo test -p opensks-daemon conversation_supervisor_tick_dispatches_provider_registry_route_through_chat_adapter --locked -- --test-threads=1` proves a provider-backed conversation turn requesting `max_parallelism = 8` is bootstrapped with registry-derived effective caps of 2 from provider/model metadata, durable `scheduler:provider-registry-concurrency` evidence, a provider-derived hyperparallel role plan, 4 child role work items with parent/dependency wiring, 4 completed scheduler role-child records, 4 timeline role-worker completion observations, provider-backed role model subcalls with `daemon:role-worker-model-call` evidence and response hashes, scoped parallel role batches with `daemon:role-worker-parallel-batch` evidence, observed provider-side overlap of at least 2 concurrent role requests under the per-model cap, a verification-role semantic judgment artifact with credential/path/raw-response redaction and a parsed `pass` verdict, a Code-role tool-loop patch candidate under `.opensks/runtime/role-candidates/`, role-specific `file_patch_applied` timeline evidence for `ROLE_CODE_NOTE.md`, no main-workspace role file, and no credential/workspace path leak. `cargo test -p opensks-contracts semantic_verifier_judgment_receipt_roundtrips --locked` proves the judgment receipt contract round-trips verdict/gate fields while preserving redaction flags/provider-model metadata. Planner-shard metadata, policy-tagged enforcement, and semantic verifier hard gating are verified by `cargo test -p opensks-contracts objective_plan_request_and_receipt_roundtrip --locked`, `cargo test -p opensks-contracts integration_candidate --locked`, `cargo test -p opensks-graph objective_planner_generates_valid_parallel_integration_dag --locked`, `cargo test -p opensks-engine engine_carries_objective_shard_policy_into_scheduler_items --locked`, `cargo test -p opensks-daemon integration_candidate_apply --locked -- --test-threads=1`, `cargo test -p opensks-cli graph --locked`, `cargo test -p opensks-contracts capability --locked`, `cargo test -p opensks-cli capability --locked`, `cargo test -p opensks-scheduler conversation_turn_bootstrap_records_scheduler_root_item_idempotently --locked -- --test-threads=1`, `cargo run -p xtask -- schemas`, `cargo run -p xtask -- capability-matrix --runtime-fixture release`, and `cargo clippy -p opensks-contracts -p opensks-graph -p opensks-engine -p opensks-scheduler -p opensks-daemon -p opensks-cli --all-targets -- -D warnings`. `cargo test -p opensks-daemon conversation_supervisor_tick --locked -- --test-threads=1` proves the provider image/vision E2E path still completes 4 post-root role subcalls after its root chat/image/vision tool loop. Provider role routing is covered by `cargo test -p opensks-provider role_routing --locked -- --test-threads=1`, `cargo test -p opensks-provider close_role_scores_consider_cost_and_model_concurrency --locked -- --test-threads=1`, and full `cargo test -p opensks-provider --locked -- --test-threads=1` with local mock-server binding approval. `cargo test -p opensks-daemon --locked -- --test-threads=1`, `cargo test -p opensks-scheduler conversation_turn_bootstrap_records_scheduler_root_item_idempotently --locked -- --test-threads=1`, `cargo test -p opensks-engine --locked`, `cargo test -p opensks-cli scheduler --locked`, `cargo test -p opensks-contracts capability --locked`, `cargo test -p opensks-cli capability --locked`, `cargo run -p xtask -- capability-matrix --runtime-fixture release`, `cargo clippy -p opensks-daemon -p opensks-scheduler -p opensks-provider -p opensks-contracts -p opensks-cli --all-targets -- -D warnings`, `cargo run -p xtask -- schemas`, `cargo fmt --all --check`, and `git diff --check` also passed across the scheduler parallel/resource-semaphore/registry-cap/provider-role-router/scheduler-role-plan receipt/role-child-work-item/role-child-execution/model-subcall/product-overlap/code-candidate slices.
- tests update: `cargo test -p opensks-daemon live_objective_planner_directive_clamps_to_dispatchable_runtime_lanes --locked` proves live planner numeric directives are clamped to thread max parallelism, require worktree/integration approval, and force optional image/research lanes off with explicit evidence until their claimed-turn executors exist. `cargo test -p opensks-daemon conversation_turn_start_objective_planner --locked -- --test-threads=1` proves both deterministic objective-plan bootstrap and provider-backed live planner directive ingestion, including receipt source `model_authored_objective_planner`, provider/model metadata, response hash/byte count, no credential leak, and no raw objective/model text in the objective receipt. `cargo test -p opensks-daemon conversation_supervisor_tick_executes_objective_plan_worker_and_verifier_children --locked -- --test-threads=1` proves claimed-turn objective-plan worker/verifier/apply/seal children complete through the role subcontract, semantic-verifier, integration-apply, and final-seal observation paths while preserving no-main-workspace-mutation isolation when approval is absent. `cargo test -p opensks-daemon conversation_supervisor_tick_executes_objective_plan_approved_apply_and_seal_children --locked -- --test-threads=1` proves a persisted integration approval lets objective `apply` mutate tracked main-workspace targets through the existing integration coordinator and objective `seal` observes `seal.json`.
- migration: scheduler work-item schema now includes defaulted `provider_selector`, `context_pack_ref`, `worker_context_pack_ref`, `shard_policy_id`, `shard_policy_selection_policy`, `shard_policy_required_source_count`, and `shard_policy_required_verifier_count`; objective-plan receipts, compiled plans, and work templates now include defaulted planner-shard policy metadata/counts; objective-plan receipts now also include additive optional live planner provider/model id and response hash/byte metadata; scheduler queued-event receipts carry effective provider/model worker caps, `resource_limit_source`, `role_plan_source`, a compact role-plan summary, and child role work-item queued events under `conversation.role_plan`; role subcontract candidates now have generated `opensks.role-subcontract-candidate.v1` schema with planner policy selection/count fields; semantic verifier judgments now have generated `opensks.semantic-verifier-judgment.v1` schema; planner shard policies now have generated `opensks.planner-shard-policy.v1` schema; aggregate integration candidates now have generated `opensks.integration-candidate.v1` schema with source-candidate refs and planner policy selected/required counts; candidate selections now have generated `opensks.integration-candidate-selection-receipt.v1` schema and additive candidate `selection_ref` plus planner policy selected/required counts; integration verification receipts now have defaulted planned/passed/failed verifier counts and per-lane verifier receipts; capability matrix now includes provider/model semaphore, provider-registry concurrency, provider role-routing, daemon live objective planner, daemon role-worker parallel/model-call/code-candidate evidence, aggregate integration-candidate evidence, candidate-selection receipt evidence, read-only verifier-lane evidence, planner shard-policy evidence, and health/cost/concurrency scoring evidence.
- removal/deletion: cosmetic `maxParallelism` is less cosmetic at the scheduler layer because it now participates in real batch admission alongside provider/model semaphore limits derived from registry metadata; multi-model role selection is no longer limited to a single chat/image/vision route because the provider registry now has a planner-facing role allocation primitive, and role assignments are no longer only opaque root-event metadata because they now become durable scheduler child work items that the daemon drains after root completion. Product Code role children now produce isolated role candidate patches, those role candidates are no longer stranded outside the integration receipt, and aggregate source-candidate selection is no longer implicit in list order because the coordinator writes a selection receipt.
- final evidence: scheduler dispatch overlap, daemon product role-worker overlap under provider/model semaphores, duplicate-outcome rejection, scoped worker context refs, provider/model semaphore admission, daemon provider-registry-derived scheduler concurrency caps, provider/model role-router allocation, live provider-backed objective planner directive ingestion, durable scheduler role-plan receipts, queued and completed role child work items, objective-generated planner/DAG role-router/worker/verifier/integration graph output, artifact-backed accepted-turn objective-plan child work-item queueing, deterministic planner shard-policy metadata with scheduler-item propagation and engine validation, single-model reuse fallback, capability-specific role blocking, role worker timeline events, no-tool advisory role model subcalls with redacted response metadata, verification-role semantic judgment artifacts with verdict/gate metadata, isolated Code-role candidate patch production with policy metadata when present, aggregate root-plus-role candidate receipt generation, deterministic/policy-tagged candidate-selection receipt generation, bounded read-only verifier-lane execution from turn settings or planner-required counts, integration semantic-verifier hard gating, aggregate approval-gated final apply/seal, fail-closed missing-shard/verifier-cap/semantic-verifier checks, and cost/health/concurrency scoring verified. Live vendor credential smoke and native release proof remain pending.

### PR-094 - Integration Coordinator

- status: In Progress
- owner file/module: `crates/opensks-daemon`, `crates/opensks-git-service`, `crates/opensks-patch-engine`
- current evidence: work order requires candidate patch selection, coordinator worktree apply, conflict repair, aggregate verification, final seal. Isolated daemon executions that produce applied patches now emit a durable schema-backed candidate receipt and unified diff under `.opensks/runtime/integration-candidates/{run_id}/`, emit a redacted `integration_candidate_ready` worker event with aggregate/source counts, and return an assistant message that the main workspace was not modified and integration is pending verification/approval. The candidate writer now scans `.opensks/runtime/role-candidates/{run_id}/`, loads schema-backed Code-role subcontract candidate receipts, and folds root plus role patches into one aggregate `candidate.patch` with `source_candidates`, aggregate counts, and redacted artifact refs. It now also writes `.opensks/runtime/integration-candidates/{run_id}/selection.json` as an `opensks.integration-candidate-selection-receipt.v1` receipt, links it from `candidate.json` via `selection_ref`, records the selected aggregate candidate, source-candidate pool, selected source ids, deterministic selection policy, required verification gates, planned verifier count from the turn settings snapshot, and redacted selection evidence, and exposes `integration_selection_ref` plus `integration_planned_verifier_count` in supervisor/run-completed metadata. A contract-backed `integration_candidate_apply` daemon request now loads that typed candidate by `run_id`, rejects invalid/secret-looking targets, writes a pre-approval `.opensks/runtime/integration-candidates/{run_id}/verification.json` receipt from bounded read-only `opensks_git::check_unified_diff_apply` verifier lanes, writes per-lane verifier state/reason/gates, writes `repair.json` for dirty-target and patch-conflict verification failures, then requires a persisted `ApprovalApproved` event with `approval-integration-{run_id}` and `scope: integration_apply` before applying the unified diff to the main git workspace through `opensks-git::apply_unified_diff_with_rollback`. Successful applies capture a path-limited main-workspace `git diff` through `opensks-git::working_tree_diff`, write `integration.json`, `final.diff`, success-only `seal.json`, and success-only `cleanup.json`, link `verification_ref`, `seal_ref`, and `cleanup_ref` through apply/final-seal receipts and timeline events, and remove source worktree/snapshot directories after retaining the candidate/patch/final-diff/seal artifacts.
- target change: IntegrationCoordinator state machine and durable receipts, including aggregate candidate selection, multi-candidate verifier execution, persistent approval matching, conflict repair, and final main-workspace apply receipt. Planner-authored shard policy now binds policy-tagged source/verifier counts into candidate and selection receipts and blocks apply when required policy-tagged shards are missing or verifier counts exceed the runtime cap. Model-authored verification-role judgments now carry redacted verdict/gate metadata and integration apply treats any missing/failing semantic verdict as a hard verification failure before approval-gated main-workspace apply.
- tests: local-test and provider-backed daemon tests assert candidate JSON/patch artifacts, candidate refs in tick output, no main-workspace file writes, no credential or absolute workspace path leakage in events, and pending-integration assistant copy. The single-source daemon path now asserts `integration_selection_ref`, candidate `selection_ref`, and `selection.json` with the selected turn-supervisor source id plus verification gates. The provider-backed daemon route now asserts the aggregate candidate receipt contains both the root `ROUTED_NOTE.md` source and the Code-role `ROLE_CODE_NOTE.md` subcontract source, including `source_candidates`, `aggregate_candidate_count = 2`, `aggregate_target_count = 2`, an aggregate patch containing both edits, and a selection receipt whose selected ids match the candidate pool. Additional daemon tests assert apply verifies before approval, awaiting-approval receipts carry `verifier_passed=true` without writing `seal.json`, approved single-candidate apply mutates the main git workspace and writes linked `verification.json`, `integration.json`, `final.diff`, `seal.json`, and `cleanup.json`, approved aggregate apply mutates two main-workspace targets and writes a multi-file `final.diff` plus final seal target coverage and two-source cleanup coverage, aggregate apply records three passed verifier lanes from a planned verifier count of three, policy-tagged missing required shards fail with `planner_required_shards_missing` before apply/seal, policy-required verifier counts above the runtime cap fail with `planner_required_verifier_count_exceeds_runtime_cap`, passing semantic verifier judgments add model-semantic verifier lanes and allow approved apply, failed semantic verifier judgments fail with `semantic_verifier_rejected` before main-workspace mutation/final diff/seal/cleanup, dirty main-workspace targets are refused during verification without overwriting user content and write failed verifier-lane evidence plus `repair.json` without seal evidence, and clean patch conflicts write verification-linked `repair.json` without final diff or seal evidence. Git tests assert final diff capture is path-limited, the check helper remains non-mutating, and `cleanup_isolation` removes snapshot directories plus real git worktrees. Current cleanup/policy/semantic-specific verification: `cargo test -p opensks-git cleanup_isolation --locked`, `cargo test -p opensks-daemon integration_candidate_apply --locked -- --test-threads=1`, `cargo test -p opensks-daemon --locked -- --test-threads=1`, `cargo test -p opensks-contracts --locked`, `cargo test -p opensks-git-service --locked`, and focused clippy all passed.
- migration: generated `opensks.role-subcontract-candidate.v1`, `opensks.semantic-verifier-judgment.v1`, `opensks.integration-candidate.v1`, `opensks.integration-candidate-selection-receipt.v1`, `opensks.integration-verification-receipt.v1`, `opensks.integration-apply-receipt.v1`, `opensks.integration-repair-item.v1`, `opensks.integration-final-seal.v1`, and `opensks.integration-cleanup-receipt.v1` contract schemas now cover role candidates, semantic verifier judgments, aggregate integration candidates, selection receipts, verification receipts, final apply receipts, repair items, success-only final seal artifacts, and success-only source-isolation cleanup receipts. `SemanticVerifierJudgmentReceipt` gained additive/defaulted `verdict`, `passed_gates`, and `failed_gates`; `IntegrationCandidateReceipt` gained additive optional `selection_ref` plus defaulted `planned_verifier_count`, planner policy id/selection policy, required/selected source count, and required verifier count fields; `IntegrationSourceCandidateRef` gained additive optional source isolation id/mode fields plus planner policy count fields; selection and verification receipts also gained defaulted verifier-count fields, and verification receipts gained defaulted `verifier_lanes`.
- removal/deletion: isolated applied proposals are no longer reported as already integrated into the main workspace; role subcontract candidates are no longer stranded as timeline-only artifacts; aggregate candidate selection is no longer only implied by the source-candidate vector; successful explicit apply no longer leaves source worktree/snapshot directories behind after final seal; automatic final apply remains unavailable, while explicit candidate apply is available only through the approval-event-gated coordinator request.
- final evidence: typed candidate handoff, role-candidate-to-integration aggregation, aggregate candidate source refs, deterministic/policy-tagged candidate-selection receipt, planned verifier count propagation from turn settings or planner policy, bounded read-only verifier-lane receipts, semantic verifier judgment gate lanes, explicit pre-approval verification receipt, fail-closed planner missing-shard/verifier-cap/semantic-verifier checks, approval-event-gated single and aggregate candidate apply, durable final diff artifact, dirty-target and committed target-base-drift repair item creation, verification-linked apply/final-seal/cleanup receipts, final seal coverage for multi-target aggregate patches, and source worktree cleanup verified. Remaining integration risk is live objective-planner orchestration proving this path through fully remote multi-session runtime, not the local coordinator gate itself.

### PR-095 - Live Stream/Timeline

- status: In Progress
- owner file/module: `crates/opensks-stream`, `crates/opensks-conversation`, Swift chat/timeline
- current evidence: work order reports reload-after-tick instead of real-time deltas. `opensks-stream` already has explicit frame cursor/dedup primitives, and the conversation DB already has `timeline_items` and `stream_cursors`; the missing product path was a durable event-journal-to-timeline reducer plus a framed subscribe wire path. Added repository-owned `project_execution_events_into_timeline`, `TimelineProjectionReport`, `stream_cursor_for_run`, and `stream_metadata_for_run` in `crates/opensks-conversation`. The reducer materializes `ExecutionEventEnvelope` replay into stable `timeline-event-{event.id}` rows, redacts JSON payload strings, advances `stream_cursors.last_sequence`, updates `run_projections.last_sequence`, and treats replayed rows as duplicates instead of appending them again. The CLI timeline helper now calls this repository reducer and reads the durable timeline. Daemon `SubscribeEvents` now projects replayed/tail events into the same conversation timeline when the run belongs to a conversation, without creating a conversation DB for non-chat engine runs. The same subscription response now also emits explicit v2 `stream_opened`, framed `event`, and `stream_completed`/`stream_failed` lines while preserving the raw legacy `ExecutionEventEnvelope` lines. Conversation-backed subscriptions reuse the accepted turn stream id/project/conversation metadata; generic engine runs use a deterministic fallback event stream id. `EventStore::last_sequence` now exposes the committed cursor for reconnect validation, and daemon subscription requests whose `since_sequence` is beyond the durable cursor emit a redacted `subscription_cursor_gap` engine error plus a resumable `stream_failed` frame with `daemon:subscription-cursor-gap`/`event-store:last-sequence` evidence and reconnect remediation instead of silently returning an empty completed tail. Swift pending-response routing now binds `stream_opened` to its request and routes later frame lines by `stream_id`; `EngineProcess.decodeRunStream` accepts framed event payloads, dedups mixed raw/framed compatibility lines by execution event id, and records typed `EngineStreamFailure` summaries for resumable `stream_failed` frames. Swift Chat send now starts a stored background `subscribe_events` tail subscription for each accepted run, advances a per-run cursor across repeated subscribe calls, applies received execution events into local `timeline-event-*` cards immediately with event-id dedup, stops the subscription on terminal run events, preserves locally observed live event cards across durable timeline reloads until the repository projection with the same event id arrives, and surfaces resumable cursor-gap failures as preserved `live_stream_failure` timeline error rows plus a reconnect banner instead of collapsing them into an opaque process error. The event-journal timeline projection now also flattens safe worker/tool/patch/test metadata from both top-level and nested agent payloads into durable timeline payload fields, including event id/kind/sequence/actor, `agent_event_kind`, worker/work-item/lease/batch ids, role label, tool name, redacted command, exit code, timeout, duration, applied/target/touched files, test targets, patch/apply counts, code/reason, receipt/verification refs, context-pack refs, provider/model ids, and parallel-batch metadata. Swift `ConversationTimelinePayload.liveExecutionEvent` extracts the same fields before durable reload, and `TimelineItemCell` renders a compact detail grid for generic worker/tool/patch/verification rows.
- target change: durable event stream subscription with cursor/gap/reconnect/dedup and typed timeline projection
- assistant event follow-up: event-journal replay now treats `assistant_text_delta` and `assistant_text_completed` as first-class assistant timeline events instead of generic scheduler rows. Durable projection carries the accepted assistant message id, redacted assistant delta/text snippet, provider/model ids, response hash/bytes, and completion reason; assistant event state is normalized to `streaming` or `completed`. Swift live timeline extraction mirrors those fields, applies a compact live redaction pass for secret-looking tokens, and renders `AssistantTimelineEventCell` for assistant event cards that are not canonical message rows.
- observer-mode follow-up: Swift `ConversationStore.send` now stops after durable `turnStart`, starts the run-event subscription, clears the draft, and reloads accepted read models; it no longer executes `conversation_supervisor_tick` as part of send. Default `load()` also no longer drains queued/running turns, and the product `load(recoverQueuedTurns:)` recovery flag has been removed. `drainSupervisorQueue` remains an explicit diagnostic/manual recovery controller action.
- drain lifecycle follow-up: explicit `drainSupervisorQueue` now applies each supervisor tick receipt to the visible Swift read model before any durable reload. A relaunched chat with no live subscription can show run/message/sidebar lifecycle completion from the tick receipt itself when `refreshAfterDrain: false`, while assistant content remains reload-owned. When Chat is backgrounded, drain reconciliation refreshes light summary/run/settings state but does not rehydrate the heavy message/timeline page until activation.
- tests: `cargo test -p opensks-conversation --locked` passed with 24 tests covering replay/dedup/cursor/redaction, stream metadata lookup, git/image typed timeline items, and the new `execution_event_replay_flattens_tool_worker_patch_details` regression for durable tool/worker/patch/test detail fields. `cargo test -p opensks-stream --locked` passed. `cargo test -p opensks-daemon subscribe_events --locked` and `cargo test -p opensks-daemon --locked` passed with regressions proving subscription responses include explicit stream frames, preserve raw event lines, do not leak workspace paths, use accepted conversation stream metadata, materialize event timeline items once, and report out-of-range reconnect cursors with a redacted cursor-gap event plus resumable `stream_failed` frame. `cargo test -p opensks-event-store last_sequence --locked` and `cargo test -p opensks-event-store --locked` passed after adding the committed-cursor helper. `cargo clippy -p opensks-event-store -p opensks-daemon --all-targets -- -D warnings`, `cargo fmt --all --check`, architecture ownership, architecture graph, direct-I/O, and `git diff --check` passed for the cursor-gap slice. `cargo clippy -p opensks-conversation --all-targets -- -D warnings` passed after event-detail projection was added. `swift test --package-path swift --filter OpenSKSStudioTests.ContractsTests` passed with 36 tests, including pending-response stream-id routing, framed-only execution event ingestion, raw/framed duplicate dedup coverage, `stream_failed` failure-summary decoding, and durable timeline execution-detail DTO decoding. `swift test --package-path swift --filter OpenSKSStudioTests.ConversationTurnTests` passed with 21 tests covering Chat send background subscription startup, mock accepted-run event replay, live timeline card dedup, terminal `snapshot_written` shutdown evidence, resumable cursor-gap timeline/banner presentation surviving the normal send drain/reload reconciliation, live tool/patch detail extraction, and `TimelineItemCell` render coverage. `swift build --package-path swift` and full `swift test --package-path swift` passed with 336 Swift tests after the event-detail timeline slice. Earlier in this PR-095 slice, `cargo test -p opensks-cli timeline --locked` and `cargo test -p opensks-cli conversation --locked` passed after the CLI helper moved to the repository reducer.
- assistant event follow-up tests: `cargo test -p opensks-conversation execution_event_replay_flattens_assistant_text_event_details --locked`, `cargo test -p opensks-conversation --locked` (25 tests), `cargo clippy -p opensks-conversation --all-targets -- -D warnings`, `swift test --package-path swift --filter OpenSKSStudioTests.ContractsTests` (37 tests), `swift test --package-path swift --filter OpenSKSStudioTests.ConversationTurnTests` (22 tests), full `swift test --package-path swift` (338 tests), `cargo fmt --all --check`, architecture ownership, architecture graph, direct-I/O audit, and `git diff --check` passed. `swift-format` remains unavailable, and SKS artifact/wiki validation remains blocked by `update_migration_lock_held`.
- observer-mode follow-up tests: `swift test --package-path swift --filter OpenSKSStudioTests.ConversationTurnTests` passed with 28 tests, including send-without-supervisor-tick, default-load-without-drain, explicit drain recovery, reload-free tick lifecycle application after relaunch, backgrounded-drain heavy-page retention, live subscription terminal event after explicit drain, and assistant event card coverage. Full `swift test --package-path swift` passed with 340 tests in an earlier observer-mode slice; architecture ownership, architecture graph, direct-I/O audit, and `git diff --check` also passed in that earlier slice. `swift-format` remains unavailable.
- migration: timeline projection version
- removal/deletion: local compatibility arrays as timeline SSOT
- final evidence: partial backend durable timeline projection, explicit framed subscription replay, backend reconnect cursor-gap detection, Swift Chat send background cursor subscription-to-timeline wiring, front-end resumable gap presentation, generic typed detail rendering for durable/live worker, tool, patch, and targeted-test event metadata, specialized durable/live assistant delta/completion cards, Swift primary send/load observer-mode behavior, reload-free explicit-drain lifecycle receipt application, and backgrounded-drain heavy-page preservation are verified through Rust and Swift tests. Remaining PR-095 gap: complete native-app lifecycle/manual release proof beyond the current tested observer/control split.

### PR-096 - Conversation/Event SSOT Migration

- status: In Progress
- owner file/module: `crates/opensks-conversation`, `crates/opensks-event-store`
- current evidence: work order reports duplicate state in turns/runs/run_projections/conversation status/event store. The conversation repository already treats `run_projections` as the real run-state read model for `runs_for_conversation`, and no-projection linked runs surface as `None`/unknown rather than completed. PR-096 now removes the accepted-handle replay fallback that silently converted a missing run projection into `queued`: idempotent `accept_conversation_turn` replay requires a valid projection row and returns `ProjectionMissing` or `InvalidRunProjectionState` for corrupt/migrating data instead of fabricating a normal state. Event replay through `project_execution_events_into_timeline` now also updates `run_projections.state` from durable lifecycle events (`RunStarted`, `RunPaused`, `RunResumed`, `RunCancelled`, `VerificationFailed`, `RunCompleted`) using the same terminal-sticky/no-regress-to-queued merge semantics as the engine reducer, while keeping `SnapshotWritten` as lower-information metadata that cannot overwrite lifecycle state. The same replay transaction now refreshes non-archived conversation summary status from `run_projections`: active queued/running/paused projections keep the summary running, while terminal-only projected runs let failed/cancelled/completed terminal state surface as `failed`/`completed`. Cursor-0 subscription replay now uses `rebuild_execution_events_into_timeline`, which starts from the accepted turn's queued state, deletes stale event-derived timeline rows, recomputes `run_projections`, resets `stream_cursors`, and refreshes conversation status from the replayed event journal instead of trusting stale live projection rows. Successful daemon supervisor execution now appends a durable `RunCompleted` event before finalizing the turn, so completion can be rebuilt from the event journal. Supervisor completion, cancellation-before-dispatch, and failed dispatch now project the run event journal into conversation read models before lease finalization; `finish_turn_supervisor_lease_after_projection` then clears the fenced lease and leaves `run_projections` as the lifecycle/checkpoint authority while `runs` stores only lease/fencing/checkpoint metadata. Claim, heartbeat, expired-lease recovery, migration status repair, and supervisor-finish status refresh now gate lifecycle decisions through `run_projections` rather than trusting duplicate `turns.state` / `runs.state` rows. Projection-terminal runs cannot be claimed, heartbeated, or requeued by stale legacy rows, and projection-queued/running runs remain claimable, heartbeatable, and recoverable even when duplicate legacy rows are stale terminal. `runs` remains lease metadata storage for holder/expiry but no longer decides lifecycle eligibility. `run_last_event_sequence` now reads `run_projections.last_sequence`, and the compatibility `set_turn_run_state_with_last_sequence` path transactionally updates `run_projections.state`/`last_sequence` and refreshes conversation status from projections instead of leaving the projection stale behind legacy updates. Migration version 6 physically removes the duplicate lifecycle columns from `turns` and `runs`; before dropping old `runs.state`, it backfills missing `run_projections` rows from legacy run state/checkpoint metadata.
- target change: append-only event journal lifecycle, reducer-owned projections, unknown state preservation
- tests: `cargo test -p opensks-conversation --locked` passed with 35 tests, including regressions for missing/unrecognized run projection state on idempotent accepted-turn replay, event replay coverage for run lifecycle projection, terminal stickiness, snapshot non-downgrade behavior, failed/completed summary refresh, active multi-run summary preservation, full replay rebuild from queued + events instead of stale projection rows, projection-terminal rows not being claimable/heartbeatable/requeued, projection-queued/running work remaining claimable/heartbeatable/recoverable with no duplicate lifecycle columns present, migration repair using projection state, `run_last_event_sequence` returning the projection checkpoint, `set_turn_run_state_with_last_sequence` updating projection state/checkpoint plus conversation status, `finish_turn_supervisor_lease_after_projection_preserves_event_reducer_state`, fresh schema omission of `turns.state`/`runs.state`, and legacy migration backfill of `runs.state='completed'` plus `last_event_sequence=42` into `run_projections` before dropping the columns. `cargo test -p opensks-daemon conversation_supervisor_tick --locked -- --test-threads=1`, `cargo test -p opensks-cli capability --locked`, `cargo test -p opensks-contracts capability --locked`, `cargo run -p xtask -- capability-matrix --runtime-fixture release`, `cargo clippy -p opensks-conversation -p opensks-daemon -p opensks-contracts -p opensks-cli --all-targets -- -D warnings`, and `cargo fmt --all --check` passed after the physical duplicate-column removal. Earlier PR-096 tests also passed after adding the `RunCompleted` lifecycle event and projection-authority tightening. The Swift follow-up passed `swift test --disable-sandbox --package-path swift --scratch-path /tmp/opensks-swift-build-pipeline --filter OpenSKSStudioTests.PipelineProjectionTests`, `OpenSKSStudioTests.LivePipelineTests`, and `OpenSKSStudioTests.GraphViewTests`, proving unrecognized run/node state strings decode as `.unknown`, render as `Unknown state · migration required`, and no longer inflate queued/active counters or expose active-run controls.
- migration: conversation DB `user_version` is now 6; migration backfills missing `run_projections` rows from legacy `runs.state`/`last_event_sequence` for linked conversation runs, then drops duplicate lifecycle columns from `turns` and `runs`. Fresh schemas omit those columns.
- removal/deletion: removed the accepted-turn idempotency `unknown`/missing-projection -> `queued` fallback from the daemon conversation accept path; cursor-0 event replay now removes stale event-derived timeline rows before rebuilding them from the event journal; successful completion is no longer event-journal-invisible; duplicate `turns.state` / `runs.state` rows no longer authorize or block supervisor claim, heartbeat, expired-lease recovery, migration status repair, finish-time conversation status decisions, or last-sequence reads when `run_projections` says otherwise. The duplicate lifecycle columns are now physically removed from fresh and migrated conversation schemas; `runs` keeps lease owner, lease expiry, fencing token, and checkpoint metadata.
- final evidence: partial projection integrity, unknown-state preservation across Rust repository and Swift pipeline UI, event-replay-to-run-state projection, event-replay-to-conversation-status projection, cursor-0 event-journal rebuild for run projection/stream cursor/conversation status/timeline rows, durable `RunCompleted` lifecycle evidence, event-projected supervisor completion/cancellation/failure finalization, bidirectional projection-authoritative claim/heartbeat/recovery/status repair, projection checkpoint reads, compatibility setter projection updates, and physical duplicate lifecycle-column removal with legacy backfill verified. Remaining PR-096 scope is broader append-only event/repair hardening, not retained `turns.state`/`runs.state` compatibility mirrors.

### PR-097 - CodeGraph/TriWiki Context Integration

- status: Verified
- owner file/module: `crates/opensks-context`, `crates/opensks-codegraph`, `crates/opensks-triwiki`, `crates/opensks-intel`, `crates/opensks-contracts`, `crates/opensks-scheduler`, `crates/opensks-daemon`, `swift/Sources/Editor`, `swift/Sources/Conversations`
- current evidence: work order requires context assembler and worker-scoped token packs. `opensks-context` now builds context packs from TriWiki records plus CodeGraph records, using a persisted CodeGraph index when present and refreshing only changed source files in memory before selection. The `ContextPack` contract now carries defaulted `codegraph_record_ids`, `changed_paths`, `selected_test_targets`, `turn_context_refs`, structured `turn_context_items`, optional `conversation_summary`, optional `branch_freshness`, and optional Intel `freshness` metadata. Known `editor://path#Lx-Ly#fnv1a64:...` selected-context refs are resolved into structured items with path/range/captured hash/current hash, stale/redacted flags, reason code, evidence refs, and body text only when the current range is fresh and safe. Opaque refs are retained as unresolved metadata; stale captured hashes fail closed without body text; secret-like selected text is redacted before the pack JSON/body is written. Conversation summaries now flow through the existing redacted `conversation_summaries` store as a typed `ConversationDigest`, then into the generated context pack as `ContextPackConversationSummary` with source sequence/generated timestamp, evidence refs, and a defensive secret-like re-redaction guard. Branch freshness now records current branch, head, base ref, merge-base, ahead/behind counts, and paths changed since base; branch-diff paths are unioned into context changed-path selection so clean feature branches still produce worker context for committed changes. CodeGraph indexing and incremental update now refresh generated relationship edges for imports, calls, references, and tests from current source text and records. CodeGraph selection traverses one-hop graph edges from selected changed records and carries adjacent neighbor records with `opensks-context:codegraph-adjacent` plus edge-kind evidence such as `opensks-context:codegraph-edge:calls` or `opensks-context:codegraph-edge:references`. `opensks context pack` reports CodeGraph/test-target counts, and generated `schemas/context-pack.schema.json` includes the new metadata. The pack body includes freshness, branch freshness, changed paths, selected test targets, selected turn context refs/items, conversation summary, TriWiki entries, and selected plus adjacent CodeGraph entries within the token budget. `SchedulerWorkItem` now carries defaulted `context_pack_ref` and `worker_context_pack_ref` fields. Daemon conversation turn start creates a real `.opensks/wiki/context-packs/generated/turn-context-{turn_id}.json` context-pack artifact before scheduler bootstrap, queues the root planning work item with only the safe `artifact://...` ref and `context:turn-context-pack` evidence, and scheduler parallel dispatch derives deterministic per-worker scoped context artifact refs from that root artifact for lease/dispatch/running payloads and worker inputs with `context:worker-context-pack` evidence. `opensks-context::build_worker_context_pack` now creates minimized per-worker bodies from the root pack with worker scope, freshness, branch/changed-path signals, selected test targets, selected context refs/items, conversation summary, CodeGraph record IDs for code/verifier roles, TriWiki record IDs rather than full TriWiki prose, bounded token budgets, and `opensks-context:worker-scoped-context-pack` evidence. Daemon role workers now resolve root/worker artifact refs safely under the workspace, materialize each worker JSON artifact before provider dispatch, emit materialization status in role-worker events, include `context:worker-context-pack` / `opensks-context:worker-scoped-context-pack` evidence, and insert the worker pack body into advisory and code-role provider prompts. Swift now captures editor selected-line refs as backend-formatted `editor://path#Lx-Ly#hash` values, stages them in the conversation store, refreshes their stale/fresh state from live editor text, renders a compact composer attachment strip with stale warnings/removal controls, and sends the resulting `TurnContextSelection.refs` plus the real thread settings snapshot through `ConversationService.turnStart` instead of hardcoded defaults.
- target change: context pack assembly with architecture/symbol/tests/wrongness/glossary/branch evidence and freshness hashes
- tests: `cargo test -p opensks-codegraph --locked` passed with coverage that generated import/call/test edges are written and reference lookup makes caller files adjacent to called helper symbols. `cargo test -p opensks-context --locked` passed with regression coverage that a stale persisted CodeGraph is refreshed in memory for a changed source file, changed paths are recorded, a committed feature-branch diff path is selected relative to `main`, branch freshness carries branch/base/merge-base/ahead/behind metadata, the changed symbol appears in `codegraph_record_ids`, the related test target appears in `selected_test_targets`, selected editor/symbol refs appear in `turn_context_refs`, explicit fresh editor ranges resolve into `turn_context_items`, stale selected ranges fail closed on captured-hash mismatch, secret-like selected ranges are redacted without raw secret text in body/JSON, a redacted conversation summary is structured/budgeted into the pack, secret-like summary text is defensively re-redacted, one-hop adjacent CodeGraph records are included with adjacent/edge-kind evidence, generated call edges bring helper symbols into the pack, freshness is attached, body entries remain budgeted, and a minimized code-role worker pack includes worker scope, fresh selected context, CodeGraph record IDs, worker-context evidence, no full TriWiki prose entries, and a token estimate inside the worker budget. `cargo test -p opensks-conversation --locked` passed after typed `ConversationDigest` retrieval was added. `cargo test -p opensks-cli context --locked` passed with generated artifact coverage for CodeGraph/freshness fields. `cargo test -p opensks-scheduler --locked` passed with the root-work-item `context_pack_ref`/evidence regression and the 21-test parallel dispatch regression proving distinct worker artifact refs reach worker inputs and running events. `cargo test -p opensks-daemon conversation_turn_start --locked` passed with artifact creation, scheduler event ref, freshness metadata, selected context refs/items, conversation summary, branch freshness, and no absolute-path leak coverage. `cargo test -p opensks-daemon conversation_supervisor_tick_dispatches_provider_registry_route_through_chat_adapter --locked -- --test-threads=1` passed with provider-backed role worker coverage that materialized all four scoped worker context-pack artifacts, included worker bodies in role model requests, carried `worker_context_pack_materialized`, and preserved redaction/no-main-worktree mutation guarantees. `cargo test -p opensks-contracts --locked`, `cargo test -p opensks-intel --locked`, `cargo test -p opensks-engine --locked`, schema generation, clippy, fmt, diff whitespace, and architecture/direct-I/O guards passed. `swift test --package-path swift --filter OpenSKSStudioTests.ConversationTurnTests` passed with coverage that Swift sends thread settings plus selected context refs into turn start and renders stale context attachments. `swift test --package-path swift --filter OpenSKSStudioTests.ConflictDiffTests` passed with coverage for backend `editor://` wire formatting and selected-line state capture.
- migration: context artifact schema now includes defaulted CodeGraph/freshness/turn-context-ref, structured turn-context-item metadata, optional conversation summary metadata, and optional branch freshness metadata, and scheduler work-item schema now includes defaulted `context_pack_ref` and `worker_context_pack_ref`; older JSON without these fields remains decodable through serde defaults.
- removal/deletion: context packs no longer package only TriWiki prose; the CLI-generated product artifact, daemon turn-start scheduler path, and provider role-worker prompts now carry CodeGraph, adjacent-CodeGraph, branch-freshness, changed-path, selected-test, selected turn-context-ref/item, conversation-summary, freshness, and context-pack artifact-ref evidence from the existing SSOTs instead of prose-only root packs or cosmetic worker refs. Worker-scoped packs carry TriWiki record IDs instead of full TriWiki prose entries. The product path does not dereference arbitrary opaque selected-context strings into trusted source text, and it does not read raw unredacted conversation text for the context summary.
- final evidence: context assembler and daemon/scheduler root-worker context-pack handoff verified through focused Rust/CLI tests, regenerated schemas, clippy, fmt, whitespace, and architecture/direct-I/O guards; explicit `editor://` selected file ranges are now validated, stale-checked, and secret-redacted before inclusion, stored redacted conversation summaries are included with source metadata, generated CodeGraph import/call/test/reference edges feed one-hop adjacent record selection with edge evidence, branch/base freshness comparisons feed committed branch diff paths into worker context, true parallel scheduler dispatch now hands each worker a distinct scoped artifact ref, daemon role workers now materialize minimized per-worker context-pack JSON artifacts and include their bodies in provider role prompts, and Swift Chat now exposes stale-context indicators/background refresh while sending real context refs/settings to turn start.

### PR-098 - Image/Vision Provider Pipeline

- status: In Progress
- owner file/module: `crates/opensks-image`, `crates/opensks-provider`, `crates/opensks-contracts`, `crates/opensks-conversation`, `crates/opensks-cli`, Swift model selector/live timeline
- current evidence: work order requires image/vision capabilities in same provider registry/router. `opensks-provider` already inferred OpenAI-compatible image/vision capabilities from model metadata; this slice adds an explicit `RoutingRequest::for_vision` path and a fake vision model test helper so `image.inspect` can route by `vision_input` without requiring `image_output`. `opensks-image` no longer creates a hard-coded `fake-local` placeholder asset after route selection: image generation now builds assets from the provider route receipt, records provider/model id from that receipt, hashes prompt text instead of storing it, carries the route receipt on the asset, and appends a schema-backed image provenance receipt with content/provenance hashes. The mock image lane now generates deterministic PPM bytes, derives the asset `content_hash` from those bytes with `sha256:v1`, writes the asset atomically under `.opensks/assets/candidates/`, reads it back, and records `opensks-image:asset-file-verified` only after the on-disk hash matches the ledger. Unsafe asset ids are rejected before path construction. `opensks-image` now also has provider-byte generation and provider-backed inspection paths: generation derives the provider-registry remote model id, honors an explicit provider-registry image model id when the daemon has one from durable thread settings, calls an injected image generation client, hashes the returned bytes, writes the returned image atomically with a safe extension, verifies the on-disk hash, and records provider-generation evidence; inspection loads an existing ledger asset, verifies its on-disk content hash, derives a vision route/remote model id, calls an injected inspection client, and appends an inspection provenance receipt without storing raw prompt or image bytes in the ledger. `opensks-adapter` now exposes `OpenAiCompatibleImageGenerator`, which posts a secret-redacted bearer-header request to `/images/generations`, sends `model`/`prompt`/`n`/`size`, decodes `b64_json` image responses, supports provider URL image outputs, and can inspect existing images through `/chat/completions` with text plus `image_url` data URL content without putting the credential in argv/log/event payloads. The agentic tool loop now parses `image.generate` and `image.inspect`, can selectively advertise them only when a runtime supplies the matching image/vision executor, reports `image_executor_unavailable` instead of fake success when no executor is present, emits `ImageArtifactCreated` events plus typed observations when generation succeeds, and returns provider vision text plus provenance metadata as a typed inspection observation. The daemon now builds an optional provider-registry-backed image executor for live provider dispatch: it preflights enabled image and vision routes independently, uses `ConversationTurnSettings.image_model_id` as an explicit generation route when present, resolves the selected provider secret through the existing dispatch secret path, enables only dispatchable image tools for that turn, calls the OpenAI-compatible image/vision transport, writes generated assets and inspection receipts into the same image ledger, maps daemon `ImageArtifactCreated` events to durable `image_artifact_created` event kind, and keeps credentials out of request/output/event surfaces. Swift `ProviderStore` now exposes eligible image and vision model lists from the same provider/model registry state, seeds a disabled-until-probed local image model for compatible providers, and `ConversationComposer` binds a visible image model selector to the existing durable `ConversationThreadSettings.imageModelId` field. Durable execution events now include an explicit `image_artifact_created` kind; the CLI agent bridge preserves `AgentEventKind::ImageArtifactCreated` as that execution kind; the conversation event-journal projector materializes image artifact timeline rows with flattened, secret-redacted asset/provider/model/path/hash/dimension fields; and Swift live timeline DTOs decode/render the same typed image artifact fields before reload.
- target change: image model selector, image/vision capability route, asset/provenance receipts
- tests: `cargo test -p opensks-image --locked` passed with coverage for disabled image route failure, provider/model provenance on generated assets, prompt hash/provenance hash receipts, SHA-256 byte-bound generated PPM asset writes, provider-byte returned image writes using the provider-registry remote model id, unsafe asset id rejection, image inspection via a vision route, blocked inspection when no vision model is enabled, and provider-backed inspection that reads verified asset bytes and records an inspection receipt. `cargo test -p opensks-adapter --locked` passed with local HTTP coverage for `OpenAiCompatibleImageGenerator`, OpenAI-compatible `/chat/completions` vision data-URL inspection, `image.generate`/`image.inspect` parser and selective schema advertising, executor-missing failures, successful image artifact event/observation emission, and successful image inspection provider observation emission through the agentic loop. `cargo test -p opensks-daemon --locked` passed with local `/images/generations` and `/chat/completions` servers proving daemon provider-registry image dispatch sends the remote image model id and size, daemon vision dispatch sends the remote vision model id plus data URL image content, both paths use the existing secret resolver without leaking the fixture credential, generated image bytes are written, generation/inspection ledger receipts persist, and a provider chat conversation can call `image.generate` then `image.inspect` end to end while `imageModelId` selects a lower-scored image model over the auto-route default. The same daemon E2E asserts the isolated worktree ledger contains generate/inspect receipts, provider request logs do not contain `sk-`, persisted events do not leak the fixture credential or absolute workspace path, image generation emits durable `image_artifact_created`, and `image.inspect` completion carries the vision model receipt. `cargo clippy -p opensks-image -p opensks-adapter -p opensks-daemon --all-targets -- -D warnings`, `cargo fmt --all --check`, and `git diff --check` passed for the provider image/vision tool integration slice. `cargo test -p opensks-provider --locked` passed with the new vision capability routing regression plus existing provider repository/probe/routing tests. `cargo test -p opensks-contracts --locked` passed after adding the image provenance receipt contract and image inspect tool schema inputs. `cargo test -p opensks-cli foundation_commands_write_artifacts --locked` passed after the CLI image ledger fixture moved to the provider-backed generation path and verifies the generated file bytes hash back to the ledger `content_hash`. `swift test --package-path swift --filter OpenSKSStudioTests.ProviderTests` passed with image eligibility and disabled-until-probed coverage. Timeline projection coverage now includes `cargo test -p opensks-contracts run_control_requests_roundtrip_with_typed_params --locked`, `cargo test -p opensks-conversation execution_event_replay_materializes_image_artifacts_as_typed_timeline_items --locked`, `cargo test -p opensks-cli image_agent_events_keep_image_artifact_execution_kind --locked`, `cargo test -p opensks-engine --locked`, `swift test --package-path swift --filter OpenSKSStudioTests.ConversationTurnTests`, and `cargo clippy -p opensks-contracts -p opensks-conversation -p opensks-cli -p opensks-engine --all-targets -- -D warnings`. `cargo run -p xtask -- schemas` regenerated `schemas/image-asset.schema.json`, `schemas/image-ledger.schema.json`, `schemas/execution-event-envelope.schema.json`, and new `schemas/image-provenance-receipt.schema.json`.
- migration: image asset ledger now has defaulted `provenance_receipts`; `ImageAsset` has defaulted optional `provenance_hash` and `route_receipt`, so older ledgers remain decodable. Generated execution event schema now admits `image_artifact_created`, and Swift decoding handles that label explicitly.
- removal/deletion: product image generation path no longer hard-codes provider provenance after routing; generated assets and inspections are tied to provider/model route receipts, and the mock image path no longer records a file path without creating and verifying the file. Agent image artifact events no longer collapse into generic `work_item_running`, and timeline projections no longer bury image asset fields only inside an opaque nested payload. The live model no longer has to rely on out-of-band helpers for `image.generate` or `image.inspect`; the daemon can advertise and execute each tool only when the provider registry has a matching dispatchable image or vision model.
- final evidence: partial provider-backed image/vision pipeline verified through focused Rust, daemon E2E, CLI, schema, and Swift Provider tests; deterministic mock image execution now writes a durable byte-bound asset file and ledger receipt; OpenAI-compatible provider image generation can now fetch real provider image bytes into the same durable asset/provenance path; daemon/provider-registry `image.generate` execution can write an artifact and ledger through the agentic tool loop; daemon/provider-registry `image.inspect` execution can read a verified asset, call an OpenAI-compatible vision model, return provider text to the agent, and append an inspection receipt; provider chat can now drive `image.generate` -> `image.inspect` in one isolated turn while respecting the durable image model selector; durable image artifact events now project into typed conversation timeline cards across Rust replay and Swift live updates. Remaining PR-098 risk is native-app manual/release smoke for the same image/vision path, which is tracked under PR-100 release readiness rather than a missing backend runtime path.

### PR-099 - Git Commit/Push Receipts

- status: Verified
- owner file/module: `crates/opensks-git-service`, `crates/opensks-git`, `crates/opensks-cli`, Swift Git views/timeline
- current evidence: work order requires verified integration diff, commit approval, push receipt, durable timeline items. Current code already has local commit preview/commit receipts bound to a reviewed index hash, an approval-gated push outbox, at-most-once push receipts, and Swift commit/push cards. This slice adds a schema-backed `opensks.push-failure-diagnostic.v1` contract and extends `PushStatus` with defaulted `failures`. `opensks-git` now persists a secretless failed-push diagnostic in `.opensks/runtime/push-outbox.sqlite3` when an approved remote push reaches execution but fails or cannot verify the landed remote oid, preserves the retryable approved intent and local commit, and deletes the diagnostic only after a verified successful receipt. `opensks git push-status` now recovers those diagnostics, and Swift `GitPushStatus` decodes them so the app does not silently lose failed-push recovery state after relaunch. Swift `PushOutboxSummary` now counts failed diagnostics, renders compact failed rows with retry context, and `GitPushOutboxState.hasOutbox` treats recovered failures as visible outbox work. Swift Git Studio now also renders a reusable `PushFailureDiagnosticsPanel` from `GitPushStatus.failures`; the compact Git view refreshes push status itself and surfaces recovered failed-push diagnostics on non-commit panes with a review action back to the commit/outbox pane, so failures are visible outside the compact outbox summary. Event-backed timeline projection now has explicit `git_commit_receipt`, `git_push_receipt`, and `git_push_failed` execution-event kinds; conversation replay projects them into typed commit/push timeline items with secret-redacted flattened receipt fields, and Swift live event handling renders matching commit/push cards before the durable reload. Runless Git Studio commit/push callbacks now call `conversation receipt-event-append` instead of direct `timeline-append`: the CLI creates a hidden event-message anchor turn/run, appends the receipt event to `.opensks/runtime/engine.sqlite3`, projects the typed durable timeline item, and returns the projected item idempotently. Swift keeps the local optimistic card for immediate feedback, then reloads from the event-backed projection. Commit preview and commit receipts now also carry `staged_diff_hash/ref` and `reviewed_staged_diff_hash/ref`, so the local commit approval surface binds the resulting receipt back to the exact staged diff evidence reviewed under the `index_hash` gate. When an integration coordinator `final.diff` artifact has the same content hash as the current staged diff, commit preview and commit receipt now automatically carry `integration_final_diff_hash`, `integration_final_diff_ref`, `integration_run_id`, and `integration_candidate_id`; Swift receipt cards and durable timeline payloads preserve that link. The local PR-099 release E2E now stitches the chain together with public APIs: daemon integration apply writes the integration receipt and `final.diff`, CLI Git stages the applied path, commit preview and commit receipt preserve the integration final-diff/run/candidate evidence, approved push execution writes the committed oid to a local bare remote, `push-status` recovers the completed receipt, and repeat execution returns the existing idempotent receipt.
- target change: approval-gated commit/push with receipts and no compatibility-array SSOT
- tests: `cargo test -p opensks-contracts -p opensks-git --locked` passed with secretless diagnostic contract coverage and failed-push SQLite recovery after reopening the outbox. `cargo test -p opensks-cli push_cli --locked` passed with failed `push-execute` returning `push_failed`, no completed receipt, one approved retryable intent, one recovered `push-failure-diagnostic` in `push-status`, protected-ref acknowledgement, and local bare remote success. `swift test --package-path swift --filter OpenSKSStudioTests.PushOutboxTests` passed with 23 tests after Swift status decoding gained `failures`, the relaunch recovery test asserted recovered failure state, and `PushOutboxSummary` rendered recovered failure diagnostics at desktop widths; the suite now passes with 25 tests after adding coverage for `PushFailureDiagnosticsPanel` details and compact Git Studio recovered-failure visibility outside the commit pane. `cargo test -p opensks-contracts --locked`, `cargo test -p opensks-conversation --locked`, `cargo test -p opensks-engine projection --locked`, and `swift test --package-path swift --filter OpenSKSStudioTests.ConversationTurnTests` passed after adding event-backed git receipt projection and Swift live receipt card rendering. `cargo test -p opensks-cli receipt_event_append --locked` proves the runless receipt-event append writes one event, hides the anchor, projects a typed timeline item, and retries idempotently. Focused Swift commit/push sink tests prove the UI receives the event-backed commit/push items after local optimistic cards. Reviewed staged-diff evidence binding is verified by `cargo test -p opensks-contracts -p opensks-git-service --locked`, `cargo test -p opensks-conversation --locked`, `cargo test -p opensks-cli receipt_event_append --locked`, `swift test --package-path swift --filter OpenSKSStudioTests.GitCommitTests`, `cargo run -p xtask -- schemas`, Rust fmt/clippy, and diff whitespace checks. Integration final-diff auto-linkage is verified by `cargo test -p opensks-git-service commit_preview_and_receipt_link_matching_integration_final_diff --locked`, `cargo test -p opensks-contracts -p opensks-git-service --locked`, `cargo test -p opensks-conversation execution_event_replay_materializes_git_receipts_as_typed_timeline_items --locked`, `cargo test -p opensks-cli receipt_event_append --locked`, `swift test --package-path swift --filter OpenSKSStudioTests.GitCommitTests`, schema generation, Rust clippy/fmt, and diff whitespace checks. The connected receipt-chain E2E is verified by `cargo test -p opensks-cli --test pr099_receipt_e2e --locked`; full `cargo test -p opensks-cli --locked` passes with 94 unit tests plus that integration test, and `cargo clippy -p opensks-cli --all-targets -- -D warnings`, `cargo fmt --all --check`, architecture ownership, architecture graph, direct-I/O audit, and `git diff --check` pass. `swift build --package-path swift` and full `swift test --package-path swift` (334 tests) also pass after the recovered failed-push diagnostics UI polish. `swift-format` is unavailable in this shell.
- migration: push outbox SQLite `user_version` bumped to 2 with `push_failure_diagnostics`; generated `push-failure-diagnostic.schema.json` and defaulted `PushStatus.failures` keep older status consumers decodable. Generated `execution-event-envelope.schema.json` now includes git receipt event labels. Generated `git-commit-preview.schema.json` and `git-commit.schema.json` now include backward-compatible optional reviewed staged-diff and integration final-diff evidence fields. Runless Git Studio receipt appends create synthetic hidden conversation anchor rows (`messages` role `event`, `turns`, `runs`, `conversation_runs`, `run_projections`, `stream_cursors`) so the existing event-journal projector remains the timeline SSOT.
- removal/deletion: the product Git Studio receipt path no longer calls direct `timeline-append`; local commit/push card arrays remain as optimistic UI cache while durable receipt state is projected from the event journal.
- final evidence: PR-099 is verified locally for failed push durability, retryability, Swift status recovery, Swift outbox failure surfacing, broader compact Git view failed-diagnostic visibility outside the outbox summary, run-backed event-journal projection, runless Git Studio event-journal receipt append, reviewed staged-diff binding, automatic content-hash linkage from integration-coordinator `final_diff_ref` into Git commit preview/receipt/timeline payloads, and a connected local release E2E covering daemon integration apply -> CLI stage -> commit preview -> commit receipt -> approval-gated push receipt -> durable push status recovery -> idempotent repeat execute. Native-app manual release smoke remains tracked under PR-100 rather than PR-099.

### PR-100 - Security/Reliability/Performance Release Gate

- status: In Progress
- owner file/module: CI, docs/runtime truth, test fixtures, release readiness, `crates/opensks-adapter/src/lib.rs`, `crates/opensks-adapter/src/patch_write.rs`, `crates/opensks-adapter/src/agentic.rs`, `crates/opensks-cli/src/lib.rs`, `crates/opensks-cli/src/file_command.rs`
- current evidence: release guards had regressed locally after the integration-verification slice: `bash scripts/check-architecture-ownership.sh` failed because `crates/opensks-cli/src/lib.rs` exceeded the 8000-line budget and `crates/opensks-adapter/src/lib.rs` exceeded its public-function budget, while `cargo run -p xtask -- direct-io-audit --check` failed because product `artifact.write` still used direct `std::fs::write`. The CLI `file` implementation now lives in `file_command.rs` with allowed public `run_file_command`/`file_usage` wrappers in `lib.rs`; adapter patch-write implementation details now live in `patch_write.rs` with the allowed public `apply_file_writes` wrapper in `lib.rs`; and `artifact.write` persistence now routes through the lease-bound PatchEngine path using the authorized gateway workspace root. The adapter product direct-write guard passes, and the architecture budget reports `crates/opensks-cli/src/lib.rs` at 7862 lines and `crates/opensks-adapter/src/lib.rs` at 899 lines. After the live planner follow-up grew the CLI root again, capability report implementation was extracted into `crates/opensks-cli/src/capability.rs`; `lib.rs` now stays below budget with thin public wrappers for the allowed capability functions, and the implementation ledger no longer records a developer-machine absolute path. The latest broad Rust rerun exposed a default-parallel daemon race where resident turn-start wakeup could overlap an explicit supervisor tick and collide on duplicate event-journal ids; the wakeup now waits for stream request quiescence and rechecks explicit claim generation before resident drain. This follow-up reran the broad release gates: full Rust workspace tests, workspace clippy with all targets/features, Swift build/test, schema/capability generation stability, local security audit, scheduler dispatch, perf stress, and acceptance audit. The beta-006 validator now accepts a strict hash-bound Codex App multi-agent proof shape (`sks.codex-app-agent-session-proof.v1`) alongside the existing native CLI proof shape, rejects mock/fake/non-empty-blocker proof, and emits explicit `provenance_proof_kind` plus Codex App proof refs when that shape is used. The `ci-performance` workflow now also runs the 100k `opensks perf stress` contract with `--cache-capacity 256`, gates the emitted JSON for schema/events/`within_budget`/`leaked_handles`/child reaping, and uploads the JSON as a GitHub Actions artifact. Current workspace acceptance is honest at 22/23 criteria passed with one partial: `mvp-004` still requires explicit live remote OpenRouter/OpenAI `/models` probe opt-in and relevant credentials.
- target change: mock provider E2E, crash/soak/fault injection, secret leak scan, capability/runtime truth matrix updates, and full final command set. This slice closes the previously unverified broad Rust/Swift/local-security/local-scheduler evidence while preserving the explicit live-credential/live-provenance gaps.
- tests: `cargo metadata --format-version 1 --no-deps`, `cargo fmt --all --check`, `bash scripts/check-architecture-ownership.sh`, `cargo run -p xtask -- direct-io-audit --check`, `cargo test --workspace --locked`, `cargo clippy --workspace --all-targets --all-features -- -D warnings`, `cargo run -p xtask -- schemas`, `cargo run -p xtask -- capability-matrix --runtime-fixture release`, `cargo run -p xtask -- architecture-graph --check`, `swift build --package-path swift`, `swift test --package-path swift` (330 tests), `cargo run -- security audit` (0 secret findings, 0 security findings), `cargo run -- scheduler dispatch 128` (128 attempted/completed, 0 failed, max_concurrent_workers 28), `cargo run -- perf stress --events 100000 --cache-capacity 256` (within_budget true, 0 leaked handles), `cargo run -- provider adapter-check` (2 adapters, 0 attempted because remote probe opt-in/credentials absent), `cargo run -- acceptance audit` (23 total, 21 passed, 2 partial, 0 failed), and `git diff --check` passed. Schema and runtime-truth-matrix generation were stable: the tracked `schemas/` diff hash stayed `218ca62a1756dfbf06018c66b5c5d373e41f93dbc9570d89d71e338a30362cb8` before/after `xtask -- schemas`, and the `docs/runtime-truth-matrix.generated.md` diff hash stayed `e29aa8642b6992020eb432cd678293aaf1547dd26266578439867f65adee8205` before/after `capability-matrix`. The CLI architecture-budget follow-up passed `wc -l crates/opensks-cli/src/lib.rs crates/opensks-cli/src/capability.rs`, `bash scripts/check-architecture-ownership.sh`, `cargo test -p opensks-cli capability --locked`, `cargo clippy -p opensks-cli --all-targets -- -D warnings`, `cargo fmt --all --check`, `git diff --check`, and the tracked path-marker scan. The resident explicit-tick race follow-up passed `cargo test -p opensks-daemon streaming_conversation_turn_start_with_explicit_tick_does_not_strand_resident_wakeup --locked -- --test-threads=1 --nocapture`, `cargo test -p opensks-daemon --lib --locked -- --test-threads=1`, `cargo test -p opensks-daemon --lib --locked`, a rerun of `cargo test --workspace --locked`, `swift test --package-path swift` (346 tests), and `cargo clippy --workspace --all-targets --all-features -- -D warnings`. The CI performance follow-up passed `cargo run -- perf stress --events 100000 --cache-capacity 256` with a `jq` contract check, `cargo test -p opensks-cli perf_stress_command_reports_within_budget --locked`, YAML parsing for `.github/workflows/ci-performance.yml`, and targeted diff whitespace checks. The current environment also found the Xcode toolchain formatter and `/Applications/Xcode.app/Contents/Developer/Toolchains/XcodeDefault.xctoolchain/usr/bin/swift-format lint --recursive swift/Sources swift/Tests` exited 0 with output captured to `.sneakoscope/evidence/swift-format-lint-20260625.log`; the existing warning backlog was not auto-formatted to avoid broad Swift churn. Earlier `sks hook user-prompt-submit`, `sks validate-artifacts latest`, and `sks wiki validate .sneakoscope/wiki/context-pack.json` blocker rows were resolved for the current mission by `sks validate-artifacts M-20260625-021208-3eb0`, `sks wiki refresh`, and `sks wiki validate .sneakoscope/wiki/context-pack.json`; no `sks doctor --fix --yes` was run.
- migration: release evidence docs updated only; no runtime migration in this slice. `.opensks/security`, `.opensks/scheduler`, `.opensks/providers`, and `.opensks/acceptance` release-evidence artifacts were refreshed by product commands and remain outside tracked source diffs.
- removal/deletion: removed duplicated CLI file-command implementation from `crates/opensks-cli/src/lib.rs`; removed public patch-write implementation details from `crates/opensks-adapter/src/lib.rs`; removed the product `artifact.write` direct file write path in favor of PatchEngine apply. Test-only fixture writes remain in `#[cfg(test)]` areas and are outside the product direct-I/O guard.
- final evidence: partial PR-100 gate evidence is stronger: architecture ownership, architecture graph, direct-I/O audit, full Rust workspace tests, full workspace clippy, Swift build, full Swift tests, schema/capability generation stability, local security audit/secret leak scan, scheduler dispatch artifact refresh, perf stress, provider adapter-check artifact refresh, acceptance audit, and diff whitespace are verified. A final product-audit follow-up removed a test-fixture literal that looked like an assigned OpenAI key, then reran `cargo run -- security audit` to `secret_findings: 0` / `security_findings: 0`. This continuation extracted native collaboration proof handling into `crates/opensks-native-collaboration`, added Codex App multi-agent proof-contract support for beta-006, recorded completed/closed Codex App subagents in hash-bound mission artifacts, and verified it with `cargo test beta006 --locked -- --test-threads=1`, `cargo test mvp004 --locked -- --test-threads=1`, `cargo clippy --all-targets -- -D warnings`, `cargo fmt --all --check`, `cargo run -- bench`, `cargo run -- acceptance audit`, `cargo run -- security audit`, `bash scripts/check-architecture-ownership.sh`, `sks validate-artifacts M-20260625-043232-80db`, `sks wiki validate .sneakoscope/wiki/context-pack.json`, and `git diff --check`. The next mvp-004 slice added explicit non-secret provider adapter-check blockers to the CLI stdout, top-level report, and per-adapter rows: current local blocker evidence is `set_OPENSKS_ALLOW_REMOTE_PROVIDER_PROBE_1`, `configure_OPENROUTER_API_KEY_credential`, and `configure_OPENAI_API_KEY_credential`. Focused regressions prove additive empty blocker fields do not prevent a real passing mvp-004 fixture, secret-like blocker text keeps mvp-004 partial, and secret values still do not serialize. The Swift Provider Center/composer readiness follow-up now decodes `.opensks/providers/provider-adapter-check.json` into optional typed Swift state, tolerates older reports without `blockers`, maps known non-secret blocker codes into Provider Center and Chat readiness text, redacts unknown blocker strings before UI/view-model exposure, and keeps raw stderr out of the UI; it was verified with `swift test --package-path swift --filter OpenSKSStudioTests.ProviderTests` (10 tests, including Provider Center render), `swift test --package-path swift --filter OpenSKSStudioTests.ConversationTurnTests`, `cargo test mvp004 --locked -- --test-threads=1`, `cargo run -- acceptance audit`, and `git diff --check`. The latest CI performance follow-up wires the GitHub Actions workflow to persist the 100k perf-stress JSON artifact and locally verifies the same contract. The current follow-up removes the remaining product direct parent-directory creation from `artifact.write`, reruns direct-I/O/architecture guards, captures read-only Computer Use state from the running native app, verifies budget propagation plus integration apply path-scope proof with focused Rust/Swift tests, schema generation, fmt, clippy, acceptance audit, and diff whitespace, and then closes the old formatter availability gap by running the Xcode toolchain `swift-format` lint successfully without applying broad auto-format churn. The latest `cargo run -- acceptance audit` reports 23 total, 22 passed, 1 partial, 0 failed; beta and production tiers pass, while MVP remains partial only for live remote provider adapter reachability (`mvp-004`). Current `cargo run -- release proof` writes `.opensks/release/release-proof.json` with `status: not_verified`, no missing artifacts, and one explicit blocker: `workspace_dirty`, which prevents same-SHA artifact binding and keeps the artifact digest gate false until release sealing. Full work-order completion is still not proven because `mvp-004` needs real OpenRouter/OpenAI credentials plus `OPENSKS_ALLOW_REMOTE_PROVIDER_PROBE=1`, the Swift formatting warning backlog remains unmodified, GitHub-hosted CI artifact upload has not been observed from an actual Actions run, native-app manual release smoke remains state-only rather than a successful end-to-end run, live vendor credential smoke is unverified, and production signing/notarization proof remains unverified.

### Cross-cutting - Thread Settings Legacy Echo Cleanup

- status: Verified
- owner file/module: `crates/opensks-contracts/src/turn.rs`, `crates/opensks-contracts/src/lib.rs`, `crates/opensks-conversation/src/lib.rs`, `crates/opensks-daemon/src/lib.rs`, Swift conversation DTO/service/store/tests, `schemas/conversation-turn-start-request.schema.json`, `schemas/engine-request.schema.json`
- current evidence: `ConversationTurnStartRequest.settings` was still a required compatibility echo even though runtime settings are snapshotted from durable thread settings plus `thread_settings_updated_at_ms`. Swift Chat also still sent the duplicate settings payload on product turn-start.
- target change: make the `settings` echo optional, keep legacy decode compatibility, stop Swift product send from serializing it, and prove persisted thread settings remain the runtime SSOT.
- tests: `cargo test -p opensks-contracts conversation_turn_start --locked`, `cargo test -p opensks-contracts turn_request_decodes_without_legacy_settings_echo --locked`, `cargo test -p opensks-conversation accept_conversation_turn_snapshots_persisted_thread_settings --locked`, `cargo test -p opensks-daemon conversation_turn_start_snapshots_persisted_thread_settings --locked -- --test-threads=1`, `swift test --disable-sandbox --package-path swift --scratch-path /tmp/opensks-swift-build-contracts --filter OpenSKSStudioTests.ContractsTests/testConversationTurnStartRequestEncodesTypedPayload`, `swift test --disable-sandbox --package-path swift --scratch-path /tmp/opensks-swift-build-turn --filter OpenSKSStudioTests.ConversationTurnTests/testSendPassesThreadSettingsAndContextRefs`, `cargo run -p xtask -- schemas`, `cargo clippy -p opensks-contracts -p opensks-conversation -p opensks-daemon --all-targets -- -D warnings`, `cargo fmt --all --check`, and `git diff --check` passed.
- migration: generated schemas now remove `settings` from `conversation-turn-start-request` required fields and model it as nullable optional compatibility data; `engine-request` schema updates through the nested request reference.
- removal/deletion: Swift product Chat no longer sends the duplicated turn settings echo on start; legacy clients can still send it, and focused Rust tests prove it is ignored in favor of persisted thread settings.
- final evidence: `.sneakoscope/evidence/thread-settings-legacy-echo-evidence.json` records the focused verification and Computer Use app-running observation. Full workspace reruns were not repeated for this narrow cleanup slice; the release-level gaps listed under PR-100 still apply.

### Cross-cutting - Provider Adapter Check Registry Keychain Path

- status: Verified
- owner file/module: `src/lib.rs`, `src/tests.rs`
- current evidence: `provider adapter-check` wrote correct non-secret blockers, but its credential status collection only used environment variables and a legacy Keychain account name. Provider Center saves provider credentials as provider registry `SecretRef`s (`ai.opensks.provider.<kind>` service and provider-id account), so a user could have a valid product credential while `adapter-check` still reported `configure_OPENROUTER_API_KEY_credential` / `configure_OPENAI_API_KEY_credential`.
- target change: connect `provider adapter-check`, auth evidence, and provider status artifacts to the workspace provider registry Keychain refs without serializing secrets, while keeping env override and legacy Keychain compatibility.
- tests: `cargo test provider_adapter_check_uses_registry_keychain_secret_ref_without_serializing_secret --locked -- --test-threads=1`, `cargo test provider_keychain --locked -- --test-threads=1`, `cargo test provider_env_source_overrides_keychain_without_serializing_secrets --locked -- --test-threads=1`, `cargo test provider_commands_write_zero_leak_registry_probe_and_usage --locked -- --test-threads=1`, `cargo run -- provider adapter-check`, `cargo run -- acceptance audit`, `cargo clippy --all-targets -- -D warnings`, `cargo fmt --all --check`, `git diff --check`, `sks validate-artifacts latest`, and `sks wiki validate .sneakoscope/wiki/context-pack.json` passed.
- migration: no schema migration. `credential_source` values can now include `provider_registry_keychain:<provider_id>` in local provider status/adapter-check artifacts; Swift already treats unknown blocker/source text defensively and does not expose secret values.
- removal/deletion: none. This is additive and preserves env-first behavior plus the legacy `opensks-provider-credentials` Keychain fallback.
- final evidence: `.sneakoscope/evidence/provider-adapter-check-registry-keychain-evidence.json` records the focused verification. Local acceptance remains 23 total, 22 passed, 1 partial, 0 failed because no real OpenRouter/OpenAI credentials and no `OPENSKS_ALLOW_REMOTE_PROVIDER_PROBE=1` were available for a live `/models` probe; the partial is now more directly actionable through the same Provider Center Keychain path.

### Cross-cutting - Swift Format Toolchain Gate

- status: Verified
- owner file/module: Swift formatting release gate, Xcode toolchain
- current evidence: earlier local runs recorded `swift-format` as unavailable because it was not on PATH.
- target change: locate and execute the installed Xcode toolchain `swift-format` binary without rewriting broad Swift files.
- tests: `/Applications/Xcode.app/Contents/Developer/Toolchains/XcodeDefault.xctoolchain/usr/bin/swift-format lint --recursive swift/Sources swift/Tests` exited 0; output was captured to `.sneakoscope/evidence/swift-format-lint-20260625.log` with 34,605 warning lines from the existing Swift tree. `cargo run -- acceptance audit` still reports 23 total, 22 passed, 1 partial, 0 failed, and `git diff --check` passed after the lint run.
- migration: none.
- removal/deletion: the previous "swift-format not installed in this shell" blocker is superseded for this environment by the Xcode toolchain path. The Swift warning backlog remains separate; no sweeping auto-format was applied because it would touch broad existing/user-edited Swift files.
- final evidence: `.sneakoscope/evidence/swift-format-toolchain-lint-evidence.json` records the executable path, command, exit code, and log reference.

### Cross-cutting - Release Proof SHA Binding Seal

- status: Verified for SHA binding; release signing remains NotVerified
- owner file/module: release hardening evidence, Git history, `.opensks/release/release-proof.json`
- current evidence: the tracked implementation diff had previously kept `cargo run -- release proof` at `status: NotVerified` with an explicit `workspace_dirty` blocker, `artifact_digest_gate_passed: false`, and `same_sha_artifact_binding: false`.
- target change: seal the verified tracked implementation diff into Git history so release proof artifacts can bind to a stable commit SHA without claiming production signing or notarization.
- tests: before sealing, `cargo fmt --all --check`, `git diff --check`, `cargo test provider_commands_write_zero_leak_registry_probe_and_usage --locked -- --test-threads=1`, and `swift test --disable-sandbox --package-path swift --scratch-path /tmp/opensks-swift-build-provider-readiness --filter OpenSKSStudioTests.ProviderTests` passed. After commit `c0c9a98 Implement provider runtime release hardening`, `cargo run -- release proof` reported `blockers: 0`, `artifact_digest_gate_passed: true`, and `same_sha_artifact_binding: true`; `cargo run -- acceptance audit` remained 23 total, 22 passed, 1 partial, 0 failed; `sks validate-artifacts latest` passed.
- migration: none.
- removal/deletion: no source removal in this seal step. The existing untracked backup file was intentionally left untouched and is not part of the release-proof tracked dirty blocker.
- final evidence: release proof no longer has a tracked dirty-workspace blocker, but full release completion is still not proven because `signed_app: false` keeps `status: NotVerified`, live remote provider smoke still requires real OpenRouter/OpenAI credentials plus `OPENSKS_ALLOW_REMOTE_PROVIDER_PROBE=1`, and GitHub-hosted CI artifact upload plus production notarization remain unverified.

### Cross-cutting - Release Verification Blocker Surfacing

- status: Verified
- owner file/module: `crates/opensks-retention/src/lib.rs`, `crates/opensks-cli/src/retention.rs`, `.opensks/release/release-proof.json`
- current evidence: after SHA binding was sealed, `cargo run -- release proof` could report `blockers: 0` while the proof remained `status: NotVerified` because `signed_app`, `notarized`, and `upgrade_checked` were false. That made the remaining production release actions less actionable even though the raw boolean fields were present.
- target change: preserve the artifact/SHA digest gate as an artifact-only proof (`artifact_digest_gate_passed: true` when required artifacts bind to a clean HEAD) while adding explicit release verification blockers for missing production app signing, macOS notarization, fresh install/clone, and upgrade receipts. This keeps NotVerified honest without erasing the SHA-binding progress.
- tests: `cargo test -p opensks-retention release_proof --locked`, `cargo test -p opensks-cli release_proof --locked`, `cargo fmt --all --check`, `cargo clippy -p opensks-retention -p opensks-cli --all-targets -- -D warnings`, `git diff --check`, `cargo run -- release proof`, and `cargo run -- acceptance audit` passed. On clean HEAD `ec0dd93`, `cargo run -- release proof` now reports `artifact_digest_gate_passed: true`, `same_sha_artifact_binding: true`, and three explicit blockers: `signed_app_missing`, `notarization_missing`, and `upgrade_unverified`.
- migration: no schema migration; the existing defaulted `blockers` array now carries release verification blockers in addition to artifact/workspace blockers.
- removal/deletion: no product runtime removal. This is a reporting/evidence-contract hardening change.
- final evidence: release proof now exposes the concrete remaining release blockers instead of showing zero blockers with a NotVerified status. Full work-order completion remains unproven until production signing, notarization, upgrade proof, live provider smoke credentials/opt-in, and hosted CI artifact evidence are available.

### Cross-cutting - Release Proof Updater Evidence Binding

- status: Verified for local updater evidence; production signing/notarization remain NotVerified
- owner file/module: `crates/opensks-cli/src/retention.rs`, `.opensks/updater/*`, `.opensks/release/release-proof.json`
- current evidence: `updater plan` already produced a local signed-update manifest artifact set that passes production acceptance `prod-006`, but `cargo run -- release proof` still hardcoded `upgrade_checked: false` and kept `upgrade_unverified` even when the updater evidence was present and valid.
- target change: make release proof validate the existing `.opensks/updater` artifact set before setting `upgrade_checked`. The validator checks manifest schema/version/channels/artifacts/signature/rollback/boundary/final-state fields, recomputes the local fnv1a64 manifest signature, requires rollback and operator approval, and requires `network_or_install_performed: false` so it does not pretend a live production upgrade happened.
- tests: `cargo test -p opensks-cli release_proof --locked`, `cargo fmt --all --check`, `cargo clippy -p opensks-cli --all-targets -- -D warnings`, `git diff --check`, `cargo run -- updater plan`, `cargo run -- release proof`, `cargo run -- acceptance audit`, and `sks validate-artifacts latest` passed. On clean HEAD `57c4801`, `cargo run -- release proof` reports `upgrade_checked: true` in the JSON artifact, `artifact_digest_gate_passed: true`, `same_sha_artifact_binding: true`, and only two blockers: `signed_app_missing` and `notarization_missing`.
- migration: no schema migration. Existing release proof consumers already decode `upgrade_checked` and `blockers`.
- removal/deletion: removed no runtime path; this only connects release proof to the already-generated updater SSOT artifact set.
- final evidence: local upgrade-path artifact evidence is now bound into release proof. The proof remains correctly `NotVerified` because the current app bundle is only ad-hoc signed and no macOS notarization receipt is present.

### Cross-cutting - Release Signing Diagnostic Evidence

- status: Verified for diagnostic evidence; production signing/notarization remain NotVerified
- owner file/module: `crates/opensks-contracts/src/lib.rs`, `crates/opensks-retention/src/lib.rs`, `crates/opensks-cli/src/retention.rs`, `schemas/release-proof.schema.json`, `.opensks/release/release-proof.json`
- current evidence: release proof already blocked on `signed_app_missing` and `notarization_missing`, but it did not preserve structured evidence explaining what the local macOS app bundle actually reported. Manual `codesign` showed an ad-hoc signature and no team identifier, while `spctl` did not produce an accepted/notarized result.
- target change: add defaulted optional `signing_evidence` to `opensks.release-proof.v1` and have `cargo run -- release proof` inspect `.opensks/macos/OpenSKS.app` with `codesign` and `spctl`. The proof now records app path, identifier, signature kind, team identifier, CDHash, probe statuses, production-signed boolean, notarized boolean, and a concise diagnostic without marking ad-hoc signing as production signing.
- tests: `cargo test -p opensks-cli release_proof --locked`, `cargo test -p opensks-retention release_proof --locked`, `cargo test -p opensks-contracts release_proof --locked`, `cargo fmt --all --check`, `cargo clippy -p opensks-cli -p opensks-contracts -p opensks-retention --all-targets -- -D warnings`, `cargo run -p xtask -- schemas`, `git diff --check`, `cargo run -- release proof`, `cargo run -- acceptance audit`, and `sks validate-artifacts latest` passed. On clean HEAD `36982cf`, release proof records `signing_evidence.checked: true`, `signature: adhoc`, `team_identifier: not set`, `production_signed: false`, `notarized: false`, `codesign_status: 0`, and `notarization_status: 1`.
- migration: generated `schemas/release-proof.schema.json` now includes optional/defaulted `signing_evidence`, so older proof JSON without the field remains decodable.
- removal/deletion: no runtime removal. This is an evidence-contract hardening change.
- final evidence: release proof now explains the remaining signing/notarization blockers with local app-bundle probe evidence. Full work-order completion remains unproven until a production-signed and notarized app bundle is available, plus live provider smoke credentials/opt-in.

### Cross-cutting - Provider Adapter Remediation Actions

- status: Verified for local action evidence; live provider smoke remains partial
- owner file/module: `src/lib.rs`, `src/tests.rs`, `swift/Sources/Providers/ProviderService.swift`, `swift/Tests/OpenSKSStudioTests/ProviderTests.swift`, `.opensks/providers/provider-adapter-check.json`
- current evidence: provider adapter-check exposed non-secret blocker codes and Swift mapped those codes into user-facing text, but the canonical backend report did not carry structured remediation actions. Operators had to know or reimplement the code-to-action mapping outside the provider adapter-check artifact.
- target change: add top-level `remediation_actions` to `opensks.provider-adapter-check.v1` reports while preserving the existing `blockers` codes for acceptance and automation. Each action records the blocker code, a safe action string, and a scope such as `operator_environment` or `provider_credential`; unknown blockers map to a redacted diagnostic action. Swift decodes the optional field while remaining backward compatible with older reports that lack `blockers` or actions.
- tests: `cargo test provider_commands_write_zero_leak_registry_probe_and_usage --locked -- --test-threads=1`, `swift test --disable-sandbox --package-path swift --scratch-path /tmp/opensks-swift-build-provider-actions --filter OpenSKSStudioTests.ProviderTests`, `cargo clippy --all-targets -- -D warnings`, `cargo fmt --all --check`, `git diff --check`, `cargo run -- provider adapter-check`, `cargo run -- release proof`, and `cargo run -- acceptance audit` passed. On clean HEAD `b902e2e`, `provider-adapter-check.json` includes action entries for `set_OPENSKS_ALLOW_REMOTE_PROVIDER_PROBE_1`, `configure_OPENROUTER_API_KEY_credential`, and `configure_OPENAI_API_KEY_credential` without any secret value.
- migration: additive report field only. Swift uses `decodeIfPresent` with an empty default, so older reports stay decodable.
- removal/deletion: none.
- final evidence: the remaining `mvp-004` partial is more actionable from the canonical backend artifact, but it still honestly requires real OpenRouter/OpenAI credentials plus `OPENSKS_ALLOW_REMOTE_PROVIDER_PROBE=1` for a 2xx live `/models` probe.

### Cross-cutting - Provider Adapter Check Schema

- status: Verified
- owner file/module: `crates/opensks-contracts/src/lib.rs`, `schemas/provider-adapter-check.schema.json`, `crates/opensks-cli/src/retention.rs`
- current evidence: `provider-adapter-check.json` declared `schema: opensks.provider-adapter-check.v1`, and the product now emits `remediation_actions`, but there was no generated JSON Schema artifact for that report shape.
- target change: add a typed `ProviderAdapterCheckReport` contract with timestamp, summary, blockers, remediation actions, adapter rows, secret-leak flags, and backward-compatible defaults; generate `schemas/provider-adapter-check.schema.json`; bind that schema into release proof required artifact digests.
- tests: `cargo test -p opensks-contracts provider_adapter_check_report_decodes_remediation_actions --locked`, `cargo run -p xtask -- schemas`, `cargo test -p opensks-cli release_proof --locked`, `cargo clippy -p opensks-contracts --all-targets -- -D warnings`, `cargo clippy -p opensks-cli --all-targets -- -D warnings`, `cargo fmt --all --check`, `git diff --check`, `cargo run -- release proof`, `cargo run -- provider adapter-check`, and `cargo run -- acceptance audit` passed. On clean HEAD `04848e3`, release proof includes required artifact `provider_adapter_check_schema` with path `schemas/provider-adapter-check.schema.json`, a SHA-256 digest, and the current source commit SHA.
- migration: additive generated schema and defaulted contract fields; existing reports without blockers/actions remain decodable by Swift and by the Rust contract defaults.
- removal/deletion: none.
- final evidence: provider adapter-check now has a generated schema and same-SHA release-proof binding. Live adapter reachability remains partial until real provider credentials and remote probe opt-in are present.

Follow-up evidence: `cargo test provider_commands_write_zero_leak_registry_probe_and_usage --locked -- --test-threads=1`, `cargo test -p opensks-contracts provider_adapter_check_report_decodes_remediation_actions --locked`, `cargo clippy -p opensks-contracts --all-targets -- -D warnings`, `cargo clippy --all-targets -- -D warnings`, `cargo fmt --all --check`, `git diff --check`, `cargo run -- release proof`, `cargo run -- provider adapter-check`, and `cargo run -- acceptance audit` passed after the product provider command regression was updated to decode the generated `provider-adapter-check.json` through `opensks_contracts::ProviderAdapterCheckReport`. On clean HEAD `57dd066`, the product renderer, Rust contract, Swift optional decoder, generated schema, and release-proof digest are all bound to the same report shape.

### Cross-cutting - macOS App Icon Generation and Full Workspace Test Gate

- status: Verified
- owner file/module: `src/lib.rs`, macOS app bundle launcher, workspace test gate
- current evidence: `cargo test --workspace --locked` on macOS exposed that `empty_args_creates_native_app_bundle` could fail when QuickLook SVG thumbnail rendering (`qlmanage`) hit a local sandbox error while generating icon PNGs. The app bundle path was otherwise valid, but the external renderer made the launcher test environment-sensitive.
- target change: replace QuickLook-dependent icon PNG generation with deterministic in-process PNG and ICNS writers for the OpenSKS launcher resources. The bundle still writes the canonical `opensks-logo.svg`; `AppIcon.icns` is now built from generated PNG entries without shelling out to `qlmanage` or `iconutil`.
- tests: `cargo test -p opensks empty_args_creates_native_app_bundle --locked -- --test-threads=1` passed; `file` recognized the generated `AppIcon.icns` as `Mac OS X icon`; `cargo clippy --workspace --all-targets --all-features -- -D warnings` passed; `cargo test --workspace --locked` passed when run with localhost bind permission for daemon integration tests.
- migration: no data migration. Existing app bundles are regenerated by the launcher path.
- removal/deletion: removed the runtime dependency on external icon rendering/conversion commands for launcher icon creation.
- final evidence: the macOS launcher no longer fails because of QuickLook/iconutil sandbox behavior, and the full Rust workspace test suite now passes in the verified environment.

### Cross-cutting - Provider Test Socket Fixture Hardening

- status: Verified
- owner file/module: `crates/opensks-adapter/src/openrouter.rs`, `crates/opensks-cli/src/provider_registry.rs`, `crates/opensks-provider/src/lib.rs`
- current evidence: sandboxed `cargo test --workspace --locked` failed in adapter image and CLI provider-registry tests because local TCP listener creation returned `Operation not permitted`. Those tests were verifying request/response contracts and secretless registry sync, not network stack behavior.
- target change: add a test-only in-process HTTP responder for `OpenAiCompatibleImageGenerator` so image generation/vision tests still assert endpoint path, bearer header, model, prompt, size, and data URL payloads without binding sockets. Expose the OpenAI-compatible model parser from `opensks-provider` and add test-only registry-probe fixture options in `opensks-cli` so the CLI test still resolves the mock Keychain credential, records a `ProviderProbeReceipt`, syncs parsed models, and proves no secret appears in stdout.
- tests: `cargo test -p opensks-adapter openai_compatible_image_generator --locked -- --test-threads=1`, `cargo test -p opensks-cli registry_probe_resolves_keychain_ref_and_syncs_models_secretlessly --locked -- --test-threads=1`, `cargo clippy -p opensks-adapter -p opensks-cli -p opensks-provider --all-targets -- -D warnings`, `cargo fmt --all --check`, and `git diff --check` passed.
- migration: no runtime migration. The adapter responder and CLI probe fixture flags are compiled only for tests; production provider calls still use `reqwest` and the existing provider registry path.
- removal/deletion: removed test-only TCP server helpers from adapter image tests and CLI registry probe tests. Daemon integration tests still exercise real localhost HTTP and were verified with bind permission.
- final evidence: provider adapter/image/registry unit tests no longer depend on local socket permissions, while full daemon integration remains covered by the privileged full workspace test run.

### Cross-cutting - Release Proof Remediation Actions

- status: Verified locally; production signing/notarization remain NotVerified
- owner file/module: `crates/opensks-contracts/src/lib.rs`, `crates/opensks-retention/src/lib.rs`, `crates/opensks-cli/src/retention.rs`, `schemas/release-proof.schema.json`
- current evidence: release proof exposed explicit blockers for missing production signing and notarization, but the proof did not carry structured remediation actions. Operators had to infer the next action from blocker text.
- target change: add defaulted `remediation_actions` to `opensks.release-proof.v1`. Each action maps a blocker code to a safe action string and a scope such as `release_signing`, `release_verification`, `source_control`, or `release_artifact`. The status remains `NotVerified`; the new field only makes the remaining evidence gap executable.
- tests: `cargo test -p opensks-contracts release_proof_decodes_remediation_actions --locked`, `cargo test -p opensks-retention unsigned_release_proof_is_not_verified --locked`, `cargo test -p opensks-cli release_proof_binds_required_artifacts_to_clean_head --locked`, `cargo run -p xtask -- schemas`, `cargo clippy -p opensks-contracts -p opensks-retention -p opensks-cli --all-targets -- -D warnings`, `cargo fmt --all --check`, `git diff --check`, and `cargo run -- release proof` passed during implementation. The dirty-workspace proof showed actions for `workspace_dirty`, `signed_app_missing`, and `notarization_missing` before commit.
- migration: additive/defaulted proof field; older release proof JSON without `remediation_actions` remains decodable.
- removal/deletion: none.
- final evidence: release proof now carries structured next actions for the remaining blockers. Full release completion still requires a production Developer ID signed app and Apple notarization evidence.

### Cross-cutting - Release Proof App Evidence Surface

- status: Verified for data/model/UI render path; packaged app relaunch verified by later macOS launch hardening evidence
- owner file/module: `src/lib.rs`, `src/tests.rs`, `swift/Sources/Models.swift`, `swift/Sources/Runs/EvidenceWorkspaceView.swift`, `swift/Tests/OpenSKSStudioTests/ContractsTests.swift`, `swift/Tests/OpenSKSStudioTests/ReleaseReadinessTests.swift`
- current evidence: `opensks.release-proof.v1` now carries structured blockers and remediation actions, but `opensks-cli app-data` only exposed acceptance and GUI posture to Swift, so the Evidence workspace could not show the concrete release blockers without shelling out or duplicating file reads.
- target change: add a `release` section to `opensks.app-data.v1` from `.opensks/release/release-proof.json`, keep missing/invalid proof files non-fatal, decode the section in Swift as optional data, and render release proof status, blocker rows, and remediation action rows in Evidence.
- tests: `cargo test app_data_ --locked -- --test-threads=1`, `cargo test app_data_exposes_release_proof_remediation_actions --locked -- --test-threads=1`, `cargo run -- app-data . | jq -e '.release.status == "not_verified" and (.release.remediation_actions | length) == 2'`, `cargo clippy -p opensks --all-targets -- -D warnings`, `cargo fmt --all --check`, `git diff --check`, `swift test --disable-sandbox --package-path swift --scratch-path /tmp/opensks-swift-build-release-app-data --filter OpenSKSStudioTests.ContractsTests/testAppDataDecodesReleaseProofRemediationActions`, and `swift test --disable-sandbox --package-path swift --scratch-path /tmp/opensks-swift-build-release-app-data --filter OpenSKSStudioTests.ReleaseReadinessTests/testEvidenceWorkspaceRendersReleaseProofActions` passed. `swift-format lint --recursive swift/Sources swift/Tests` exited 0 from the Xcode toolchain path but emitted the repository's existing broad formatting warning backlog, so no auto-format churn was applied. Computer Use observed the existing OpenSKS window and current proof/footer state, but the rebuilt `.opensks/macos/OpenSKS.app` could not be relaunched through LaunchServices after local rebuild: `open` returned `kLSNoExecutableErr`, `spctl` reported an internal Code Signing subsystem error, and ad-hoc signing failed on the root SwiftPM resource bundle. SwiftUI render tests verify the new card path until the packaging/signing gap is addressed.
- migration: additive `app-data` field only; Swift decodes the field as optional so older app-data remains compatible.
- removal/deletion: none.
- final evidence: `opensks-cli app-data` now exposes release status `not_verified`, blockers `signed_app_missing` and `notarization_missing`, and two `release_signing` remediation actions to Swift. Evidence renders those release blockers/actions from decoded `AppData` without fabricating release completion. Later macOS app launch hardening regenerated, signed, relaunched, and Computer-Use verified the same app-data surface in a fresh OpenSKS window.

### Cross-cutting - macOS App Resource Bundle Signing Hygiene

- status: Verified locally; production Developer ID signing/notarization remain separate release blockers
- owner file/module: `src/lib.rs`, `src/tests.rs`, `swift/Sources/Branding/BrandAssetLoader.swift`
- current evidence: rebuilding `.opensks/macos/OpenSKS.app` for Computer Use validation exposed that the generated SwiftPM resource bundle was copied into the app root and `Contents/MacOS`, which made `codesign --force --deep --sign -` fail with unsealed root contents or an invalid nested resource bundle.
- target change: load brand resources from `Contents/Resources/OpenSKSStudio_OpenSKSStudio.bundle` before falling back to `Bundle.module`, copy SwiftPM resource bundles only into `Contents/Resources`, remove stale app-root/`Contents/MacOS` resource bundles during regeneration, and write `Contents/PkgInfo` for stricter LaunchServices scanners.
- tests: `cargo test -p opensks empty_args_creates_native_app_bundle --locked -- --test-threads=1`, `cargo clippy -p opensks --all-targets -- -D warnings`, `cargo fmt --all --check`, and `swift test --disable-sandbox --package-path swift --scratch-path /tmp/opensks-swift-build-release-app-data --filter OpenSKSStudioTests.BrandingTests` passed. Rebuilt `.opensks/macos/OpenSKS.app` now has resource bundles only under `Contents/Resources`; `codesign --force --deep --sign - .opensks/macos/OpenSKS.app` succeeds and `codesign --verify --deep --strict --verbose=2` reports valid on disk.
- migration: generated app bundle layout only; no user data migration.
- removal/deletion: stale generated resource-bundle copies under the app root and `Contents/MacOS` are removed on regeneration.
- final evidence: local code-signing structure is healthier. Follow-up validation after the sandbox was disabled regenerated the bundle, `codesign --force --deep --sign - .opensks/macos/OpenSKS.app` succeeded, `codesign --verify --deep --strict --verbose=2` reported valid on disk, and `open -n .opensks/macos/OpenSKS.app` launched a fresh window. Production Developer ID signing and notarization are still intentionally reported as release-proof blockers.

### Cross-cutting - macOS App Launch Data Loading Hardening

- status: Verified for fresh launch data loading, bounded failure surfacing, and recovery affordance
- owner file/module: `src/main.rs`, `src/lib.rs`, `src/tests.rs`, `swift/Sources/Runtime/OpenSKSCLIProcess.swift`, `swift/Sources/Runtime/ProcessSupervisor.swift`, `swift/Sources/Backend.swift`, Swift live CLI service wrappers, `swift/Sources/StatusBarView.swift`
- current evidence: after app bundle signing hygiene, `open -n .opensks/macos/OpenSKS.app` launched the rebuilt app, but app-launched `opensks-cli` helpers inherited LaunchServices app privacy context and hung in startup `getcwd()` or later file opens under `~/Desktop/devs/opensks`. The UI stayed at `0 providers`, `Workspace`, `Loading conversations`, and footer `loading...`, while helper processes accumulated.
- target change: make CLI workspace resolution explicit and bounded for app-launched helpers. Rust CLI startup now prefers `OPENSKS_WORKSPACE`, any `--workspace <path>`, or `app-data <workspace>` before calling `current_dir()`. The binary-owned CLI cwd helper stays private in `src/main.rs`, while the test-only `src/lib.rs` helpers are hidden behind `#[cfg(test)]` so architecture ownership does not grow the crate public API. Swift OpenSKS CLI invocations now set `OPENSKS_WORKSPACE`, run from a temporary cwd, and apply a 12-second timeout. `ProcessSupervisor` now has an environment overlay and escalates timeout/cancel cleanup from terminate to SIGKILL fallback. `CLIRunner.capture` uses the same supervisor instead of an unbounded pipe read. Proof artifact refresh uses metadata fingerprints instead of reading proof file contents. The status bar now reports `workspace unavailable` when app-data fails instead of continuing to say `loading...`, and exposes an icon-only folder access action that asks the operator to reselect the active workspace through `NSOpenPanel`; only the exact current workspace path is accepted before rescanning and reloading data. The recovery panel opens at the workspace parent so the active folder itself can be selected. The app generator also accepts `OPENSKS_SWIFT_DISABLE_SANDBOX=1` to pass `swift build --disable-sandbox` for Codex/seatbelt validation environments, without changing the default SwiftPM sandbox behavior.
- tests: `cargo test -p opensks workspace_override --locked`, `cargo test -p opensks default_cli_cwd_prefers_workspace_environment --locked`, `CLANG_MODULE_CACHE_PATH=/tmp/opensks-clang-module-cache MODULE_CACHE_DIR=/tmp/opensks-clang-module-cache swift build --disable-sandbox --package-path swift --scratch-path /tmp/opensks-swift-build-release-app-data`, `swift test --disable-sandbox --package-path swift --scratch-path /tmp/opensks-swift-build-release-app-data --filter OpenSKSStudioTests.ProcessSupervisorTests/testMergesEnvironmentOverlay`, `swift test --disable-sandbox --package-path swift --scratch-path /tmp/opensks-swift-build-release-app-data --filter OpenSKSStudioTests.ProcessSupervisorTests/testTimeoutTerminatesChild`, `swift test --disable-sandbox --package-path swift --scratch-path /tmp/opensks-swift-build-release-app-data --filter OpenSKSStudioTests.RuntimeHardeningTests/testFileScannerHonorsNodeBudget`, `swift test --disable-sandbox --package-path swift --scratch-path /tmp/opensks-swift-build-release-app-data --filter OpenSKSStudioTests.RuntimeHardeningTests/testProofArtifactFingerprintChangesWhenAcceptanceSummaryChanges`, `CLANG_MODULE_CACHE_PATH=/tmp/opensks-clang-module-cache MODULE_CACHE_DIR=/tmp/opensks-clang-module-cache swift build --disable-sandbox --package-path swift --scratch-path /tmp/opensks-swift-build-workspace-access`, `swift test --disable-sandbox --package-path swift --scratch-path /tmp/opensks-swift-build-workspace-access-tests --filter OpenSKSStudioTests.RuntimeHardeningTests/testFileScannerHonorsNodeBudget`, `swift test --disable-sandbox --package-path swift --scratch-path /tmp/opensks-swift-build-statusbar-access --filter OpenSKSStudioTests.ReleaseReadinessTests/testStatusBarRendersWorkspaceAccessRecoveryControl`, `swift test --disable-sandbox --package-path swift --scratch-path /tmp/opensks-swift-build-workspace-panel --filter OpenSKSStudioTests.RuntimeHardeningTests/testWorkspaceAccessPanelStartsAtWorkspaceParent`, `cargo test -p opensks empty_args_creates_native_app_bundle --locked -- --test-threads=1`, `cargo clippy -p opensks --all-targets -- -D warnings`, `cargo fmt --all --check`, `bash scripts/check-architecture-ownership.sh`, and `git diff --check` passed during this slice. `OPENSKS_SWIFT_DISABLE_SANDBOX=1 cargo run --` regenerated `.opensks/macos/OpenSKS.app`; the rebuilt app was ad-hoc signed and `codesign --verify --deep --strict --verbose=2` reported valid on disk. A follow-up architecture rerun caught accidental public functions `default_cli_cwd` and `workspace_override_from_args`; after moving the runtime helper into `src/main.rs` and gating the library helpers to tests, `bash scripts/check-architecture-ownership.sh`, `cargo fmt --all --check`, `cargo test -p opensks workspace_override --locked`, `cargo test -p opensks default_cli_cwd_prefers_workspace_environment --locked`, and `git diff --check` passed.
- migration: additive launch/env behavior only; no user data migration. The generated app bundle must be regenerated to pick up the Swift/Rust launch hardening.
- removal/deletion: removed no product feature. Generated stale stuck helper processes were manually killed during validation; the persistent daemon remains expected while the app is open.
- final evidence: after unrestricted verification, stale OpenSKS processes were terminated, `.opensks/macos/OpenSKS.app` was regenerated, ad-hoc signed, and opened with `open -n`. Computer Use observed fresh pid `92999` loading real workspace data: title `OpenSKS Studio ~/Desktop/devs/opensks`, titlebar `1 provider`, conversation sidebar populated, model/image/worktree controls visible, and footer `~/Desktop/devs/opensks 22 passed 1 partial 0 failed In progress 14 missions · 424 voxels`. After the timeout window, process inspection showed only the app and expected daemon helper remained. Production release completion still requires real provider credentials/remote probe opt-in and Developer ID signing/notarization evidence.

### Cross-cutting - Secret Scan Fixture Literal Hardening

- status: Verified
- owner file/module: `crates/opensks-conversation/src/lib.rs`, `crates/opensks-daemon/src/lib.rs`
- current evidence: the final security audit rerun after macOS app verification reported `secret_findings: 2` even though `security_findings: 0`. The findings were test-only fixture literals matching an `OPENAI_API_KEY` assignment pattern in daemon raw-prompt restoration/redaction tests and conversation encrypted raw-content tests.
- target change: keep the runtime redaction/encrypted-raw tests exercising secret-like API-key assignment strings, but construct the assignment through test helpers (`OPENAI_API_KEY` plus `=` plus fixture value) so source and security artifacts do not contain direct secret-assignment literals. The tests still execute the same redaction and raw-prompt restore paths and still assert that secret-like values are absent from projected messages/events.
- tests: the exact `OPENAI_API_KEY` assignment literal scan returned no matches in `crates/opensks-daemon/src/lib.rs` or `crates/opensks-conversation/src/lib.rs`; `cargo fmt --all --check`, `cargo test -p opensks-conversation encrypted_raw_content_is_separate_from_redacted_read_models --locked`, `cargo test -p opensks-daemon conversation_supervisor_tick_blocks_redacted_prompt_without_raw_vault --locked -- --test-threads=1`, `cargo test -p opensks-daemon conversation_supervisor_tick_restores_encrypted_raw_prompt_for_execution --locked -- --test-threads=1`, `cargo test -p opensks-daemon conversation_turn_start_encrypts_raw_prompt_for_later_supervisor_restore --locked -- --test-threads=1`, `cargo clippy -p opensks-conversation -p opensks-daemon --all-targets -- -D warnings`, `git diff --check`, and `cargo run -- security audit` passed. The latest security audit reports `secret_findings: 0` and `security_findings: 0`.
- migration: no runtime migration; test-only fixture construction changed.
- removal/deletion: removed no test coverage. Direct source literals that looked like assigned provider secrets were removed.
- final evidence: `.opensks/security/security-audit.json` and `.opensks/security/secret-leak-gate.json` now report zero current secret findings while the targeted raw-prompt/encrypted-content tests continue to pass.

### Cross-cutting - Stream Protocol Capability Truth

- status: Verified
- owner file/module: `crates/opensks-contracts/src/capability.rs`, `crates/opensks-cli/src/capability.rs`, `docs/runtime-truth-matrix.generated.md`, `docs/release/requirement-matrix.md`
- current evidence: the daemon and Swift client already implement the explicit streaming contract: every parseable daemon request ends with a correlated `request_completed` terminal marker, `SubscribeEvents` emits `opensks.engine-stream-frame.v2` `stream_opened` / framed `event` / `stream_completed` or resumable `stream_failed` frames, and Swift routing completes responses on terminal markers instead of quiet-window timing. The runtime truth matrix still marked `stream.protocol` as Degraded with a stale missing-v2 reason.
- target change: promote `stream.protocol` capability truth from Degraded/Limited to Live/Available and replace the stale missing-v2 reason with `daemon_stream_protocol_v2_explicit_terminal_frames`. The capability evidence now cites the daemon marker, Swift explicit-terminal router, generated engine-stream-frame schema, daemon terminal-marker test, and subscribe-events v2 frame test. Release requirement #4 now records the implemented protocol slice while still keeping broader live provider/tool execution smoke as a remaining end-to-end gate.
- tests: `cargo fmt --all --check`, `cargo test -p opensks-contracts capability --locked`, `cargo test -p opensks-cli capability --locked`, `cargo test -p opensks-daemon request_response_ends_with_an_explicit_terminal_marker --locked`, `CLANG_MODULE_CACHE_PATH=/tmp/opensks-clang-module-cache MODULE_CACHE_DIR=/tmp/opensks-clang-module-cache swift test --disable-sandbox --package-path swift --scratch-path /tmp/opensks-swift-build-stream-truth --filter OpenSKSStudioTests.ContractsTests/testEnginePendingResponseRouterCompletesOnExplicitTerminalMarker`, `cargo run -p xtask -- capability-matrix --runtime-fixture release`, `cargo clippy -p opensks-contracts -p opensks-cli --all-targets -- -D warnings`, `bash scripts/check-architecture-ownership.sh`, `cargo run -p xtask -- direct-io-audit --check`, `cargo run -- acceptance audit`, `cargo run -- security audit`, and `git diff --check` passed. The first Swift attempt without module-cache overrides failed in the managed sandbox because SwiftPM tried to write under the user clang module cache; the rerun with `/tmp` module-cache paths passed.
- migration: generated `docs/runtime-truth-matrix.generated.md` changed only capability truth text for `stream.protocol`; no schema or data migration.
- removal/deletion: removed stale missing-v2 capability reason. No streaming code paths were removed.
- final evidence: runtime truth now reports `stream.protocol` as Live/Available with explicit v2 frame evidence. Overall acceptance remains 23 total, 22 passed, 1 partial, 0 failed because `mvp-004` still requires live OpenRouter/OpenAI credentials plus explicit remote-probe opt-in.
