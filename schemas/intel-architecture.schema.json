{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Architecture",
  "description": "The workspace architecture records, with the current freshness stamp.",
  "type": "object",
  "required": [
    "freshness",
    "records",
    "schema"
  ],
  "properties": {
    "freshness": {
      "$ref": "#/definitions/FreshnessStamp"
    },
    "records": {
      "type": "array",
      "items": {
        "$ref": "#/definitions/ArchitectureRecord"
      }
    },
    "schema": {
      "type": "string"
    }
  },
  "definitions": {
    "ArchitectureRecord": {
      "description": "One architecture record: a titled component/decision with detail and refs.",
      "type": "object",
      "required": [
        "detail",
        "id",
        "title"
      ],
      "properties": {
        "detail": {
          "type": "string"
        },
        "id": {
          "type": "string"
        },
        "refs": {
          "default": [],
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "title": {
          "type": "string"
        }
      }
    },
    "FreshnessStamp": {
      "description": "A deterministic freshness stamp for a workspace at one instant.\n\n- `head_hash` is the committed `HEAD` object id, or `None` when the workspace is not a Git repository (or has an unborn HEAD). - `worktree_hash` is a content-addressed digest over the tracked working-tree state, so any in-place edit flips it. - `index_hash` is a digest of the codegraph index (the staged knowledge of symbols), so a re-index flips it.",
      "type": "object",
      "required": [
        "in_repo",
        "index_hash",
        "schema",
        "worktree_hash"
      ],
      "properties": {
        "head_hash": {
          "type": [
            "string",
            "null"
          ]
        },
        "in_repo": {
          "type": "boolean"
        },
        "index_hash": {
          "type": "string"
        },
        "schema": {
          "type": "string"
        },
        "worktree_hash": {
          "type": "string"
        }
      }
    }
  }
}
