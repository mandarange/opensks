{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "WorkerRole",
  "description": "The role a worker plays in a pipeline (§7.4). Routing maps roles to models.",
  "type": "string",
  "enum": [
    "planner",
    "code_explorer",
    "implementer",
    "test_author",
    "reviewer",
    "security_reviewer",
    "image_generator",
    "arbiter"
  ]
}
