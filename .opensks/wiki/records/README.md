# OpenSKS Shared Wiki Records

This directory is intentionally trackable. Runtime indexes, generated context
packs, raw logs, caches, secrets, and worktrees stay in the managed local state
block in `.gitignore`.
